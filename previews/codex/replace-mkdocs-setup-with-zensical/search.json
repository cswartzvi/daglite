{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Welcome to Daglite","text":"<p>Lightweight, type-safe Python framework for building DAGs</p>","path":["Welcome to Daglite"],"tags":[]},{"location":"#what-is-daglite","level":2,"title":"What is Daglite?","text":"<p>Daglite is a lightweight Python framework for building Directed Acyclic Graphs (DAGs) with explicit data flow, complete type safety, and composable operations. It provides a simple, intuitive API for defining and executing computational pipelines—no infrastructure required.</p> <p>Built for computational work in restricted environments. Originally designed for operations research analysts working on air-gapped, Windows-only systems, Daglite solves a specific problem: building workflows that are easy to analyze, share with colleagues, and re-run—even after returning to a project months later.</p>","path":["Welcome to Daglite"],"tags":[]},{"location":"#core-principles","level":2,"title":"Core Principles","text":"<p>No infrastructure required Daglite runs anywhere Python runs—no databases, no containers, no cloud services, no servers. When you need more capabilities, plugins extend functionality without adding mandatory dependencies.</p> <p>Explicit over implicit All data dependencies are clearly defined. The DAG structure is static and analyzable before execution. Type checkers catch errors before runtime.</p> <p>Type-safe and modular Full support for <code>mypy</code>, <code>pyright</code>, <code>pyrefly</code>, and <code>ty</code>. Your IDE provides autocomplete and catches type mismatches.</p> <p>Lazy by default Execution only happens when you explicitly evaluate. Build your entire pipeline, then execute it once.</p> <p>Testable Pure functions make testing straightforward—no mocking infrastructure or database connections.</p>","path":["Welcome to Daglite"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick Example","text":"<pre><code>from daglite import task, evaluate\n\n@task\ndef fetch_data(url: str) -&gt; dict:\n    return requests.get(url).json()\n\n@task\ndef transform(data: dict, multiplier: int) -&gt; list:\n    return [item * multiplier for item in data[\"values\"]]\n\n@task\ndef save(items: list, path: str) -&gt; None:\n    with open(path, \"w\") as f:\n        json.dump(items, f)\n\n# Build the DAG (lazy - doesn't execute yet)\nresult = evaluate(\n    fetch_data(url=\"https://api.example.com/data\")\n    .then(transform, multiplier=10)\n    .then(save, path=\"output.json\")\n)\n</code></pre>","path":["Welcome to Daglite"],"tags":[]},{"location":"#when-to-use-daglite","level":2,"title":"When to Use Daglite","text":"","path":["Welcome to Daglite"],"tags":[]},{"location":"#perfect-for","level":3,"title":"Perfect for:","text":"<ul> <li>Data transformation pipelines and ETL workflows</li> <li>Machine learning pipelines (feature engineering, training, evaluation)</li> <li>Computational science workflows</li> <li>Analysts and data scientists who need reproducible workflows</li> <li>Air-gapped or restricted environments</li> <li>CLI tools with workflow orchestration</li> <li>Local development and prototyping</li> <li>Projects where simplicity and type safety matter</li> </ul>","path":["Welcome to Daglite"],"tags":[]},{"location":"#not-ideal-for","level":3,"title":"Not ideal for:","text":"<ul> <li>Production job scheduling with cron-like triggers → Use Airflow, Prefect</li> <li>Real-time streaming data → Use Kafka, Flink</li> <li>Distributed computing at massive scale → Use Spark, Dask</li> <li>Multi-tenant orchestration platforms → Use Dagster</li> </ul> <p>Daglite complements these excellent tools by providing a lightweight alternative for local, type-safe workflows.</p>","path":["Welcome to Daglite"],"tags":[]},{"location":"#key-features","level":2,"title":"Key Features","text":"","path":["Welcome to Daglite"],"tags":[]},{"location":"#fluent-api","level":3,"title":"Fluent API","text":"<p>Chain operations naturally with method chaining:</p> <pre><code>result = (\n    fetch(url=\"https://example.com\")\n    .then(parse, format=\"json\")\n    .then(validate, strict=True)\n    .then(save, path=\"output.json\")\n)\n</code></pre>","path":["Welcome to Daglite"],"tags":[]},{"location":"#map-reduce-patterns","level":3,"title":"Map-Reduce Patterns","text":"<p>Built-in support for fan-out/fan-in operations:</p> <pre><code># Cartesian product\nresults = task.product(x=[1, 2, 3], y=[10, 20])\n\n# Pairwise zip\nresults = task.zip(x=[1, 2, 3], y=[10, 20, 30])\n\n# Map and reduce\ntotal = (\n    square.product(x=[1, 2, 3, 4])\n    .map(double)\n    .join(sum_all)\n)\n</code></pre>","path":["Welcome to Daglite"],"tags":[]},{"location":"#complete-type-safety","level":3,"title":"Complete Type Safety","text":"<p>Works seamlessly with type checkers:</p> <pre><code>@task\ndef process(data: pd.DataFrame) -&gt; dict[str, float]:\n    return {\"mean\": data[\"value\"].mean()}\n\nresult = process(data=df)\nreveal_type(result)  # TaskFuture[dict[str, float]]\n</code></pre>","path":["Welcome to Daglite"],"tags":[]},{"location":"#async-execution","level":3,"title":"Async Execution","text":"<p>Run tasks in parallel with threading or multiprocessing:</p> <pre><code>result = evaluate(my_dag, use_async=True)\n\n# Per-task backends\n@task(backend_name=\"threading\")\ndef io_task(url: str) -&gt; bytes:\n    return requests.get(url).content\n\n@task(backend_name=\"multiprocessing\")\ndef cpu_task(data: np.ndarray) -&gt; np.ndarray:\n    return expensive_computation(data)\n</code></pre>","path":["Welcome to Daglite"],"tags":[]},{"location":"#next-steps","level":2,"title":"Next Steps","text":"<ul> <li>Getting Started - Install Daglite and learn the basics in 5 minutes</li> <li>User Guide - In-depth guides on tasks, composition, and evaluation</li> <li>CLI &amp; Pipelines - Command-line interface and pipeline definitions</li> <li>Plugins - Extend Daglite with serialization and more</li> <li>Examples - Real-world examples and patterns</li> <li>API Reference - Complete API documentation</li> </ul>","path":["Welcome to Daglite"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"<p>Real-world examples of using Daglite for common tasks.</p>","path":["Examples"],"tags":[]},{"location":"examples/#etl-pipeline","level":2,"title":"ETL Pipeline","text":"<pre><code>from daglite import task, evaluate\nimport pandas as pd\n\n@task\ndef extract(source: str) -&gt; pd.DataFrame:\n    \"\"\"Extract data from source.\"\"\"\n    return pd.read_csv(source)\n\n@task\ndef transform(df: pd.DataFrame, columns: list[str]) -&gt; pd.DataFrame:\n    \"\"\"Transform by selecting and cleaning columns.\"\"\"\n    return df[columns].dropna()\n\n@task\ndef load(df: pd.DataFrame, destination: str) -&gt; None:\n    \"\"\"Load data to destination.\"\"\"\n    df.to_parquet(destination)\n\n# Build and execute pipeline\nevaluate(\n    extract(source=\"data.csv\")\n    .then(transform, columns=[\"id\", \"name\", \"value\"])\n    .then(load, destination=\"output.parquet\")\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#parameter-sweep","level":2,"title":"Parameter Sweep","text":"<pre><code>@task\ndef train_model(lr: float, batch_size: int, epochs: int) -&gt; dict:\n    \"\"\"Train model with given hyperparameters.\"\"\"\n    model = Model(lr=lr)\n    model.fit(data, batch_size=batch_size, epochs=epochs)\n    return {\"accuracy\": model.evaluate(), \"lr\": lr, \"batch_size\": batch_size}\n\n@task\ndef find_best(results: list[dict]) -&gt; dict:\n    \"\"\"Find best hyperparameters.\"\"\"\n    return max(results, key=lambda r: r[\"accuracy\"])\n\n# Search hyperparameter space\nbest = evaluate(\n    train_model.product(\n        lr=[0.001, 0.01, 0.1],\n        batch_size=[32, 64],\n        epochs=[10]\n    )\n    .join(find_best)\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#parallel-data-processing","level":2,"title":"Parallel Data Processing","text":"<pre><code>@task\ndef fetch_user(user_id: int) -&gt; dict:\n    \"\"\"Fetch user data from API.\"\"\"\n    return api.get(f\"/users/{user_id}\")\n\n@task\ndef enrich_user(user: dict, include_orders: bool) -&gt; dict:\n    \"\"\"Enrich user with additional data.\"\"\"\n    if include_orders:\n        user[\"orders\"] = api.get(f\"/users/{user['id']}/orders\")\n    return user\n\n@task\ndef save_users(users: list[dict], path: str) -&gt; None:\n    \"\"\"Save users to database.\"\"\"\n    db.bulk_insert(\"users\", users)\n\n# Process users in parallel\nevaluate(\n    fetch_user.product(user_id=range(1, 101))\n    .map(enrich_user, include_orders=True)\n    .join(save_users, path=\"users.db\")\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#more-examples-coming-soon","level":2,"title":"More Examples Coming Soon!","text":"<p>We're working on adding more examples. In the meantime, check out the tests directory for additional usage patterns.</p>","path":["Examples"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting Started","text":"<p>This guide will help you install Daglite and build your first DAG in just a few minutes.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#installation","level":2,"title":"Installation","text":"<p>Daglite requires Python 3.10 or higher.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#basic-installation","level":3,"title":"Basic Installation","text":"<pre><code>uv pip install daglite\n</code></pre> <p>This installs the core library including the <code>daglite</code> command-line tool.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#development-installation","level":3,"title":"Development Installation","text":"<p>To contribute or run tests:</p> <pre><code>git clone https://github.com/cswartzvi/daglite.git\ncd daglite\nuv sync --all-groups\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#your-first-dag","level":2,"title":"Your First DAG","text":"<p>Let's build a simple data processing pipeline:</p> <pre><code>from daglite import task, evaluate\n\n# Step 1: Define tasks\n@task\ndef load_data(path: str) -&gt; list[int]:\n    \"\"\"Load numbers from a file.\"\"\"\n    with open(path) as f:\n        return [int(line) for line in f]\n\n@task\ndef square_all(numbers: list[int]) -&gt; list[int]:\n    \"\"\"Square each number.\"\"\"\n    return [x ** 2 for x in numbers]\n\n@task\ndef sum_values(numbers: list[int]) -&gt; int:\n    \"\"\"Sum all numbers.\"\"\"\n    return sum(numbers)\n\n# Step 2: Build the DAG\ndata = load_data(path=\"numbers.txt\")\nsquared = square_all(numbers=data)\ntotal = sum_values(numbers=squared)\n\n# Step 3: Execute\nresult = evaluate(total)\nprint(f\"Sum of squares: {result}\")\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#whats-happening","level":3,"title":"What's Happening?","text":"<ol> <li><code>@task</code> - Decorates functions to make them composable</li> <li>Calling tasks - Creates a \"future\" representing the task's eventual result</li> <li><code>evaluate()</code> - Executes the DAG and returns the final result</li> </ol> <p>Lazy Evaluation</p> <p>Calling a task doesn't execute it - it just builds the DAG. Execution only happens when you call <code>evaluate()</code>.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#the-fluent-api","level":2,"title":"The Fluent API","text":"<p>The same DAG can be written more concisely using method chaining:</p> <pre><code>result = evaluate(\n    load_data(path=\"numbers.txt\")\n    .then(square_all)\n    .then(sum_values)\n)\n</code></pre> <p>The <code>.then()</code> method automatically connects the output of one task to the input of the next.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#fan-out-with-product","level":2,"title":"Fan-Out with <code>.product()</code>","text":"<p>Process multiple inputs using Cartesian product:</p> <pre><code>@task\ndef add(x: int, y: int) -&gt; int:\n    return x + y\n\n# Create all combinations of x and y\nresults = evaluate(\n    add.product(x=[1, 2, 3], y=[10, 20])\n)\n# Result: [11, 21, 12, 22, 13, 23]\n#         (1+10, 1+20, 2+10, 2+20, 3+10, 3+20)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#map-reduce-pattern","level":2,"title":"Map-Reduce Pattern","text":"<p>Transform and aggregate sequences:</p> <pre><code>@task\ndef square(x: int) -&gt; int:\n    return x ** 2\n\n@task\ndef sum_all(values: list[int]) -&gt; int:\n    return sum(values)\n\n# Square each number, then sum them\nresult = evaluate(\n    square.product(x=[1, 2, 3, 4])\n    .join(sum_all)\n)\n# Result: 30 (1² + 2² + 3² + 4² = 1 + 4 + 9 + 16 = 30)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#with-map-for-transformations","level":3,"title":"With <code>.map()</code> for transformations","text":"<pre><code>@task\ndef double(x: int) -&gt; int:\n    return x * 2\n\nresult = evaluate(\n    square.product(x=[1, 2, 3, 4])  # [1, 4, 9, 16]\n    .map(double)                     # [2, 8, 18, 32]\n    .join(sum_all)                   # 60\n)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#pairwise-operations-with-zip","level":2,"title":"Pairwise Operations with <code>.zip()</code>","text":"<p>Process sequences element-by-element:</p> <pre><code>@task\ndef multiply(x: int, y: int) -&gt; int:\n    return x * y\n\n# Zip sequences together (must be same length)\nresults = evaluate(\n    multiply.zip(x=[1, 2, 3], y=[10, 20, 30])\n)\n# Result: [10, 40, 90]\n#         (1×10, 2×20, 3×30)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#passing-additional-parameters","level":2,"title":"Passing Additional Parameters","text":"<p>You can pass extra parameters inline:</p> <pre><code>@task\ndef scale(x: int, factor: int) -&gt; int:\n    return x * factor\n\n# Inline parameter with .then()\nresult = evaluate(\n    square.product(x=[1, 2, 3])\n    .map(scale, factor=10)  # Pass factor inline\n)\n# Result: [10, 40, 90]\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#type-safety","level":2,"title":"Type Safety","text":"<p>Daglite works seamlessly with type checkers:</p> <pre><code>@task\ndef get_number() -&gt; int:\n    return 42\n\n@task\ndef double(x: int) -&gt; int:\n    return x * 2\n\n@task\ndef to_string(x: int) -&gt; str:\n    return f\"Result: {x}\"\n\n# Type checker knows the result is str\nresult: str = evaluate(\n    get_number()\n    .then(double)\n    .then(to_string)\n)\n</code></pre> <p>Your IDE will provide autocomplete and catch type errors:</p> <pre><code># ❌ Type error - expects int, got str\nevaluate(double(x=\"hello\"))\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#parallel-execution","level":2,"title":"Parallel Execution","text":"<p>Daglite supports parallel execution of sibling tasks (tasks at the same level of the DAG) using threading or multiprocessing backends:</p> <pre><code>import requests\nfrom daglite import task, evaluate\n\n@task(backend_name=\"threading\")\ndef fetch_data(url: str) -&gt; dict:\n    # I/O-bound tasks benefit from threading\n    response = requests.get(url)\n    return response.json()\n\n@task\ndef combine(data_a: dict, data_b: dict, data_c: dict) -&gt; dict:\n    return {**data_a, **data_b, **data_c}\n\n# These three fetches run in parallel!\na = fetch_data(url=\"https://api.example.com/a\")\nb = fetch_data(url=\"https://api.example.com/b\")\nc = fetch_data(url=\"https://api.example.com/c\")\n\nresult = evaluate(combine(data_a=a, data_b=b, data_c=c))\n</code></pre> <p>For CPU-bound tasks, use the processes backend:</p> <pre><code>@task(backend_name=\"processes\")\ndef compute_heavy(x: int) -&gt; int:\n    # CPU-intensive work\n    return sum(i**2 for i in range(x))\n\n# Run calculations in parallel across processes\nresults = evaluate(\n    compute_heavy.product(x=[1000, 2000, 3000])\n)\n</code></pre> <p>Sync vs Async</p> <p>For most use cases, <code>evaluate()</code> with threading/processes backends is simpler than <code>evaluate_async()</code>. Use <code>evaluate_async()</code> only when you have async tasks or need to integrate with existing async code.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#next-steps","level":2,"title":"Next Steps","text":"<p>Now that you understand the basics:</p> <ul> <li>Learn more about tasks - Task decorators, options, and patterns</li> <li>Explore composition patterns - Calling tasks, <code>()</code>, <code>.product()</code>, <code>.zip()</code>, <code>.partial()</code></li> <li>Master the fluent API - <code>.then()</code>, <code>.map()</code>, <code>.join()</code></li> <li>See real examples - ETL pipelines, ML workflows, and more</li> </ul>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#common-patterns","level":2,"title":"Common Patterns","text":"","path":["Getting Started"],"tags":[]},{"location":"getting-started/#inline-pipeline","level":3,"title":"Inline Pipeline","text":"<pre><code>result = (\n    fetch(url=url)\n    .then(parse, format=\"json\")\n    .then(validate, strict=True)\n    .then(save, path=\"output.json\")\n)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#parallel-processing","level":3,"title":"Parallel Processing","text":"<pre><code>results = (\n    fetch_user.product(user_id=[1, 2, 3, 4, 5])\n    .map(enrich, include_avatar=True)\n    .join(save_all)\n)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#parameter-sweeps","level":3,"title":"Parameter Sweeps","text":"<pre><code>results = (\n    train_model.product(\n        lr=[0.001, 0.01, 0.1],\n        batch_size=[32, 64, 128]\n    )\n    .map(evaluate_model, test_data=test_set)\n    .join(find_best)\n)\n</code></pre> <p>You're Ready!</p> <p>You now know enough to build useful DAGs with Daglite. Check out the User Guide for in-depth documentation.</p>","path":["Getting Started"],"tags":[]},{"location":"zensical-viability-assessment/","level":1,"title":"Zensical Viability Assessment","text":"","path":["Zensical Viability Assessment"],"tags":[]},{"location":"zensical-viability-assessment/#what-i-checked","level":2,"title":"What I checked","text":"<p>I validated external adoption using GitHub metadata and repository-topic scanning for <code>topic:zensical</code>, then confirmed actual usage by checking each candidate repo for a <code>zensical.toml</code> file (or explicit Zensical workflow/config references).</p>","path":["Zensical Viability Assessment"],"tags":[]},{"location":"zensical-viability-assessment/#confirmed-example-projects-using-zensical","level":2,"title":"Confirmed example projects using Zensical","text":"<ul> <li>https://github.com/allthingslinux/tux</li> <li>https://github.com/mainsail-crew/docs</li> <li>https://github.com/geeksforsocialchange/PlaceCal-Handbook</li> <li>https://github.com/lanbaoshen/python-package-template</li> <li>https://github.com/maroph/maroph.github.io</li> <li>https://github.com/purpleclay/zensical-theme</li> <li>https://github.com/cssnr/zensical-action-docs</li> <li>https://github.com/hazzuk/karo-docs</li> <li>https://github.com/anibridge/anibridge-docs</li> <li>https://github.com/radiantBear/writeups</li> </ul>","path":["Zensical Viability Assessment"],"tags":[]},{"location":"zensical-viability-assessment/#viability-conclusion","level":2,"title":"Viability conclusion","text":"<p>Viable for Daglite, with one important caveat:</p> <ul> <li>Zensical has strong project momentum (active development, significant GitHub stars, and visible community uptake).</li> <li>It is designed by the Material for MkDocs team and intentionally serves the same documentation use case.</li> <li>However, your current docs relied on <code>mkdocstrings</code>; in this migration, those pages are converted to stubs and should be rebuilt with a Zensical-native API docs strategy.</li> </ul>","path":["Zensical Viability Assessment"],"tags":[]},{"location":"zensical-viability-assessment/#recommended-next-milestones","level":2,"title":"Recommended next milestones","text":"<ol> <li>Decide the long-term API documentation pipeline.</li> <li>Fill migration-focus stubs with ownership + timeline.</li> <li>Reintroduce advanced docs features incrementally (math, snippets, search tuning).</li> </ol>","path":["Zensical Viability Assessment"],"tags":[]},{"location":"api-reference/","level":1,"title":"API Reference","text":"<p>The API reference pages are currently placeholders while Daglite transitions from MkDocs + mkdocstrings to Zensical.</p>","path":["Api reference","API Reference"],"tags":[]},{"location":"api-reference/#current-status","level":2,"title":"Current status","text":"<ul> <li>Core API pages exist as migration stubs.</li> <li>Detailed symbols and signatures will be restored after selecting a Zensical-compatible generation strategy.</li> </ul>","path":["Api reference","API Reference"],"tags":[]},{"location":"api-reference/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Define the long-term API docs generation approach.</li> <li>Reintroduce module/class/function detail incrementally.</li> <li>Add CI checks to prevent API-reference drift.</li> </ul>","path":["Api reference","API Reference"],"tags":[]},{"location":"api-reference/engine/","level":1,"title":"Engine","text":"<p>This page was previously generated through MkDocs plugin infrastructure and is now a placeholder during the Zensical migration.</p>","path":["Api reference","Engine"],"tags":[]},{"location":"api-reference/engine/#todo","level":2,"title":"TODO","text":"<ul> <li>Define canonical objects/functions to document.</li> <li>Choose render strategy for signatures and examples.</li> <li>Add review checklist to keep reference synchronized with source.</li> </ul>","path":["Api reference","Engine"],"tags":[]},{"location":"api-reference/futures/","level":1,"title":"Futures","text":"<p>This page was previously generated through MkDocs plugin infrastructure and is now a placeholder during the Zensical migration.</p>","path":["Api reference","Futures"],"tags":[]},{"location":"api-reference/futures/#todo","level":2,"title":"TODO","text":"<ul> <li>Define canonical objects/functions to document.</li> <li>Choose render strategy for signatures and examples.</li> <li>Add review checklist to keep reference synchronized with source.</li> </ul>","path":["Api reference","Futures"],"tags":[]},{"location":"api-reference/pipelines/","level":1,"title":"Pipelines","text":"<p>This page was previously generated through MkDocs plugin infrastructure and is now a placeholder during the Zensical migration.</p>","path":["Api reference","Pipelines"],"tags":[]},{"location":"api-reference/pipelines/#todo","level":2,"title":"TODO","text":"<ul> <li>Define canonical objects/functions to document.</li> <li>Choose render strategy for signatures and examples.</li> <li>Add review checklist to keep reference synchronized with source.</li> </ul>","path":["Api reference","Pipelines"],"tags":[]},{"location":"api-reference/settings/","level":1,"title":"Settings","text":"<p>This page was previously generated through MkDocs plugin infrastructure and is now a placeholder during the Zensical migration.</p>","path":["Api reference","Settings"],"tags":[]},{"location":"api-reference/settings/#todo","level":2,"title":"TODO","text":"<ul> <li>Define canonical objects/functions to document.</li> <li>Choose render strategy for signatures and examples.</li> <li>Add review checklist to keep reference synchronized with source.</li> </ul>","path":["Api reference","Settings"],"tags":[]},{"location":"api-reference/tasks/","level":1,"title":"Tasks","text":"<p>This page was previously generated through MkDocs plugin infrastructure and is now a placeholder during the Zensical migration.</p>","path":["Api reference","Tasks"],"tags":[]},{"location":"api-reference/tasks/#todo","level":2,"title":"TODO","text":"<ul> <li>Define canonical objects/functions to document.</li> <li>Choose render strategy for signatures and examples.</li> <li>Add review checklist to keep reference synchronized with source.</li> </ul>","path":["Api reference","Tasks"],"tags":[]},{"location":"design/async-first-engine-refactoring/","level":1,"title":"Async-First Engine Refactoring Plan","text":"","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#goal","level":2,"title":"Goal","text":"<p>Unify <code>evaluate()</code> and <code>evaluate_async()</code> to use a single async-first internal implementation, eliminating code duplication while giving sync users automatic sibling concurrency.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#key-decisions","level":2,"title":"Key Decisions","text":"","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#1-evaluate-wraps-evaluate_async","level":3,"title":"1. <code>evaluate()</code> wraps <code>evaluate_async()</code>","text":"<p>The module-level <code>evaluate()</code> function detects whether an event loop is already running and delegates to <code>evaluate_async()</code>:</p> <pre><code>def evaluate(expr, *, plugins=None):\n    try:\n        asyncio.get_running_loop()\n    except RuntimeError:\n        pass  # No loop running, safe to proceed\n    else:\n        raise RuntimeError(\"Use evaluate_async() in async contexts\")\n    return asyncio.run(evaluate_async(expr, plugins=plugins))\n</code></pre> <p><code>Engine.evaluate()</code> similarly wraps <code>Engine.evaluate_async()</code>:</p> <pre><code>def evaluate(self, root: GraphBuilder) -&gt; Any:\n    try:\n        asyncio.get_running_loop()\n    except RuntimeError:\n        pass\n    else:\n        raise RuntimeError(\"Use evaluate_async() in async contexts\")\n    return asyncio.run(self.evaluate_async(root))\n</code></pre>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#2-remove-_run_inline-and-all-sync-only-execution-code","level":3,"title":"2. Remove <code>_run_Inline</code> and all sync-only execution code","text":"<p>Only keep <code>_run_async</code> as the execution path. This removes:</p> <p>engine.py: - <code>_run_Inline</code> (line 366) — sync execution loop - <code>_submit_node_sync</code> (line 499) — sync node submission - <code>_collect_result_sync</code> (line 547) — sync result collection - <code>_NodeFutureWrapper</code> (line 640) — sync future wrapper - <code>_MapFutureWrapper</code> (line 648) — mapped sync future wrapper - <code>_materialize_sync</code> (line 754) — sync generator materialization - <code>_validate_sync_compatibility</code> (line 330) — no longer needed (see decision 3) - <code>_validate_async_compatibility</code> (line 347) — no longer needed (see decision 3)</p> <p>nodes.py: - <code>_run_sync_impl</code> (line 290) — sync node implementation (~148 lines) - <code>TaskNode.run()</code> (line 82) — sync execution method - <code>MapTaskNode.run()</code> (line 245) — sync mapped execution method</p> <p>Total: ~450 lines of duplicated code removed.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#3-remove-both-validation-functions","level":3,"title":"3. Remove both validation functions","text":"<ul> <li> <p><code>_validate_sync_compatibility</code> (line 330): No longer needed because <code>evaluate()</code> now delegates to the async path, which handles both sync and async task functions via <code>_run_async_impl</code>.</p> </li> <li> <p><code>_validate_async_compatibility</code> (line 347): Sync functions on the Inline backend are suboptimal (they block the event loop) but not incorrect. Removing this validation simplifies the API — users no longer need to think about backend/function compatibility. If performance matters, they can switch backends.</p> </li> </ul>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#4-_run_async_impl-already-handles-sync-functions-nodespy-line-522","level":3,"title":"4. <code>_run_async_impl</code> already handles sync functions (nodes.py line 522)","text":"<p>No changes needed here. The existing code handles both sync and async task functions:</p> <pre><code>if inspect.iscoroutinefunction(func):\n    result = await func(**resolved_inputs)\nelse:\n    result = func(**resolved_inputs)\n</code></pre>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#5-use-first_exception-for-fail-fast","level":3,"title":"5. Use <code>FIRST_EXCEPTION</code> for fail-fast","text":"<p>Switch from <code>ALL_COMPLETED</code> (current default) to <code>FIRST_EXCEPTION</code> to cancel pending siblings immediately on failure instead of waiting for all to finish:</p> <pre><code>done, pending = await asyncio.wait(tasks.keys(), return_when=asyncio.FIRST_EXCEPTION)\ntry:\n    for task in done:\n        nid = tasks[task]\n        result = task.result()\n        ready.extend(state.mark_complete(nid, result))\nexcept Exception:\n    for t in pending:\n        t.cancel()\n    await asyncio.gather(*pending, return_exceptions=True)\n    raise\n</code></pre> <p>Behavioral change: In-flight sibling tasks are cancelled on first failure rather than being allowed to complete. This is the desired fail-fast behavior.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#6-remove-is_async-from-the-hook-system","level":3,"title":"6. Remove <code>is_async</code> from the hook system","text":"<p>The <code>is_async</code> parameter on graph-level hooks (<code>before_graph_execute</code>, <code>after_graph_execute</code>, <code>on_graph_error</code>) becomes meaningless when all execution is async-first. Remove it from:</p> <p>Hook specs (<code>plugins/hooks/specs.py</code>): - <code>GraphSpec.before_graph_execute</code> (line 246) - <code>GraphSpec.after_graph_execute</code> (line 265) - <code>GraphSpec.on_graph_error</code> (line 285)</p> <p>Builtin plugins (<code>plugins/builtin/logging.py</code>): - <code>LifecycleLoggingPlugin.before_graph_execute</code> (line 300) - <code>LifecycleLoggingPlugin.after_graph_execute</code> (line 314) - <code>LifecycleLoggingPlugin.on_graph_error</code> (line 328)</p> <p>Extras plugins (<code>extras/rich/src/daglite_rich/</code>): - <code>RichProgressPlugin.before_graph_execute</code> (progress.py line 81) - <code>RichProgressPlugin.after_graph_execute</code> (progress.py line 161) - <code>RichLifecycleLoggingPlugin</code> (logging.py — inherits from LifecycleLoggingPlugin)</p> <p>Test plugins (<code>tests/examples/plugins.py</code>): - <code>CounterPlugin</code> (line 43, 48) - <code>ParameterCapturePlugin</code> (lines 92, 98, 103, 110) - <code>OrderTrackingPlugin</code> (lines 175, 180) - <code>ErrorRaisingPlugin</code> (lines 229, 235)</p> <p>Tests (<code>tests/integration/test_plugins.py</code>): - Assertions checking <code>is_async</code> value (lines 185, 190) - Custom hook implementations in tests (line 247)</p> <p>Note: The <code>Task.is_async</code> field (<code>tasks.py</code> line 402) is a different concept — it tracks whether the task function itself is async. This stays.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#7-add-nested-call-guard","level":3,"title":"7. Add nested call guard","text":"<p>Prevent <code>evaluate()</code>/<code>evaluate_async()</code> from being called within a task function using a <code>ContextVar</code>:</p> <pre><code>_IN_EVALUATION: ContextVar[bool] = ContextVar(\"in_evaluation\", default=False)\n\nasync def evaluate_async(expr, *, plugins=None):\n    if _IN_EVALUATION.get():\n        raise RuntimeError(\n            \"Cannot call evaluate()/evaluate_async() from within a task. \"\n            \"Compose tasks using the fluent API instead.\"\n        )\n    token = _IN_EVALUATION.set(True)\n    try:\n        engine = Engine(plugins=plugins)\n        return await engine.evaluate_async(expr)\n    finally:\n        _IN_EVALUATION.reset(token)\n</code></pre> <p>This guard belongs in the module-level entry points (<code>evaluate</code> and <code>evaluate_async</code>), not in the <code>Engine</code> class, since the <code>Engine</code> methods are the internal implementation.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#8-keep-plugin-hooks-synchronous","level":3,"title":"8. Keep plugin hooks synchronous","text":"<p>All plugin hooks remain synchronous. The rationale:</p> <ul> <li>Worker-side hooks cannot be async (they run in thread/process pool workers with no event loop)</li> <li>Graph-level and coordinator-side hooks are quick operations that don't benefit from being async</li> <li>pluggy doesn't natively support async hooks well</li> <li>Converting would require updating all plugin implementations (builtin, extras, tests) for minimal benefit</li> </ul> <p>If a specific async hook is needed later (e.g., async cache backend), add a targeted async variant rather than converting the whole system.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#testing-strategy","level":2,"title":"Testing Strategy","text":"<p>Since both <code>evaluate()</code> and <code>evaluate_async()</code> now share a single code path (<code>_run_async</code>), parameterizing every test over both modes adds limited value. Instead:</p> <p>Entry-point tests — targeted tests for the thin <code>evaluate()</code> wrapper: <pre><code>def test_evaluate_rejects_running_loop():\n    \"\"\"evaluate() raises when called from async context.\"\"\"\n    async def inner():\n        with pytest.raises(RuntimeError, match=\"Use evaluate_async\"):\n            evaluate(my_task(x=1))\n    asyncio.run(inner())\n\ndef test_evaluate_rejects_nested_call():\n    \"\"\"evaluate() raises when called from within a task.\"\"\"\n    @task\n    def outer():\n        return evaluate(inner_task(x=1))  # Should raise\n    with pytest.raises(RuntimeError, match=\"Cannot call evaluate\"):\n        evaluate(outer())\n</code></pre></p> <p>Core evaluation tests — use <code>evaluate_async()</code> directly: <pre><code>async def test_basic_chain():\n    result = await evaluate_async(my_task(x=1))\n    assert result == expected\n</code></pre></p> <p>Existing <code>evaluate()</code> tests — keep working as-is (they exercise the <code>asyncio.run</code> wrapper path). No need to rewrite them unless they tested sync-specific internals.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#performance","level":2,"title":"Performance","text":"<p>Async overhead is negligible (~microseconds per task) for real workloads. The per-node cost of <code>asyncio.create_task</code> + <code>wrap_future</code> + <code>await</code> is 1-5 microseconds. For a 100-node DAG, that's 0.1-0.5ms total — dwarfed by any actual task execution time. The current sync path also has overhead from <code>concurrent.futures.wait()</code>.</p> <p>The only scenario where this matters is a micro-benchmark with hundreds of trivial no-op tasks. No real workload looks like that. If profiling proves otherwise, optimize then.</p>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/async-first-engine-refactoring/#migration-checklist","level":2,"title":"Migration Checklist","text":"<ol> <li>Add nested call guard (<code>ContextVar</code>) to <code>evaluate()</code> and <code>evaluate_async()</code></li> <li>Rewrite <code>Engine.evaluate()</code> to wrap <code>Engine.evaluate_async()</code> with loop detection</li> <li>Remove <code>_run_Inline</code> and all sync-only code paths from <code>engine.py</code></li> <li>Remove <code>_run_sync_impl</code>, <code>TaskNode.run()</code>, <code>MapTaskNode.run()</code> from <code>nodes.py</code></li> <li>Remove <code>_validate_sync_compatibility</code> and <code>_validate_async_compatibility</code></li> <li>Switch <code>_run_async</code> to use <code>FIRST_EXCEPTION</code> in <code>asyncio.wait()</code></li> <li>Remove <code>is_async</code> parameter from hook specs, all plugin implementations, and tests</li> <li>Update tests: add entry-point-specific tests, remove sync-internal tests</li> <li>Update CLI (<code>extras/cli/src/daglite_cli/cmd_run.py</code>) — both paths still work since <code>evaluate()</code> wraps <code>evaluate_async()</code></li> <li>Update documentation (<code>docs/user-guide/evaluation.md</code>, <code>docs/api-reference/engine.md</code>)</li> </ol>","path":["Design","Async-First Engine Refactoring Plan"],"tags":[]},{"location":"design/caching-and-checkpointing-design/","level":1,"title":"Caching and Checkpointing Design","text":"<p>Status: Design Complete, Ready for Implementation Created: 2024-12-09 Target: daglite v0.5.0</p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#table-of-contents","level":2,"title":"Table of Contents","text":"<ol> <li>Overview</li> <li>Architecture</li> <li>Implementation Phases</li> <li>API Reference</li> <li>Implementation Details</li> <li>Testing Strategy</li> <li>Future Enhancements</li> </ol>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#overview","level":2,"title":"Overview","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#goals","level":3,"title":"Goals","text":"<p>Add production-ready caching and checkpointing to daglite with: - Type-safe: Leverage Python's type system for automatic serialization - Modular: Plugin-based architecture for extensibility - Performant: Smart hashing strategies for large objects - Simple: Intuitive API with sensible defaults - Professional: Match or exceed Prefect/Dagster/Hamilton capabilities</p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#key-concepts","level":3,"title":"Key Concepts","text":"<p>Caching (Performance): - Content-addressable (hash-based) storage - Transparent and automatic - Hash = f(function_source, parameters)5/ - Ephemeral (safe to clear) - Purpose: Skip expensive recomputation</p> <p>Checkpointing (Resilience &amp; Audit): - Named, versioned artifacts - Explicit via <code>.checkpoint(name)</code> - Rich metadata (timestamp, git hash, params) - Persistent (precious audit trail) - Purpose: Debugging, recovery, compliance</p> <p>Side Effects (Flexibility): - Non-branching via <code>.also(callback)</code> - Logging, metrics, notifications - Return value ignored - Purpose: Observability without affecting flow</p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#design-principles","level":3,"title":"Design Principles","text":"<ol> <li>Separation of Concerns: Cache, checkpoint, and side effects are distinct</li> <li>Shared Infrastructure: All use same serialization registry</li> <li>Hash-Based Keys: No explicit cache_key needed (content-addressable)</li> <li>Type Registration: Register once, works everywhere</li> <li>Plugin Architecture: Stores live in extras/ packages</li> </ol>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#architecture","level":2,"title":"Architecture","text":"<pre><code>┌─────────────────────────────────────────────────────┐\n│         Serialization Registry (Core)               │\n│  • Type → Serializer/Deserializer                   │\n│  • Type → Hash Strategy                             │\n│  • Format preferences (CSV vs Parquet)              │\n└─────────────────────────────────────────────────────┘\n                    ↑            ↑\n          ┌─────────┴────────┐   └────────────┐\n          │                  │                │\n┌─────────────────┐  ┌───────────────────┐  ┌──────────────┐\n│  Cache Store    │  │ Checkpoint Store  │  │ .also()      │\n│  (extras/)      │  │  (extras/)        │  │  (core)      │\n│                 │  │                   │  │              │\n│ • Hash-based    │  │ • Named locations │  │ • Side       │\n│ • Transparent   │  │ • Versioned       │  │   effects    │\n│ • LRU eviction  │  │ • Rich metadata   │  │ • Logging    │\n│ • Git layout    │  │ • S3 support      │  │ • Metrics    │\n└─────────────────┘  └───────────────────┘  └──────────────┘\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#package-structure","level":3,"title":"Package Structure","text":"<pre><code>daglite/\n├── src/daglite/\n│   ├── serialization.py         # SerializationRegistry + built-in strategies\n│   ├── caching/\n│   │   ├── __init__.py\n│   │   ├── store.py             # CacheStore Protocol\n│   │   ├── hash.py              # default_cache_hash()\n│   │   └── eviction.py          # Eviction policies\n│   ├── tasks.py                 # @task decorator updates\n│   └── ... other files ...\n│\n├── extras/\n│   ├── serialization/\n│   │   ├── src/daglite_serialization/  # Single package, multiple extras\n│   │   │   ├── __init__.py       # register_all()\n│   │   │   ├── numpy.py          # NumPy handlers\n│   │   │   ├── pandas.py         # Pandas handlers\n│   │   │   └── pillow.py         # Pillow handlers\n│   │   └── pyproject.toml        # [numpy], [pandas], [pillow], [all]\n│   │\n│   ├── cache/\n│   │   └── daglite-cache-file/       # Future\n│   │\n│   └── checkpoint/\n│       ├── daglite-checkpoint-file/  # Future\n│       └── daglite-checkpoint-s3/    # Future\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#implementation-phases","level":2,"title":"Implementation Phases","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-1-serialization-registry-complete","level":3,"title":"Phase 1: Serialization Registry ✅ COMPLETE","text":"<p>Goal: Core infrastructure that everything builds on</p> <p>Files created:</p> <ul> <li><code>src/daglite/serialization.py</code> - Single module with registry and built-in strategies</li> <li><code>extras/serialization/daglite-serialization/</code> - Plugin package</li> <li><code>tests/test_serialization.py</code> - 39 tests passing</li> <li><code>extras/serialization/tests/test_serialization_plugin.py</code> - 16 tests passing</li> </ul> <p>Key classes:</p> <ul> <li><code>SerializationRegistry</code>: Main registry with module-based error messages</li> <li><code>SerializationHandler</code>: Per-format handler</li> <li><code>HashStrategy</code>: Per-type hash function</li> </ul> <p>Success criteria:</p> <ul> <li> Register custom types with multiple formats</li> <li> Strict TypeError for unregistered types (no repr() fallback)</li> <li> Plugin package (<code>daglite_serialization</code>) with numpy/pandas/pillow</li> <li> Smart hash strategies with start/middle/end sampling (&lt;100ms for 800MB arrays)</li> <li> 100% test coverage (39 core + 16 plugin tests)</li> <li> Recursive hashing via closures for nested data structures</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-2-caching-infrastructure","level":3,"title":"Phase 2: Caching Infrastructure","text":"<p>Goal: Content-addressable caching with @task integration</p> <p>Files to create:</p> <ul> <li><code>src/daglite/caching/store.py</code> (Protocol)</li> <li><code>src/daglite/caching/hash.py</code></li> <li><code>src/daglite/caching/eviction.py</code></li> <li><code>extras/cache/daglite-cache-file/src/daglite/plugins/cache/file.py</code></li> <li><code>extras/cache/daglite-cache-file/src/daglite/plugins/cache/layout.py</code></li> </ul> <p>Success criteria:</p> <ul> <li> <code>@task(cache=True)</code> works</li> <li> Hash-based cache keys (no explicit cache_key)</li> <li> Git-style file layout (Windows-safe)</li> <li> LRU eviction with configurable size limits</li> <li> TTL support</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-3-checkpointing","level":3,"title":"Phase 3: Checkpointing","text":"<p>Goal: Named, versioned artifacts for debugging/audit</p> <p>Files to create: - <code>src/daglite/checkpointing/store.py</code> (Protocol) - <code>extras/checkpoint/daglite-checkpoint-file/...</code> - <code>extras/checkpoint/daglite-checkpoint-s3/...</code></p> <p>Success criteria:</p> <ul> <li> <code>.checkpoint(name)</code> method on TaskFuture</li> <li> Versioned storage</li> <li> Rich metadata (timestamp, git hash, params)</li> <li> S3 backend support</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-4-side-effects","level":3,"title":"Phase 4: Side Effects","text":"<p>Goal: <code>.also()</code> for non-branching operations</p> <p>Files to modify: - <code>src/daglite/task.py</code> (add <code>.also()</code> to TaskFuture)</p> <p>Success criteria:</p> <ul> <li> <code>.also(callback)</code> runs side effects</li> <li> Doesn't affect data flow</li> <li> Used internally by <code>.checkpoint()</code></li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-5-plugin-registration-complete-done-with-phase-1","level":3,"title":"Phase 5: Plugin Registration ✅ COMPLETE (done with Phase 1)","text":"<p>Goal: Auto-register pandas, numpy serializers</p> <p>Implemented:</p> <ul> <li>Single package: <code>extras/serialization/daglite-serialization/</code></li> <li>Package name: <code>daglite_serialization</code> (flat namespace)</li> <li>Optional dependencies: <code>[numpy]</code>, <code>[pandas]</code>, <code>[pillow]</code>, <code>[all]</code></li> <li>API: <code>register_all()</code> convenience function</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#api-reference","level":2,"title":"API Reference","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#task-decorator-parameters","level":3,"title":"@task Decorator Parameters","text":"<pre><code>@task(\n    # === CACHING ===\n    cache: bool = False,\n    # Enable content-addressable caching\n\n    cache_ttl: int | timedelta | None = None,\n    # Time-to-live for cache entries\n    # None = never expire, int = seconds, timedelta = duration\n\n    cache_hash: Callable[[Callable, dict], str] | None = None,\n    # Custom hash function: (function, bound_args) → hash_string\n    # None = use default (source + params)\n\n    ....  # existing parameters\n)\ndef my_task(...) -&gt; ...:\n    ...\n</code></pre> <p>Examples:</p> <pre><code># Simple caching\n@task(cache=True)\ndef compute(x: int, y: int) -&gt; pd.DataFrame:\n    # Expensive computation\n    ...\n\n# With TTL\n@task(cache=True, cache_ttl=3600)  # 1 hour\ndef fetch_stock_price(ticker: str) -&gt; float:\n    return api.get_latest_price(ticker)\n\n# Completely custom hash\ndef my_hash(func, bound_args):\n    return hashlib.sha256(\n        func.__name__.encode() +\n        str(bound_args['version']).encode()\n    ).hexdigest()\n\n@task(cache=True, cache_hash=my_hash)\ndef train_model(data: pd.DataFrame, version: str) -&gt; Model:\n    ...\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#taskfuture-methods","level":3,"title":"TaskFuture Methods","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#also-side-effects","level":4,"title":"<code>.also()</code> - Side Effects","text":"<pre><code>def also(self, callback: Callable[[T], None]) -&gt; TaskFuture[T]:\n    \"\"\"\n    Execute a side effect without affecting data flow.\n\n    The callback receives the task result but its return value\n    is ignored. Exceptions in callbacks are logged but don't\n    stop the pipeline.\n\n    Args:\n        callback: Function that takes result, returns None\n\n    Returns:\n        Same TaskFuture (for chaining)\n    \"\"\"\n</code></pre> <p>Examples:</p> <pre><code>result = (\n    compute()\n    .also(lambda x: logger.info(f\"Result: {x}\"))\n    .also(lambda x: metrics.record(\"value\", x))\n    .also(save_to_audit_log)\n    .then(next_task)\n)\n\n# Save copy without branching\npipeline = (\n    process_data()\n    .also(lambda x: x.to_csv(\"/audit/data.csv\"))\n    .then(analyze)  # Main flow continues\n)\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#checkpoint-named-artifacts","level":4,"title":"<code>.checkpoint()</code> - Named Artifacts","text":"<pre><code>def checkpoint(\n    self,\n    name: str,\n    store: CheckpointStore | None = None,\n    versioning: bool = True,\n    metadata: dict[str, Any] | None = None,\n) -&gt; TaskFuture[T]:\n    \"\"\"\n    Save task result as a named, versioned artifact.\n\n    Args:\n        name: Human-readable checkpoint name\n        store: Optional custom checkpoint store\n        versioning: If True, keep multiple versions\n        metadata: Additional metadata to store\n\n    Returns:\n        Same TaskFuture (for chaining)\n    \"\"\"\n</code></pre> <p>Examples:</p> <pre><code>result = (\n    expensive_computation()\n    .checkpoint(\"raw_features\")\n    .then(normalize)\n    .checkpoint(\"normalized_features\")\n    .then(train_model)\n    .checkpoint(\"trained_model\", metadata={'accuracy': 0.95})\n)\n\n# Different store (e.g., S3)\nfrom daglite.plugins.checkpoint import S3CheckpointStore\n\ns3_store = S3CheckpointStore(bucket=\"ml-artifacts\")\n\nresult = (\n    train_model()\n    .checkpoint(\"model_v1\", store=s3_store)\n    .then(evaluate)\n)\n\n# Later: recover from checkpoint\ncheckpoint_store = FileCheckpointStore(Path(\"/checkpoints\"))\nfeatures = checkpoint_store.load(\"normalized_features\", version=\"latest\")\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#serializationregistry","level":3,"title":"SerializationRegistry","text":"<pre><code>class SerializationRegistry:\n    \"\"\"Central registry for type-based serialization\"\"\"\n\n    def register(\n        self,\n        type_: Type[T],\n        serializer: Callable[[T], bytes],\n        deserializer: Callable[[bytes], T],\n        format: str = 'default',\n        file_extension: str = 'bin',\n        make_default: bool = False,\n    ) -&gt; None:\n        \"\"\"Register a serialization handler\"\"\"\n\n    def register_hash_strategy(\n        self,\n        type_: Type,\n        hasher: Callable[[Any], str],\n        description: str = \"\",\n    ) -&gt; None:\n        \"\"\"Register how to hash a type for cache keys\"\"\"\n\n    def serialize(\n        self,\n        obj: Any,\n        format: str | None = None,\n    ) -&gt; tuple[bytes, str]:\n        \"\"\"Serialize object → (bytes, file_extension)\"\"\"\n\n    def deserialize(\n        self,\n        data: bytes,\n        type_: Type[T],\n        format: str | None = None,\n    ) -&gt; T:\n        \"\"\"Deserialize bytes → object\"\"\"\n\n    def hash_value(self, obj: Any) -&gt; str:\n        \"\"\"Hash a value using registered strategy\"\"\"\n\n    def set_default_format(self, type_: Type, format: str) -&gt; None:\n        \"\"\"Set default format for a type\"\"\"\n\n    def get_extension(self, type_: Type, format: str | None = None) -&gt; str:\n        \"\"\"Get file extension for type/format\"\"\"\n\n# Global instance\ndefault_registry = SerializationRegistry()\n</code></pre> <p>Usage:</p> <pre><code>from daglite.serialization import default_registry\n\n# Register custom type\nclass MyModel:\n    def to_bytes(self) -&gt; bytes: ...\n    @classmethod\n    def from_bytes(cls, data: bytes) -&gt; 'MyModel': ...\n\ndefault_registry.register(\n    MyModel,\n    lambda m: m.to_bytes(),\n    lambda b: MyModel.from_bytes(b),\n    format='default',\n    file_extension='model'\n)\n\n# Register hash strategy (don't serialize whole model!)\ndefault_registry.register_hash_strategy(\n    MyModel,\n    lambda m: m.get_version_hash(),\n    \"Hash model version and config\"\n)\n\n# Change format preference\ndefault_registry.set_default_format(pd.DataFrame, 'parquet')\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#cachestore-protocol","level":3,"title":"CacheStore Protocol","text":"<pre><code>class CacheStore(Protocol):\n    \"\"\"Protocol for cache storage backends\"\"\"\n\n    def get(self, hash_key: str, return_type: Type[T]) -&gt; Optional[T]:\n        \"\"\"Retrieve cached value by hash\"\"\"\n\n    def put(\n        self,\n        hash_key: str,\n        value: Any,\n        format: str | None = None,\n        ttl: int | None = None,\n    ) -&gt; None:\n        \"\"\"Store value with hash key\"\"\"\n\n    def invalidate(self, hash_key: str) -&gt; None:\n        \"\"\"Remove cached entry\"\"\"\n\n    def clear(self) -&gt; None:\n        \"\"\"Clear entire cache\"\"\"\n\n    def get_stats(self) -&gt; CacheStats:\n        \"\"\"Get cache statistics\"\"\"\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#checkpointstore-protocol","level":3,"title":"CheckpointStore Protocol","text":"<pre><code>class CheckpointStore(Protocol):\n    \"\"\"Protocol for checkpoint storage backends\"\"\"\n\n    def save(\n        self,\n        name: str,\n        value: Any,\n        metadata: dict[str, Any],\n        format: str | None = None,\n    ) -&gt; str:\n        \"\"\"Save checkpoint, returns version identifier\"\"\"\n\n    def load(\n        self,\n        name: str,\n        version: str | None = None,\n        return_type: Type[T] | None = None,\n    ) -&gt; T:\n        \"\"\"Load checkpoint (latest or specific version)\"\"\"\n\n    def list_versions(self, name: str) -&gt; list[CheckpointVersion]:\n        \"\"\"List all versions of a checkpoint\"\"\"\n\n    def delete(self, name: str, version: str | None = None) -&gt; None:\n        \"\"\"Delete checkpoint\"\"\"\n\n@dataclass\nclass CheckpointVersion:\n    version: str\n    timestamp: datetime\n    metadata: dict[str, Any]\n    size_bytes: int\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#implementation-details","level":2,"title":"Implementation Details","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#hash-strategies-performance-critical","level":3,"title":"Hash Strategies (Performance Critical!)","text":"<p>The default hash function can be devastating for large objects:</p> <pre><code># BAD: 2 seconds to hash 800MB array\nlarge_array = np.random.rand(10000, 10000)\nhash = hashlib.sha256(pickle.dumps(large_array)).hexdigest()\n</code></pre> <p>Solution: Sample-based hashing in <code>hash_strategies.py</code></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#numpy-arrays","level":4,"title":"Numpy Arrays","text":"<pre><code>def hash_numpy_array(arr: np.ndarray) -&gt; str:\n    \"\"\"\n    Fast hash: metadata + sample of data\n\n    Time: ~1ms for 800MB array (vs ~2000ms for full hash)\n    \"\"\"\n    h = hashlib.sha256()\n\n    # Metadata (always fast)\n    h.update(str(arr.shape).encode())\n    h.update(str(arr.dtype).encode())\n\n    # Sample data\n    if arr.size &gt; 1000:\n        flat = arr.flatten()\n        sample = np.concatenate([flat[:500], flat[-500:]])\n        h.update(sample.tobytes())\n    else:\n        h.update(arr.tobytes())\n\n    return h.hexdigest()\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#pandas-dataframes","level":4,"title":"Pandas DataFrames","text":"<pre><code>def hash_dataframe(df: pd.DataFrame) -&gt; str:\n    \"\"\"\n    Fast hash: schema + sample rows\n\n    Time: ~10ms for 1M rows (vs ~5000ms for full hash)\n    \"\"\"\n    h = hashlib.sha256()\n\n    # Schema\n    h.update(str(df.shape).encode())\n    h.update(str(df.dtypes.to_dict()).encode())\n    h.update(str(df.columns.tolist()).encode())\n\n    # Sample rows\n    if len(df) &gt; 1000:\n        sample = pd.concat([df.head(500), df.tail(500)])\n        h.update(pd.util.hash_pandas_object(sample).values.tobytes())\n    else:\n        h.update(pd.util.hash_pandas_object(df).values.tobytes())\n\n    return h.hexdigest()\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#images","level":4,"title":"Images","text":"<pre><code>def hash_image(img: Image.Image) -&gt; str:\n    \"\"\"\n    Fast hash: downsample to 32x32\n\n    Much faster than full resolution, catches all visible changes\n    \"\"\"\n    h = hashlib.sha256()\n    h.update(str(img.size).encode())\n    h.update(str(img.mode).encode())\n\n    thumb = img.resize((32, 32), Image.Resampling.LANCZOS)\n    h.update(thumb.tobytes())\n\n    return h.hexdigest()\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#git-style-cache-layout-windows-safe","level":3,"title":"Git-Style Cache Layout (Windows-Safe)","text":"<p>Problem: Windows path limit (260 chars), filesystem limits on files per directory</p> <p>Solution: Git's proven approach</p> <pre><code>class GitStyleCacheLayout:\n    \"\"\"\n    Git-style hash storage.\n\n    Hash: \"abcdef1234567890...\"\n    Path: base/ab/cd/ef1234567890...\n\n    Benefits:\n    - Windows-safe (stays under 260 chars)\n    - Balanced tree (256 buckets per level)\n    - Fast lookups\n    - Familiar pattern\n    \"\"\"\n\n    def __init__(self, base_path: Path, split_pattern: tuple[int, ...] = (2, 2)):\n        \"\"\"\n        Args:\n            split_pattern: (2, 2) means: ab/cd/remainder\n        \"\"\"\n        self.base_path = base_path\n        self.split_pattern = split_pattern\n\n    def get_path(self, hash_key: str, extension: str) -&gt; Path:\n        \"\"\"\n        Convert hash to path.\n\n        Example: \"abcdef123...\" → base/ab/cd/ef123....ext\n        \"\"\"\n        parts = []\n        offset = 0\n\n        for length in self.split_pattern:\n            parts.append(hash_key[offset:offset + length])\n            offset += length\n\n        filename = hash_key[offset:]\n        return self.base_path.joinpath(*parts, f\"{filename}.{extension}\")\n</code></pre> <p>Path length analysis: <pre><code>C:/Users/Name/AppData/Local/daglite/cache  (47 chars)\n+ ab/cd/                                    (6 chars)\n+ ef1234567890...parquet                    (68 chars)\n= 121 chars total (well under 260!)\n</code></pre></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#cache-eviction-policies","level":3,"title":"Cache Eviction Policies","text":"<p>LRU (Recommended Default): <pre><code># Evict least recently used entries\nSELECT hash_key, file_path, size_bytes\nFROM cache_entries\nORDER BY last_accessed ASC\nLIMIT ?\n</code></pre></p> <p>LFU: <pre><code># Evict least frequently used\nSELECT hash_key, file_path, size_bytes\nFROM cache_entries\nORDER BY access_count ASC, last_accessed ASC\nLIMIT ?\n</code></pre></p> <p>SIZE_FIRST: <pre><code># Evict largest files first (maximize freed space)\nSELECT hash_key, file_path, size_bytes\nFROM cache_entries\nORDER BY size_bytes DESC, last_accessed ASC\nLIMIT ?\n</code></pre></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#cache-metadata-store","level":4,"title":"Cache Metadata Store","text":"<p>Use SQLite for lightweight metadata tracking:</p> <pre><code>class CacheMetadataStore:\n    \"\"\"\n    Track cache metadata in SQLite.\n\n    Schema:\n    CREATE TABLE cache_entries (\n        hash_key TEXT PRIMARY KEY,\n        file_path TEXT NOT NULL,\n        size_bytes INTEGER NOT NULL,\n        created_at TEXT NOT NULL,\n        last_accessed TEXT NOT NULL,\n        access_count INTEGER NOT NULL DEFAULT 1,\n        ttl_expires TEXT\n    )\n    \"\"\"\n\n    def __init__(self, db_path: Path):\n        self.db_path = db_path\n        self._init_db()\n\n    def record_access(self, hash_key: str) -&gt; None:\n        \"\"\"Update last_accessed and increment access_count\"\"\"\n\n    def record_put(self, hash_key: str, file_path: Path, size: int, ttl: int | None):\n        \"\"\"Record new cache entry\"\"\"\n\n    def get_eviction_candidates(\n        self,\n        policy: EvictionPolicy,\n        target_bytes: int,\n    ) -&gt; list[CacheEntry]:\n        \"\"\"Get entries to evict\"\"\"\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#filecachestore-with-eviction","level":4,"title":"FileCacheStore with Eviction","text":"<pre><code>class FileCacheStore:\n    def __init__(\n        self,\n        base_path: Path,\n        max_size_mb: int | None = None,\n        eviction_policy: EvictionPolicy = EvictionPolicy.LRU,\n        high_water_mark: float = 0.9,  # Evict at 90% full\n        low_water_mark: float = 0.7,   # Evict down to 70%\n    ):\n        self.metadata = CacheMetadataStore(base_path / \".metadata\" / \"cache.db\")\n        self.evictor = CacheEvictor(self.metadata, eviction_policy)\n        # ...\n\n    def put(self, hash_key: str, value: Any, format: str | None, ttl: int | None):\n        \"\"\"Store with eviction check\"\"\"\n        # Serialize\n        data, ext = self.registry.serialize(value, format)\n\n        # Check if eviction needed\n        if self.max_size:\n            current = self._get_total_size()\n            if current + len(data) &gt; (self.max_size * self.high_water):\n                target = int(self.max_size * self.low_water)\n                self.evictor.evict_to_size(target)\n\n        # Write file\n        cache_file = self.layout.get_path(hash_key, ext)\n        cache_file.parent.mkdir(parents=True, exist_ok=True)\n        cache_file.write_bytes(data)\n\n        # Record metadata\n        ttl_expires = datetime.now() + timedelta(seconds=ttl) if ttl else None\n        self.metadata.record_put(hash_key, cache_file, len(data), ttl_expires)\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#default-cache-hash-function","level":3,"title":"Default Cache Hash Function","text":"<pre><code>def default_cache_hash(\n    func: Callable,\n    bound_args: dict[str, Any],\n    registry: SerializationRegistry,\n    exclude: list[str] | None = None,\n    param_hashers: dict[str, Callable] | None = None,\n) -&gt; str:\n    \"\"\"\n    Default cache hash: function source + parameter values.\n\n    Uses smart hashing strategies from registry to avoid\n    performance issues with large objects.\n    \"\"\"\n    h = hashlib.sha256()\n\n    # Hash function source\n    try:\n        source = inspect.getsource(func)\n        h.update(source.encode())\n    except (OSError, TypeError):\n        # Can't get source - use name\n        h.update(func.__name__.encode())\n\n    # Hash each parameter\n    exclude = exclude or []\n    param_hashers = param_hashers or {}\n\n    for name, value in sorted(bound_args.items()):\n        if name in exclude:\n            continue\n\n        if name in param_hashers:\n            param_hash = param_hashers[name](value)\n        else:\n            param_hash = registry.hash_value(value)  # Smart!\n\n        h.update(f\"{name}={param_hash}\".encode())\n\n    return h.hexdigest()\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#testing-strategy","level":2,"title":"Testing Strategy","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#unit-tests","level":3,"title":"Unit Tests","text":"<p>Serialization Registry (<code>test_serialization.py</code>): - Register custom types - Multiple formats per type - Format preferences - Hash strategies (speed + correctness) - Type inheritance - Edge cases (None, empty collections)</p> <p>Hash Functions (<code>test_hash.py</code>): - Default hash function - Parameter exclusion - Custom per-param hashers - Large object performance - Hash stability</p> <p>Cache Store (<code>test_cache.py</code>): - Put/get operations - TTL expiration - Eviction policies (LRU, LFU, SIZE) - Git-style layout - Metadata tracking - Stats/diagnostics</p> <p>Checkpoint Store (<code>test_checkpoint.py</code>): - Save/load - Versioning - Metadata storage - List versions - S3 backend</p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#integration-tests","level":3,"title":"Integration Tests","text":"<p>Caching (<code>test_task_caching.py</code>): <pre><code>@task(cache=True)\ndef expensive(x: int) -&gt; int:\n    return x * 2\n\n# First call - computes\nresult1 = expensive(5).run()\n\n# Second call - cache hit\nresult2 = expensive(5).run()\n\n# Different params - cache miss\nresult3 = expensive(10).run()\n</code></pre></p> <p>Checkpointing (<code>test_checkpointing.py</code>): <pre><code>result = (\n    compute()\n    .checkpoint(\"step1\")\n    .then(transform)\n    .checkpoint(\"step2\")\n    .run()\n)\n\n# Recover from checkpoint\nstore = FileCheckpointStore(Path(\"/checkpoints\"))\nstep1_data = store.load(\"step1\", version=\"latest\")\n</code></pre></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#performance-tests","level":3,"title":"Performance Tests","text":"<p>Hash Performance (<code>test_performance.py</code>): <pre><code>def test_large_array_hash_performance():\n    large_array = np.random.rand(10000, 10000)  # 800MB\n\n    start = time.time()\n    hash_value = default_registry.hash_value(large_array)\n    elapsed = time.time() - start\n\n    assert elapsed &lt; 0.1  # Should be &lt; 100ms\n</code></pre></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#property-based-tests","level":3,"title":"Property-Based Tests","text":"<p>Hash Stability (using Hypothesis): <pre><code>from hypothesis import given, strategies as st\n\n@given(st.integers(), st.integers())\ndef test_hash_deterministic(x, y):\n    \"\"\"Same inputs always produce same hash\"\"\"\n    hash1 = default_cache_hash(compute, {'x': x, 'y': y}, default_registry)\n    hash2 = default_cache_hash(compute, {'x': x, 'y': y}, default_registry)\n    assert hash1 == hash2\n</code></pre></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#future-enhancements","level":2,"title":"Future Enhancements","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#lsp-integration-phase-6","level":3,"title":"LSP Integration (Phase 6)","text":"<p>Diagnostic: Detect missing serializers <pre><code>@task(cache=True)\ndef process(data: CustomClass) -&gt; Result:  # ⚠️ Warning\n    ...\n\n# LSP diagnostic:\n# \"Type 'CustomClass' has no registered serializer.\n#  Caching will fail at runtime.\"\n</code></pre></p> <p>Code Action: Generate serializer registration <pre><code># Quick fix generates:\nfrom daglite.serialization import default_registry\nimport pickle\n\ndefault_registry.register(\n    CustomClass,\n    lambda obj: pickle.dumps(obj),\n    lambda data: pickle.loads(data),\n    format='pickle',\n    file_extension='pkl'\n)\n</code></pre></p> <p>Hover Info: Show serialization details <pre><code>@task(cache=True)\ndef process(data: pd.DataFrame) -&gt; Result:\n#                  ^^^^^^^^^^^^\n# Hover shows:\n# Type: pd.DataFrame\n# Formats: csv (default), parquet, pickle\n# Hash: Sample-based (500 rows)\n# Est. time: ~10ms for 1M rows\n</code></pre></p>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#additional-store-backends","level":3,"title":"Additional Store Backends","text":"<ul> <li>Redis cache store</li> <li>PostgreSQL checkpoint store</li> <li>Azure Blob checkpoint store</li> <li>Google Cloud Storage checkpoint store</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#advanced-eviction","level":3,"title":"Advanced Eviction","text":"<ul> <li>Custom eviction scores (weighted by age + size + access)</li> <li>Per-task eviction policies</li> <li>Cache warming strategies</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#metrics-observability","level":3,"title":"Metrics &amp; Observability","text":"<ul> <li>Cache hit rate tracking</li> <li>Size growth monitoring</li> <li>Eviction event logging</li> <li>Checkpoint creation alerts</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#configuration","level":2,"title":"Configuration","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#programmatic","level":3,"title":"Programmatic","text":"<pre><code>from daglite.plugins.cache import FileCacheStore, EvictionPolicy\n\ncache = FileCacheStore(\n    Path(\"/tmp/daglite-cache\"),\n    max_size_mb=1000,\n    eviction_policy=EvictionPolicy.LRU\n)\n\nwith cache:\n    result = dag.run(cache=cache)\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#cli-daglitetoml","level":3,"title":"CLI (daglite.toml)","text":"<pre><code>[cache]\ntype = \"file\"\npath = \"/tmp/daglite-cache\"\nmax_size_mb = 1000\neviction_policy = \"lru\"\nhigh_water_mark = 0.9\nlow_water_mark = 0.7\n\n[checkpoint]\ntype = \"s3\"\nbucket = \"my-ml-artifacts\"\nprefix = \"experiment-123\"\nversioning = true\n</code></pre>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#implementation-checklist","level":2,"title":"Implementation Checklist","text":"","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-1-serialization-registry","level":3,"title":"Phase 1: Serialization Registry ✅","text":"<ul> <li> Create <code>SerializationRegistry</code> class</li> <li> Add <code>SerializationHandler</code> dataclass</li> <li> Add <code>HashStrategy</code> dataclass</li> <li> Implement <code>register()</code> method</li> <li> Implement <code>register_hash_strategy()</code> method</li> <li> Implement <code>serialize()</code> / <code>deserialize()</code> methods</li> <li> Implement <code>hash_value()</code> method</li> <li> Register built-in types (int, str, dict, list, etc.)</li> <li> Add numpy hash strategy (sample-based)</li> <li> Add pandas hash strategy (schema + sample)</li> <li> Write comprehensive tests</li> <li> Document usage</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-2-caching","level":3,"title":"Phase 2: Caching ✅","text":"<ul> <li> Create <code>CacheStore</code> Protocol</li> <li> Create <code>default_cache_hash()</code> function</li> <li> Create <code>EvictionPolicy</code> enum</li> <li> Create <code>CacheMetadataStore</code> (SQLite)</li> <li> Create <code>CacheEvictor</code> class</li> <li> Create <code>GitStyleCacheLayout</code></li> <li> Create <code>FileCacheStore</code></li> <li> Add cache parameters to <code>@task</code></li> <li> Integrate cache lookup in task execution</li> <li> Write tests (unit + integration)</li> <li> Performance benchmarks</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-3-checkpointing_1","level":3,"title":"Phase 3: Checkpointing ✅","text":"<ul> <li> Create <code>CheckpointStore</code> Protocol</li> <li> Create <code>CheckpointVersion</code> dataclass</li> <li> Create <code>FileCheckpointStore</code></li> <li> Create <code>S3CheckpointStore</code></li> <li> Add <code>.checkpoint()</code> to <code>TaskFuture</code></li> <li> Implement metadata tracking</li> <li> Write tests</li> <li> Document recovery workflow</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-4-side-effects_1","level":3,"title":"Phase 4: Side Effects ✅","text":"<ul> <li> Add <code>.also()</code> to <code>TaskFuture</code></li> <li> Handle exceptions in callbacks</li> <li> Use internally for <code>.checkpoint()</code></li> <li> Write tests</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#phase-5-plugin-registration","level":3,"title":"Phase 5: Plugin Registration ✅","text":"<ul> <li> Create pandas plugin</li> <li> Create numpy plugin</li> <li> Auto-register on import</li> <li> Document plugin creation</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#questions-for-implementation","level":2,"title":"Questions for Implementation","text":"<ol> <li>Should cache metadata DB be per-cache or global?</li> <li> <p>Recommendation: Per-cache (more flexible)</p> </li> <li> <p>How to handle cache migration/versioning?</p> </li> <li> <p>Recommendation: Include version in metadata, migrate on load</p> </li> <li> <p>Should <code>.checkpoint()</code> block execution or be async?</p> </li> <li> <p>Recommendation: Block by default, async option for S3</p> </li> <li> <p>How to handle git hash detection for metadata?</p> </li> <li> <p>Recommendation: Try <code>subprocess.run(['git', 'rev-parse', 'HEAD'])</code>, catch if fails</p> </li> <li> <p>Should eviction be synchronous or background task?</p> </li> <li>Recommendation: Synchronous on put() (simple), async option later</li> </ol>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#related-decisions","level":2,"title":"Related Decisions","text":"<ul> <li>No explicit cache_key: Use content-addressable hashing (simpler, proven)</li> <li>Separate cache and checkpoint: Different concerns, separate lifecycle</li> <li>Plugin architecture: Extensibility without core bloat</li> <li>Git-style layout: Windows-safe, proven, familiar</li> <li>Sample-based hashing: Performance critical for large objects</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-and-checkpointing-design/#references","level":2,"title":"References","text":"<ul> <li>Prefect caching: https://docs.prefect.io/concepts/tasks/#caching</li> <li>Dagster assets: https://docs.dagster.io/concepts/assets/software-defined-assets</li> <li>Hamilton IO: https://hamilton.apache.org/reference/io/</li> <li>Hamilton caching: https://hamilton.apache.org/reference/caching/</li> <li>Git object storage: https://git-scm.com/book/en/v2/Git-Internals-Git-Objects</li> </ul>","path":["Design","Caching and Checkpointing Design"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/","level":1,"title":"Caching &amp; Checkpointing Overhaul — Implementation Plan","text":"<p>Status: Pending Approval Target: daglite v0.9.0</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#summary","level":2,"title":"Summary","text":"<p>Overhaul the caching system to be a built-in feature (not a plugin) and implement checkpoints on <code>.run()</code> / <code>.run_async()</code>. Replace the <code>SerializationRegistry</code> with cloudpickle. The new <code>CacheStore</code> mimics the <code>DatasetStore</code> pattern (uses <code>Driver</code> under the hood) but is its own implementation.</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#phase-1-add-cloudpickle-refactor-cachestore","level":2,"title":"Phase 1: Add cloudpickle &amp; Refactor CacheStore","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#11-add-cloudpickle-dependency","level":3,"title":"1.1 — Add <code>cloudpickle</code> dependency","text":"<p>File: <code>pyproject.toml</code></p> <ul> <li>Add <code>cloudpickle&gt;=3.0</code> to <code>dependencies</code> list.</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#12-rewrite-cachestore-using-driver","level":3,"title":"1.2 — Rewrite <code>CacheStore</code> using <code>Driver</code>","text":"<p>The new <code>CacheStore</code> mimics <code>DatasetStore</code> (uses <code>Driver</code> for byte storage) but handles cache-specific concerns: git-style sharded keys, TTL metadata, cloudpickle serialization.</p> <p>File: <code>src/daglite/cache/store.py</code> (rewrite)</p> <pre><code>class CacheStore:\n    \"\"\"Hash-based cache store using Driver for byte storage and cloudpickle for serialization.\"\"\"\n\n    def __init__(self, driver: Driver | str) -&gt; None:\n        \"\"\"Accept a Driver instance or string path (creates FileDriver).\"\"\"\n\n    def get(self, hash_key: str) -&gt; Any | None:\n        \"\"\"Retrieve cached value. Returns None on miss or TTL expiry.\"\"\"\n\n    def put(self, hash_key: str, value: Any, ttl: int | None = None) -&gt; None:\n        \"\"\"Store value with optional TTL.\"\"\"\n\n    def invalidate(self, hash_key: str) -&gt; None:\n        \"\"\"Remove a cached entry (data + metadata).\"\"\"\n\n    def clear(self) -&gt; None:\n        \"\"\"Remove all cached entries.\"\"\"\n\n    def _hash_to_key(self, hash_key: str) -&gt; str:\n        \"\"\"Git-style sharding: 'abcdef...' → 'ab/cdef...'\"\"\"\n</code></pre> <p>Key implementation details:</p> <ul> <li>Uses <code>FileDriver</code> under the hood (or any <code>Driver</code> subclass for S3/GCS).</li> <li>Cloudpickle for <code>serialize</code> / <code>deserialize</code> (replaces stdlib pickle).</li> <li>Metadata sidecar: <code>{data_key}.meta.json</code> with <code>{\"timestamp\": ..., \"ttl\": ...}</code>.</li> <li>Git-style 2-char prefix sharding for the hash key path.</li> <li>Pickle-safe <code>__getstate__</code> / <code>__setstate__</code> for process backends.</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#13-remove-cachestore-protocol","level":3,"title":"1.3 — Remove <code>CacheStore</code> Protocol","text":"<p>File: <code>src/daglite/cache/base.py</code> (delete or keep as reference)</p> <ul> <li>The <code>CacheStore</code> Protocol in <code>base.py</code> is replaced by the concrete class in <code>store.py</code>.</li> <li>If we want to keep a protocol for future extensibility, we can keep a slimmed-down   version, but the user-facing API is the concrete <code>CacheStore</code> class.</li> </ul> <p>Decision: Delete <code>base.py</code>. The concrete <code>CacheStore</code> class is the API.</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#14-update-cache__init__py","level":3,"title":"1.4 — Update <code>cache/__init__.py</code>","text":"<p>File: <code>src/daglite/cache/__init__.py</code></p> <pre><code>from daglite.cache.store import CacheStore\nfrom daglite.cache.core import default_cache_hash\n\n__all__ = [\"CacheStore\", \"default_cache_hash\"]\n</code></pre> <p>Remove the <code>FileCacheStore</code> export (now just <code>CacheStore</code>).</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#phase-2-remove-serializationregistry-update-cache-hashing","level":2,"title":"Phase 2: Remove SerializationRegistry &amp; Update Cache Hashing","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#21-rewrite-cache-hash-function-with-cloudpickle","level":3,"title":"2.1 — Rewrite cache hash function with cloudpickle","text":"<p>File: <code>src/daglite/cache/core.py</code> (rewrite)</p> <pre><code>import cloudpickle\nimport hashlib\nimport inspect\n\ndef default_cache_hash(func: Callable, bound_args: dict[str, Any]) -&gt; str:\n    \"\"\"Generate cache key from function source and cloudpickle'd parameters.\"\"\"\n    h = hashlib.sha256()\n\n    # Hash function source\n    try:\n        source = inspect.getsource(func)\n        h.update(source.encode())\n    except (OSError, TypeError):\n        h.update(func.__qualname__.encode())\n\n    # Hash each parameter via cloudpickle\n    for name, value in sorted(bound_args.items()):\n        h.update(name.encode())\n        h.update(cloudpickle.dumps(value))\n\n    return h.hexdigest()\n</code></pre> <ul> <li>Removes dependency on <code>SerializationRegistry.hash_value()</code>.</li> <li>Uses <code>cloudpickle.dumps()</code> for all types (simpler, handles lambdas/closures).</li> <li>Trade-off: potentially slower for very large objects (numpy arrays, DataFrames).   This is acceptable per user decision.</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#22-delete-serializationpy","level":3,"title":"2.2 — Delete <code>serialization.py</code>","text":"<p>File: <code>src/daglite/serialization.py</code> (delete)</p> <ul> <li>Remove the entire <code>SerializationRegistry</code> class and all built-in type registrations.</li> <li>The only consumer is <code>cache/core.py</code> (which is being rewritten above).</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#23-deprecate-extrasserialization-package","level":3,"title":"2.3 — Deprecate <code>extras/serialization</code> package","text":"<p>Files: <code>extras/serialization/</code> directory</p> <ul> <li>The numpy/pandas/pillow hash strategies are no longer needed since we use cloudpickle.</li> <li>Mark the package as deprecated or remove it entirely.</li> <li>Update <code>pyproject.toml</code> to remove the <code>serialization</code> optional dependency.</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#24-remove-serialization-tests","level":3,"title":"2.4 — Remove serialization tests","text":"<p>Files: - <code>tests/test_serialization.py</code> (delete) - <code>extras/serialization/tests/test_serialization_plugin.py</code> (delete/deprecate)</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#phase-3-make-caching-a-built-in-feature","level":2,"title":"Phase 3: Make Caching a Built-in Feature","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#31-add-cache_store-to-daglitesettings","level":3,"title":"3.1 — Add <code>cache_store</code> to <code>DagliteSettings</code>","text":"<p>File: <code>src/daglite/settings.py</code></p> <pre><code>@dataclass(frozen=True)\nclass DagliteSettings:\n    # ... existing fields ...\n\n    cache_store: CacheStore | str | None = field(\n        default_factory=lambda: os.getenv(\"DAGLITE_CACHE_STORE\")\n    )\n    \"\"\"\n    Default cache store for @task(cache=True).\n\n    Can be a CacheStore instance or a string path (creates CacheStore with FileDriver).\n    If None, caching is disabled even when cache=True on tasks.\n    \"\"\"\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#32-move-caching-logic-into-worker-remove-plugin-indirection","level":3,"title":"3.2 — Move caching logic into worker (remove plugin indirection)","text":"<p>File: <code>src/daglite/graph/nodes/_workers.py</code></p> <p>In <code>_run_task_func</code>, replace the plugin hook calls with direct cache operations:</p> <pre><code># BEFORE (plugin-based):\ncached_result = hook.check_cache(func=func, metadata=metadata, inputs=inputs, ...)\n# ...\nhook.update_cache(func=func, metadata=metadata, inputs=inputs, result=result, ...)\n\n# AFTER (built-in):\nif cache_enabled:\n    cache_store = _get_cache_store()  # From execution context\n    if cache_store is not None:\n        cache_key = default_cache_hash(func, inputs)\n        cached = cache_store.get(cache_key)\n        if cached is not None:\n            result = cached[\"value\"]\n            hook.on_cache_hit(...)  # Keep observability hook\n            return result\n\n# ... execute task ...\n\nif cache_enabled and cache_store is not None:\n    cache_store.put(cache_key, {\"value\": result}, ttl=cache_ttl)\n</code></pre> <p>Key changes: - Caching logic is direct, not mediated through pluggy hooks. - The <code>on_cache_hit</code> hook is preserved for observability (logging plugin can still react). - Cache store is obtained from execution context (settings or per-run override).</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#33-thread-cache_store-through-execution-context","level":3,"title":"3.3 — Thread <code>cache_store</code> through execution context","text":"<p>File: <code>src/daglite/backends/context.py</code></p> <p>Add cache store to the execution context (similar to how <code>plugin_manager</code> and <code>dataset_reporter</code> are threaded):</p> <pre><code>_cache_store_var: ContextVar[CacheStore | None] = ContextVar(\"cache_store\", default=None)\n\ndef set_cache_store(store: CacheStore | None) -&gt; Token:\n    return _cache_store_var.set(store)\n\ndef get_cache_store() -&gt; CacheStore | None:\n    return _cache_store_var.get()\n</code></pre> <p>File: <code>src/daglite/engine.py</code></p> <p>In <code>evaluate_async()</code>: <pre><code># Resolve cache store: explicit param &gt; settings &gt; None\ncache_store = cache_store_param or settings.cache_store\nif isinstance(cache_store, str):\n    cache_store = CacheStore(cache_store)\n\n# Set in context for workers\nset_cache_store(cache_store)\n</code></pre></p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#34-add-cache_store-parameter-to-run-run_async","level":3,"title":"3.4 — Add <code>cache_store</code> parameter to <code>.run()</code> / <code>.run_async()</code>","text":"<p>File: <code>src/daglite/futures/base.py</code></p> <pre><code>def run(self, *, plugins=None, cache_store=None) -&gt; Any:\n    from daglite.engine import evaluate\n    return evaluate(self, plugins=plugins, cache_store=cache_store)\n\nasync def run_async(self, *, plugins=None, cache_store=None) -&gt; Any:\n    from daglite.engine import evaluate_async\n    return await evaluate_async(self, plugins=plugins, cache_store=cache_store)\n</code></pre> <p>File: <code>src/daglite/engine.py</code></p> <p>Add <code>cache_store</code> parameter to <code>evaluate()</code>, <code>evaluate_async()</code>, etc.</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#35-remove-cacheplugin-and-cache-hook-specs","level":3,"title":"3.5 — Remove <code>CachePlugin</code> and cache hook specs","text":"<p>Files: - <code>src/daglite/plugins/builtin/cache.py</code> (delete) - <code>src/daglite/plugins/hooks/specs.py</code> — remove <code>check_cache</code> and <code>update_cache</code> specs   (keep <code>on_cache_hit</code> for observability)</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#36-update-plugin-manager","level":3,"title":"3.6 — Update plugin manager","text":"<p>File: <code>src/daglite/plugins/manager.py</code></p> <ul> <li>Remove auto-registration of <code>CachePlugin</code>.</li> <li>Update any references to cache plugin.</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#phase-4-implement-checkpoint-fluent-method","level":2,"title":"Phase 4: Implement <code>.checkpoint()</code> Fluent Method","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#41-add-checkpoint-to-basetaskfuture","level":3,"title":"4.1 — Add <code>.checkpoint()</code> to <code>BaseTaskFuture</code>","text":"<p>File: <code>src/daglite/futures/base.py</code></p> <pre><code>def checkpoint(\n    self,\n    name: str,\n    *,\n    key: str | None = None,\n    store: DatasetStore | str | None = None,\n    format: str | None = None,\n    **extras: Any,\n) -&gt; Self:\n    \"\"\"\n    Mark this future's result for checkpointing (named, persistent save).\n\n    The checkpoint is saved after task execution completes. It can be used for:\n    - Pipeline resumption via .run(from_={name: key})\n    - Debugging and inspection\n    - Audit trails\n\n    Args:\n        name: Checkpoint name (used for resumption via from_=).\n        key: Storage key template. If None, uses name as the key.\n            Supports {param} format strings resolved from task params.\n        store: Override dataset store for this checkpoint.\n        format: Serialization format hint (e.g., \"pickle\").\n        **extras: Additional values for key formatting. Can include\n            TaskFutures whose results are resolved at execution time.\n\n    Returns:\n        Self for method chaining.\n\n    Examples:\n        Simple checkpoint:\n        &gt;&gt;&gt; result = expensive_task(data).checkpoint(\"step1\").run()\n\n        With key template and extras:\n        &gt;&gt;&gt; version = get_version()\n        &gt;&gt;&gt; result = train_model(data).checkpoint(\n        ...     \"trained_model\",\n        ...     key=\"models/{model_type}/{version}\",\n        ...     version=version,  # TaskFuture, resolved at execution time\n        ... ).run()\n\n        Chained checkpoints:\n        &gt;&gt;&gt; result = (\n        ...     step1(data)\n        ...     .checkpoint(\"raw_features\")\n        ...     .then(step2)\n        ...     .checkpoint(\"normalized\")\n        ...     .then(step3)\n        ...     .run()\n        ... )\n    \"\"\"\n    return self.save(\n        key or name,\n        save_checkpoint=name,\n        save_store=store,\n        save_format=format,\n        **extras,\n    )\n</code></pre> <p>This is a clean, focused API that delegates to the existing <code>.save()</code> infrastructure with <code>save_checkpoint=name</code>. The user gets a nice fluent API while reusing all the existing save/output machinery.</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#phase-5-implement-checkpoint-and-from_-on-run-run_async","level":2,"title":"Phase 5: Implement <code>checkpoint</code> and <code>from_</code> on <code>.run()</code> / <code>.run_async()</code>","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#51-add-checkpoint-and-from_-parameters-to-run","level":3,"title":"5.1 — Add <code>checkpoint</code> and <code>from_</code> parameters to <code>.run()</code>","text":"<p>File: <code>src/daglite/futures/base.py</code></p> <pre><code>def run(\n    self,\n    *,\n    plugins: list[Any] | None = None,\n    cache_store: CacheStore | str | None = None,\n    checkpoint: dict[str, str] | None = None,\n    from_: dict[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"\n    Args:\n        checkpoint: Dict of {checkpoint_name: key_template}. Saves the final\n            result to each specified checkpoint after execution.\n        from_: Dict of {checkpoint_name: key}. Attempts to resume from\n            checkpoint instead of re-executing. Falls through to normal\n            execution if checkpoint not found.\n    \"\"\"\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#52-implement-checkpoint-save-in-engine","level":3,"title":"5.2 — Implement <code>checkpoint</code> save in engine","text":"<p>File: <code>src/daglite/engine.py</code></p> <p>After graph execution completes, if <code>checkpoint</code> dict is provided, save the result:</p> <pre><code>async def evaluate_async(future, *, plugins=None, cache_store=None, checkpoint=None, from_=None):\n    # ... existing setup ...\n\n    # Check from_ first: try to load from checkpoint\n    if from_:\n        loaded = _try_load_from_checkpoint(from_)\n        if loaded is not None:\n            return loaded\n\n    # ... execute graph ...\n\n    # Save checkpoints after execution\n    if checkpoint:\n        _save_checkpoints(result, checkpoint)\n\n    return result\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#53-implement-from_-resumption-in-engine","level":3,"title":"5.3 — Implement <code>from_</code> resumption in engine","text":"<p>File: <code>src/daglite/engine.py</code></p> <pre><code>def _try_load_from_checkpoint(\n    from_: dict[str, str],\n) -&gt; Any | None:\n    \"\"\"\n    Attempt to load the result from a previously saved checkpoint.\n\n    Args:\n        from_: Dict of {checkpoint_name: key} to try loading from.\n\n    Returns:\n        The loaded result, or None if no checkpoint found.\n    \"\"\"\n    from daglite.settings import get_global_settings\n\n    settings = get_global_settings()\n    store = _resolve_dataset_store(settings.datastore_store)\n\n    for name, key in from_.items():\n        if store.exists(key):\n            return store.load(key)\n\n    return None\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#54-implement-checkpoint-save-helper","level":3,"title":"5.4 — Implement checkpoint save helper","text":"<pre><code>def _save_checkpoints(result: Any, checkpoint: dict[str, str]) -&gt; None:\n    \"\"\"Save result to each checkpoint location.\"\"\"\n    from daglite.settings import get_global_settings\n\n    settings = get_global_settings()\n    store = _resolve_dataset_store(settings.datastore_store)\n\n    for name, key in checkpoint.items():\n        store.save(key, result)\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#phase-6-cleanup-tests","level":2,"title":"Phase 6: Cleanup &amp; Tests","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#61-update-existing-cache-tests","level":3,"title":"6.1 — Update existing cache tests","text":"<p>File: <code>tests/integration/test_cache.py</code></p> <ul> <li>Remove dependency on <code>CachePlugin</code></li> <li>Use <code>CacheStore</code> directly via settings or <code>.run(cache_store=...)</code></li> <li>Test the new built-in caching flow</li> </ul> <pre><code>def test_cache_hit():\n    with tempfile.TemporaryDirectory() as tmpdir:\n        store = CacheStore(tmpdir)\n\n        @task(cache=True)\n        def expensive(x: int) -&gt; int:\n            return x * 2\n\n        result1 = expensive(x=5).run(cache_store=store)\n        result2 = expensive(x=5).run(cache_store=store)  # cache hit\n        assert result1 == result2 == 10\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#62-add-checkpoint-tests","level":3,"title":"6.2 — Add checkpoint tests","text":"<p>File: <code>tests/integration/test_checkpoint.py</code> (new)</p> <pre><code>def test_checkpoint_fluent():\n    \"\"\"Test .checkpoint() fluent method saves result.\"\"\"\n    with tempfile.TemporaryDirectory() as tmpdir:\n        store = DatasetStore(tmpdir)\n        result = my_task(x=5).checkpoint(\"step1\", store=store).run()\n        assert store.exists(\"step1\")\n\ndef test_checkpoint_with_extras():\n    \"\"\"Test .checkpoint() with TaskFuture extras for key formatting.\"\"\"\n    version = get_version()\n    result = train(data).checkpoint(\n        \"model\", key=\"models/{version}\", version=version\n    ).run()\n\ndef test_run_checkpoint_dict():\n    \"\"\"Test .run(checkpoint={...}) saves final result.\"\"\"\n    result = my_task(x=5).run(checkpoint={\"output\": \"results/output\"})\n\ndef test_run_from_checkpoint():\n    \"\"\"Test .run(from_={...}) resumes from checkpoint.\"\"\"\n    # First run saves\n    result1 = my_task(x=5).run(checkpoint={\"output\": \"results/output\"})\n    # Second run resumes\n    result2 = my_task(x=5).run(from_={\"output\": \"results/output\"})\n    assert result1 == result2\n\ndef test_from_fallthrough():\n    \"\"\"Test from_ falls through to execution if checkpoint missing.\"\"\"\n    result = my_task(x=5).run(from_={\"missing\": \"nonexistent\"})\n    assert result == expected_value\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#63-update-unit-tests-for-cache-plugin-removal","level":3,"title":"6.3 — Update unit tests for cache plugin removal","text":"<p>File: <code>tests/plugins/builtin/test_cache.py</code></p> <ul> <li>Remove or rewrite tests that reference <code>CachePlugin</code>.</li> <li>Add tests for the built-in cache store behavior.</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#64-remove-serialization-tests","level":3,"title":"6.4 — Remove serialization tests","text":"<p>Files: - <code>tests/test_serialization.py</code> — delete (SerializationRegistry removed) - <code>extras/serialization/tests/test_serialization_plugin.py</code> — delete (package deprecated)</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#65-update-imports-and-references","level":3,"title":"6.5 — Update imports and references","text":"<p>Grep the codebase for all references to: - <code>SerializationRegistry</code>, <code>default_registry</code> (from serialization) - <code>FileCacheStore</code> (renamed to <code>CacheStore</code>) - <code>CachePlugin</code> - <code>check_cache</code>, <code>update_cache</code> (removed hook specs)</p>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#files-changed-summary","level":2,"title":"Files Changed Summary","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#new-files","level":3,"title":"New Files","text":"<ul> <li>(none — we rewrite existing files)</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#modified-files","level":3,"title":"Modified Files","text":"<ul> <li><code>pyproject.toml</code> — add cloudpickle dependency, remove serialization optional dep</li> <li><code>src/daglite/cache/__init__.py</code> — update exports</li> <li><code>src/daglite/cache/store.py</code> — rewrite with Driver + cloudpickle</li> <li><code>src/daglite/cache/core.py</code> — rewrite hash to use cloudpickle</li> <li><code>src/daglite/settings.py</code> — add <code>cache_store</code> field</li> <li><code>src/daglite/futures/base.py</code> — add <code>.checkpoint()</code>, update <code>.run()</code> / <code>.run_async()</code></li> <li><code>src/daglite/engine.py</code> — add <code>cache_store</code>/<code>checkpoint</code>/<code>from_</code> params</li> <li><code>src/daglite/graph/nodes/_workers.py</code> — inline cache logic, remove hook calls</li> <li><code>src/daglite/backends/context.py</code> — add cache store context var</li> <li><code>src/daglite/plugins/hooks/specs.py</code> — remove <code>check_cache</code>/<code>update_cache</code></li> <li><code>src/daglite/workflows.py</code> — thread new params through workflow run</li> <li><code>tests/integration/test_cache.py</code> — rewrite for built-in caching</li> <li><code>tests/integration/test_checkpoint.py</code> — new checkpoint tests</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#deleted-files","level":3,"title":"Deleted Files","text":"<ul> <li><code>src/daglite/cache/base.py</code> — CacheStore protocol (replaced by concrete class)</li> <li><code>src/daglite/serialization.py</code> — entire SerializationRegistry</li> <li><code>src/daglite/plugins/builtin/cache.py</code> — CachePlugin (logic moved to workers)</li> <li><code>tests/test_serialization.py</code> — serialization tests</li> <li><code>extras/serialization/</code> — deprecated (optionally kept for backwards compat)</li> </ul>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#dependency-changes","level":2,"title":"Dependency Changes","text":"Package Action Reason <code>cloudpickle&gt;=3.0</code> Add Replaces SerializationRegistry for serialization + hashing <code>fsspec</code> Keep Still used by FileDriver (underlying CacheStore) <code>pluggy</code> Keep Still used for observability hooks (on_cache_hit, etc.)","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#migration-notes","level":2,"title":"Migration Notes","text":"","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#for-users-of-cacheplugin","level":3,"title":"For users of <code>CachePlugin</code>:","text":"<pre><code># BEFORE (v0.8):\nfrom daglite.cache.store import FileCacheStore\nfrom daglite.plugins.builtin.cache import CachePlugin\n\nstore = FileCacheStore(\"/tmp/cache\")\nplugin = CachePlugin(store=store)\nresult = my_task(x=5).run(plugins=[plugin])\n\n# AFTER (v0.9):\nfrom daglite.cache import CacheStore\n\nstore = CacheStore(\"/tmp/cache\")\nresult = my_task(x=5).run(cache_store=store)\n\n# Or via settings (no per-run config needed):\nfrom daglite.settings import set_global_settings, DagliteSettings\nset_global_settings(DagliteSettings(cache_store=\"/tmp/cache\"))\nresult = my_task(x=5).run()  # Uses global cache store\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#for-users-of-serializationregistry","level":3,"title":"For users of <code>SerializationRegistry</code>:","text":"<pre><code># BEFORE (v0.8):\nfrom daglite.serialization import default_registry\ndefault_registry.register(MyType, serializer, deserializer, ...)\ndefault_registry.register_hash_strategy(MyType, hasher, ...)\n\n# AFTER (v0.9):\n# No registration needed — cloudpickle handles all serializable types.\n# If a type isn't cloudpickle-serializable, implement __reduce__ or __getstate__.\n</code></pre>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/caching-checkpoints-overhaul-plan/#implementation-order","level":2,"title":"Implementation Order","text":"<ol> <li>Phase 1 (CacheStore refactor) — Foundation, no breaking changes yet</li> <li>Phase 2 (Remove SerializationRegistry) — Clean break from old infrastructure</li> <li>Phase 3 (Built-in caching) — Core feature, removes plugin indirection</li> <li>Phase 4 (.checkpoint() fluent method) — User-facing API addition</li> <li>Phase 5 (checkpoint/from_ on .run()) — Complete checkpoint system</li> <li>Phase 6 (Tests &amp; cleanup) — Verify everything works end-to-end</li> </ol>","path":["Design","Caching &amp; Checkpointing Overhaul — Implementation Plan"],"tags":[]},{"location":"design/claude-code-quickstart/","level":1,"title":"Claude code quickstart","text":"","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#tldr","level":2,"title":"TL;DR","text":"<p>We're adding production-ready caching and checkpointing to daglite with a type-safe, plugin-based architecture.</p> <p>Key innovations: - Content-addressable caching (no explicit cache keys!) - Shared serialization registry (register types once, use everywhere) - Smart hash strategies (sample-based for large objects = 100-2000x faster) - Git-style file layout (Windows-safe) - Separate concerns: caching (performance) vs checkpointing (audit)</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#phase-1-serialization-registry-complete","level":2,"title":"Phase 1: Serialization Registry ✅ COMPLETE","text":"<p>This is the foundation everything builds on.</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#core-package","level":3,"title":"Core Package","text":"<p><code>src/daglite/serialization.py</code> - Single module with: - <code>SerializationRegistry</code> class for type-based serialization - <code>register()</code> - register serializer/deserializer for a type - <code>register_hash_strategy()</code> - register hash function for a type - <code>serialize()</code> / <code>deserialize()</code> - use registered handlers - <code>hash_value()</code> - strict TypeError for unregistered types (no fallback) - Module-based error messages suggest correct plugin to install - Built-in hash strategies using closures for recursion (dict, list, set, etc.) - NO numpy/pandas/PIL (moved to plugin)</p> <p><code>tests/test_serialization.py</code> - 39 tests covering: - Type registration and multiple formats - Hash strategies for built-in types - Strict error handling - Edge cases - Recursive hashing for nested structures</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#plugin-package","level":3,"title":"Plugin Package","text":"<p><code>extras/serialization/daglite-serialization/</code> - Separate package: - Package name: <code>daglite_serialization</code> (flat namespace) - <code>numpy.py</code>: Sample-based hashing with start/middle/end sampling (&lt;100ms for 800MB) - <code>pandas.py</code>: Schema + sample rows hashing (~10ms for 1M rows) - <code>pillow.py</code>: Thumbnail-based image hashing - <code>register_all()</code>: Convenience function to register all available handlers - 16 tests covering 1D/2D/3D arrays, DataFrames, Series, and images</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#usage","level":3,"title":"Usage","text":"<pre><code># Install plugin\nuv pip install daglite_serialization[numpy]\n\n# Register handlers\nfrom daglite_serialization import register_all\nregister_all()\n\n# Now works with caching\nfrom daglite import task\nimport numpy as np\n\n@task(cache=True)\ndef process(arr: np.ndarray) -&gt; np.ndarray:\n    return arr * 2\n</code></pre>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#implementation-order","level":2,"title":"Implementation Order","text":"<p>Phase 1 ✅ COMPLETE: 1. ✅ <code>serialization.py</code> - Single module with registry and built-in hash strategies 2. ✅ <code>daglite_serialization</code> plugin - numpy/pandas/pillow handlers 3. ✅ Tests - 39 core + 16 plugin tests, 100% coverage</p> <p>Phase 2 (next - caching): 1. <code>caching/store.py</code> - CacheStore Protocol 2. <code>caching/hash.py</code> - default_cache_hash() 3. <code>caching/eviction.py</code> - Eviction policies 4. File-based cache store implementation 5. Update <code>@task</code> decorator with cache parameters</p> <p>Phase 3 (checkpointing): 1. <code>checkpointing/store.py</code> - CheckpointStore Protocol 2. File-based checkpoint store 3. S3 checkpoint store 4. Add <code>.checkpoint()</code> to TaskFuture</p> <p>Phase 4 (side effects): 1. Add <code>.also()</code> to TaskFuture 2. Use internally for <code>.checkpoint()</code></p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#key-design-decisions","level":2,"title":"Key Design Decisions","text":"<ol> <li>No cache_key parameter: Use content-addressable hashing (hash = function source + params)</li> <li>Sample-based hashing: Hash metadata + sample of large objects (massive speedup)</li> <li>Git-style layout: <code>ab/cd/ef123...</code> (Windows-safe, proven)</li> <li>Separate cache/checkpoint: Different purposes, different lifecycle</li> <li>Plugin architecture: Core stays lean, features in extras/</li> </ol>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#what-makes-this-special","level":2,"title":"What Makes This Special","text":"<p>Compared to Prefect/Dagster/Hamilton: - ✅ More modular: Plugin-based, not monolithic - ✅ Type-friendly: Auto-serialization via type registry - ✅ Faster hashing: Sample-based strategies (100-2000x speedup) - ✅ Simpler API: No explicit cache keys, smart defaults - ✅ Extensible: Easy to add custom types/stores</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#testing-priorities","level":2,"title":"Testing Priorities","text":"<ol> <li>Hash performance - CRITICAL! Must be fast for large objects</li> <li>Hash stability - Same inputs always produce same hash</li> <li>Eviction correctness - LRU actually evicts least recently used</li> <li>Serialization round-trip - Serialize → deserialize → same value</li> <li>Windows compatibility - Path lengths, line endings</li> </ol>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#common-pitfalls-to-avoid","level":2,"title":"Common Pitfalls to Avoid","text":"<ol> <li>Don't hash entire large objects - Use sample-based strategies!</li> <li>Don't forget Windows - Test path lengths, git-style layout</li> <li>Don't couple cache and checkpoint - Separate concerns</li> <li>Don't pickle function closures - Can't reliably hash, use inspect.getsource</li> <li>Don't block on S3 ops - Make async where appropriate</li> </ol>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#questions-check-the-design-doc","level":2,"title":"Questions? Check the Design Doc","text":"<p>The full design doc (<code>caching-and-checkpointing-design.md</code>) has: - Complete API reference - Implementation details for every component - Performance benchmarks - Testing strategy - Future enhancements (LSP integration!)</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#current-daglite-code-structure","level":2,"title":"Current daglite Code Structure","text":"<p>The existing codebase is well-organized. You'll be adding to: <pre><code>daglite-core/src/daglite/\n  task.py          # Update @task decorator here\n  dag.py           # TaskFuture lives here\n  serialization/   # NEW - create this\n  caching/         # NEW - create this\n  checkpointing/   # NEW - create this (Phase 3)\n</code></pre></p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#integration-points","level":2,"title":"Integration Points","text":"<p>@task decorator (in <code>task.py</code>): - Add: <code>cache</code>, <code>cache_ttl</code>, <code>cache_hash</code>, <code>cache_exclude</code>, <code>cache_hash_params</code> - Integration point in task execution</p> <p>TaskFuture (in <code>dag.py</code>): - Add: <code>.also(callback)</code> method - Add: <code>.checkpoint(name)</code> method</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#phase-1-success-criteria-all-met","level":2,"title":"Phase 1 Success Criteria ✅ ALL MET","text":"<p>✅ Can register custom types with multiple formats ✅ Can serialize/deserialize built-in and custom types ✅ Strict error handling (TypeError for unregistered types) ✅ Module-based error messages guide users to correct plugin ✅ Plugin package (<code>daglite_serialization</code>) with numpy/pandas/pillow ✅ Smart hash strategies with start/middle/end sampling ✅ 100% test coverage (39 core + 16 plugin tests) ✅ Performance: hash 800MB numpy array in &lt;100ms ✅ Recursive hashing via closures for nested data structures</p> <p>Phase 1 is complete! Ready for Phase 2 (Caching Infrastructure).</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/claude-code-quickstart/#next-phase-2-caching","level":2,"title":"Next: Phase 2 - Caching 🚀","text":"<p>Now that serialization is solid, the next step is implementing the caching infrastructure: 1. <code>CacheStore</code> Protocol 2. <code>default_cache_hash()</code> function 3. File-based cache with eviction policies 4. Integration with <code>@task</code> decorator</p> <p>See the full design doc for details on Phase 2.</p>","path":["Design","Claude code quickstart"],"tags":[]},{"location":"design/event-loop-alternatives/","level":1,"title":"Event Loop Performance Analysis","text":"","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#current-implementation","level":2,"title":"Current Implementation","text":"<p>The current <code>EventProcessor</code> polls event sources every 1ms when idle:</p> <pre><code># src/daglite/plugins/events.py\ndef _process_loop(self) -&gt; None:\n    while self._running:\n        has_events = False\n        for source in self._sources.values():\n            event = self._get_event(source)\n            if event:\n                self._registry.dispatch(event)\n                has_events = True\n        if not has_events:\n            time.sleep(0.001)  # 1ms sleep when idle\n</code></pre>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#performance-impact","level":3,"title":"Performance Impact","text":"<p>CPU Usage: ~0.1% for idle event loop (1ms sleep = 1000 iterations/second) - Low-frequency events (e.g., 1 event/second): 999 wasted iterations - High-frequency events (e.g., 100 events/second): 900 wasted iterations - Burst events: Good responsiveness (1ms latency)</p> <p>When is this a problem? - ❌ Long-running graphs (hours) with infrequent events → wastes CPU over time - ❌ Battery-powered devices → drains battery - ✅ Short-running graphs (seconds-minutes) → negligible overhead - ✅ High event frequency → minimal waste</p> <p>Recommendation: For most daglite use cases (data pipelines, batch jobs), the current implementation is perfectly fine. The overhead is small and the simplicity is valuable.</p>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#alternative-1-adaptive-backoff-simple","level":2,"title":"Alternative 1: Adaptive Backoff (Simple)","text":"<p>Idea: Start with 1ms, increase to 10ms → 100ms when idle</p> <pre><code>def _process_loop(self) -&gt; None:\n    sleep_time = 0.001  # Start at 1ms\n    max_sleep = 0.1     # Cap at 100ms\n\n    while self._running:\n        has_events = False\n        for source in self._sources.values():\n            event = self._get_event(source)\n            if event:\n                self._registry.dispatch(event)\n                has_events = True\n\n        if has_events:\n            sleep_time = 0.001  # Reset to 1ms when events occur\n        else:\n            sleep_time = min(sleep_time * 2, max_sleep)  # Exponential backoff\n\n        time.sleep(sleep_time)\n</code></pre> <p>Pros: - ✅ Reduces CPU usage during idle periods (0.1% → ~0.01%) - ✅ Still responsive for bursts (1ms initial latency) - ✅ Simple to implement (5 lines)</p> <p>Cons: - ⚠️ Latency increases during low-frequency events (up to 100ms) - ⚠️ Slightly more complex than current implementation</p> <p>When to use: If users report CPU usage concerns on long-running jobs</p>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#alternative-2-async-event-loop-your-idea","level":2,"title":"Alternative 2: Async Event Loop (Your Idea)","text":"<p>Idea: Use <code>asyncio.Queue</code> instead of polling</p> <pre><code>import asyncio\nfrom asyncio import Queue as AsyncQueue\n\nclass AsyncEventProcessor:\n    \"\"\"Async event processor using asyncio.Queue.\"\"\"\n\n    def __init__(self, registry: EventRegistry):\n        self._registry = registry\n        self._queue: AsyncQueue[dict[str, Any]] = AsyncQueue()\n        self._task: asyncio.Task | None = None\n\n    async def start(self) -&gt; None:\n        \"\"\"Start async event processing.\"\"\"\n        self._task = asyncio.create_task(self._process_loop())\n\n    async def stop(self) -&gt; None:\n        \"\"\"Stop async event processing.\"\"\"\n        if self._task:\n            self._task.cancel()\n            try:\n                await self._task\n            except asyncio.CancelledError:\n                pass\n\n    async def _process_loop(self) -&gt; None:\n        \"\"\"Process events from queue - no polling!\"\"\"\n        while True:\n            event = await self._queue.get()  # Blocks until event available\n            self._registry.dispatch(event)\n            self._queue.task_done()\n\n    async def dispatch(self, event: dict[str, Any]) -&gt; None:\n        \"\"\"Dispatch event immediately (non-blocking).\"\"\"\n        await self._queue.put(event)\n</code></pre> <p>Integration with ThreadReporter: <pre><code>class AsyncThreadReporter:\n    def __init__(self, queue: AsyncQueue[Any]):\n        self._queue = queue\n\n    def report(self, event_type: str, data: dict[str, Any]) -&gt; None:\n        \"\"\"Send event from worker thread to async queue.\"\"\"\n        event = {\"type\": event_type, **data}\n        # Need to use asyncio.run_coroutine_threadsafe()\n        loop = asyncio.get_event_loop()\n        asyncio.run_coroutine_threadsafe(self._queue.put(event), loop)\n</code></pre></p> <p>Pros: - ✅ Zero CPU usage when idle (no polling!) - ✅ Perfect for async-first architectures - ✅ Scales to thousands of concurrent events</p> <p>Cons: - ❌ Much more complex - requires async everywhere - ❌ Breaking change - Engine must be async - ❌ Thread safety issues - workers need <code>run_coroutine_threadsafe()</code> - ❌ Overkill for current daglite architecture</p> <p>When to use: If daglite moves to async-first architecture (big refactor)</p>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#alternative-3-threadingevent-middle-ground","level":2,"title":"Alternative 3: Threading.Event (Middle Ground)","text":"<p>Idea: Use <code>threading.Event</code> to wake up thread when events arrive</p> <pre><code>import threading\n\nclass EventDrivenProcessor:\n    \"\"\"Event processor with explicit wake-up mechanism.\"\"\"\n\n    def __init__(self, registry: EventRegistry):\n        self._registry = registry\n        self._sources: dict[UUID, Any] = {}\n        self._running = False\n        self._thread: Thread | None = None\n        self._wake_event = threading.Event()\n\n    def notify_event(self) -&gt; None:\n        \"\"\"Wake up processor thread (called by reporters).\"\"\"\n        self._wake_event.set()\n\n    def _process_loop(self) -&gt; None:\n        while self._running:\n            # Wait for wake-up signal with timeout\n            self._wake_event.wait(timeout=0.1)  # 100ms max sleep\n            self._wake_event.clear()\n\n            # Process all available events\n            for source in self._sources.values():\n                while True:  # Drain queue\n                    event = self._get_event_nonblocking(source)\n                    if event is None:\n                        break\n                    self._registry.dispatch(event)\n</code></pre> <p>Updated Reporter: <pre><code>class ThreadReporterWithWakeup:\n    def __init__(self, queue: Queue[Any], wake_callback: Callable[[], None]):\n        self._queue = queue\n        self._wake_callback = wake_callback\n\n    def report(self, event_type: str, data: dict[str, Any]) -&gt; None:\n        event = {\"type\": event_type, **data}\n        self._queue.put(event)\n        self._wake_callback()  # Wake up processor\n</code></pre></p> <p>Pros: - ✅ Near-zero CPU when idle (only wakes on events) - ✅ Simple to understand (just adds wake-up mechanism) - ✅ Compatible with existing threading architecture</p> <p>Cons: - ⚠️ Requires reporters to notify processor (tight coupling) - ⚠️ Still uses timeout for robustness (100ms max latency)</p> <p>When to use: Good middle ground if CPU usage becomes a concern</p>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#recommendation","level":2,"title":"Recommendation","text":"<p>For now: Keep current implementation (1ms polling) - Simple, predictable, works well for typical use cases - CPU overhead is negligible for batch/pipeline workloads - Easy to debug and maintain</p> <p>If CPU becomes an issue: Implement Alternative 1 (Adaptive Backoff) - Minimal code change (5 lines) - Preserves simplicity - Reduces CPU during idle periods</p> <p>For future async refactor: Consider Alternative 2 (Async Event Loop) - Only if daglite moves to async-first architecture - Requires Engine to be fully async - Best performance but high complexity cost</p>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/event-loop-alternatives/#measurement","level":2,"title":"Measurement","text":"<p>To decide if optimization is needed, add telemetry:</p> <pre><code>import time\n\nclass EventProcessor:\n    def _process_loop(self) -&gt; None:\n        iterations = 0\n        events_processed = 0\n        start_time = time.time()\n\n        while self._running:\n            iterations += 1\n            has_events = False\n\n            for source in self._sources.values():\n                event = self._get_event(source)\n                if event:\n                    self._registry.dispatch(event)\n                    has_events = True\n                    events_processed += 1\n\n            if not has_events:\n                time.sleep(0.001)\n\n        duration = time.time() - start_time\n        efficiency = events_processed / iterations if iterations &gt; 0 else 0\n        logger.info(\n            f\"EventProcessor stats: {iterations} iterations, \"\n            f\"{events_processed} events ({efficiency:.1%} efficiency), \"\n            f\"{duration:.1f}s runtime\"\n        )\n</code></pre> <p>Interpretation: - <code>efficiency &lt; 1%</code>: Consider adaptive backoff - <code>efficiency &gt; 10%</code>: Current implementation is fine - <code>runtime &gt; 1 hour</code> + <code>efficiency &lt; 1%</code>: Definitely optimize</p>","path":["Design","Event Loop Performance Analysis"],"tags":[]},{"location":"design/test-suite-organization-review/","level":1,"title":"Test Suite Organization Review","text":"<p>This document reviews the current pytest layout and proposes a clearer structure for collocation, naming, and class organization.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#why-this-review","level":2,"title":"Why this review","text":"<p>The suite already has strong coverage, but some tests are currently grouped by how they are invoked (for example, <code>.run()</code> / <code>.run_async()</code>) instead of by the API capability they validate. That creates overlap between top-level <code>tests/</code> and <code>tests/integration/</code>, and makes it harder to find the canonical place for behavior tests.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#current-structure-observations","level":2,"title":"Current structure observations","text":"","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#1-testsintegration-is-doing-two-different-jobs","level":3,"title":"1) <code>tests/integration/</code> is doing two different jobs","text":"<p><code>tests/integration/</code> currently contains both:</p> <ul> <li>true cross-component tests (plugins + backends + datasets + retries + logging), and</li> <li>many API behavior tests for task-future execution and fluent chaining.</li> </ul> <p>This makes the folder conceptually broad: \"integration\" often means environment or system boundaries, while many of these tests are behavior-level for a single API surface.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#2-test-location-guidance-is-currently-inconsistent","level":3,"title":"2) Test location guidance is currently inconsistent","text":"<p><code>tests/test_tasks_futures.py</code> contains this guidance:</p> <p>\"Tests in this file should NOT focus on evaluation. Evaluation tests are in tests/evaluation/.\"</p> <p>But there is no <code>tests/evaluation/</code> package today; evaluation behavior now lives in <code>tests/behavior/test_run_sync.py</code> and <code>tests/behavior/test_run_async.py</code>.</p> <p>This mismatch is a concrete signal that the test taxonomy evolved but naming did not.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#3-similar-domains-are-split-across-multiple-directories","level":3,"title":"3) Similar domains are split across multiple directories","text":"<p>Examples:</p> <ul> <li>Fluent API execution tests are in <code>tests/behavior/test_fluent_api.py</code>, while related node/future behavior is in <code>tests/test_iter_future.py</code>, <code>tests/test_reduce_future.py</code>, and others.</li> <li>Iteration behavior appears both as future-level tests (<code>tests/test_iter_future.py</code>) and execution-level tests (<code>tests/behavior/test_iter_execution.py</code>), which is valid but not obvious without conventions.</li> </ul>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#4-pytest-test-classes-are-used-heavily-good-but-with-uneven-granularity","level":3,"title":"4) Pytest test classes are used heavily (good), but with uneven granularity","text":"<p>The suite uses class-based grouping consistently, which improves readability. However:</p> <ul> <li>some files contain very large classes spanning many concerns,</li> <li>class names occasionally encode implementation details rather than behavior slices,</li> <li>and there are small naming collisions (<code>TestIterValidation</code> appears in multiple files), which increases navigation friction.</li> </ul>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#recommended-target-model","level":2,"title":"Recommended target model","text":"<p>Use a capability-first + level-second organization.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#proposed-directory-taxonomy","level":3,"title":"Proposed directory taxonomy","text":"<ul> <li><code>tests/unit/</code></li> <li>Pure object/model/API-contract tests without runtime evaluation orchestration.</li> <li> <p>Example topics: task definition validation, node remapping, config resolution logic.</p> </li> <li> <p><code>tests/behavior/</code></p> </li> <li>Behavior-level execution tests through public APIs (what you currently called some \"integration\" tests but are really API behavior tests).</li> <li> <p>Example topics: <code>TaskFuture.run()</code>, <code>TaskFuture.run_async()</code>, fluent chaining semantics, map/reduce/iter end-to-end behavior.</p> </li> <li> <p><code>tests/integration/</code></p> </li> <li> <p>True integration boundaries: process backend serialization, plugin lifecycle with execution engine, dataset store + backend interactions, logging plugin with reporter stack, CLI command surface.</p> </li> <li> <p><code>tests/contracts/</code> (optional)</p> </li> <li>Stable behavior guarantees (error shapes/messages, public type invariants) when you want explicit public API compatibility tests.</li> <li>Typing tests are a strong fit here when they validate the published typing contract (<code>TaskFuture[T]</code>, fluent chaining inference, map/reduce generic propagation) rather than implementation details.</li> </ul>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#where-to-put-typing-tests","level":3,"title":"Where to put typing tests","text":"<p>Short answer: yes, <code>tests/contracts</code> can contain typing tests.</p> <p>A practical split:</p> <ul> <li><code>tests/contracts/typing/</code></li> <li>Tests that lock public type behavior for users and should remain stable across refactors.</li> <li> <p>Examples: expected inference at API boundaries, regressions in generic return types, checker parity assertions.</p> </li> <li> <p><code>tests/unit/typing/</code> (or keep helper assets under <code>tests/typing/</code>)</p> </li> <li>Internal typing helpers/fixtures or checker-specific utilities that support contract tests.</li> <li>Files used only as scaffolding for tools (mypy/pyright/pyrefly/ty) can remain colocated with contract tests or in a small helpers package.</li> </ul> <p>Given the current repository, a low-risk first step would be:</p> <ul> <li>the typing contract tests now live in <code>tests/contracts/typing/</code>,</li> <li>keep checker helpers and contract files colocated in that package,</li> <li>add helper-only fixtures under <code>tests/unit/typing/</code> only when needed.</li> </ul> <p>If you prefer fewer directories, use <code>tests/core/</code> + <code>tests/integration/</code>; the key is to stop overloading \"integration\" for all evaluated graphs.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#migration-mapping-minimal-churn","level":3,"title":"Migration mapping (minimal churn)","text":"<ul> <li>✅ <code>tests/integration/test_tasks_sync.py</code> -&gt; <code>tests/behavior/test_run_sync.py</code></li> <li>✅ <code>tests/integration/test_tasks_async.py</code> -&gt; <code>tests/behavior/test_run_async.py</code></li> <li>✅ <code>tests/integration/test_fluent_api.py</code> -&gt; <code>tests/behavior/test_fluent_api.py</code></li> <li>✅ <code>tests/integration/test_iter.py</code> -&gt; <code>tests/behavior/test_iter_execution.py</code></li> <li>Keep in <code>tests/integration/</code>: <code>test_backends.py</code>, <code>test_dataset_backends.py</code>, <code>test_logging.py</code>, <code>test_plugins.py</code>, and any file that validates multiple subsystems together.</li> </ul> <p>This gives immediate collocation wins without changing assertions.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#guidance-for-pytest-class-organization","level":2,"title":"Guidance for pytest class organization","text":"<p>You can keep classes as the primary structure, but use a simple convention:</p> <ol> <li>Class = one behavior slice</li> <li>Good: <code>TestRunSyncSinglePath</code>, <code>TestRunSyncParallelSiblings</code>, <code>TestRunSyncFailurePropagation</code></li> <li> <p>Avoid: one class covering all sync execution behavior across unrelated concerns.</p> </li> <li> <p>3–10 tests per class target</p> </li> <li> <p>If a class grows beyond ~15 tests, split by behavior axis (success paths, errors, edge cases, backend-specific behavior).</p> </li> <li> <p>Name classes by outcome, not mechanism</p> </li> <li>Prefer <code>TestRetryPolicy</code> over <code>TestRetriesBranchCoverage</code>.</li> <li> <p>Coverage intent belongs in comments/PR notes, not class names.</p> </li> <li> <p>Mirror sync/async structure where possible</p> </li> <li> <p>Parallel class trees improve discoverability:</p> <ul> <li><code>TestRunSyncSinglePath</code> / <code>TestRunAsyncSinglePath</code></li> <li><code>TestRunSyncErrorPropagation</code> / <code>TestRunAsyncErrorPropagation</code></li> </ul> </li> <li> <p>Keep backend-specific execution in dedicated classes</p> </li> <li> <p>e.g., <code>TestRunAsyncProcessBackendPickleConstraints</code>.</p> </li> <li> <p>Use shared helper fixtures instead of mega-classes</p> </li> <li>Keep fixture intent in <code>conftest.py</code> or local file fixtures.</li> <li>Avoid classes that exist mainly to share setup state unless state is behavior-relevant.</li> </ol>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#low-risk-rollout-plan","level":2,"title":"Low-risk rollout plan","text":"<ol> <li>Phase 1: taxonomy + renames ✅</li> <li>Files moved to <code>tests/behavior/</code> and <code>tests/contracts/typing/</code>.</li> <li> <p>Stale comments/docstrings updated away from <code>tests/evaluation/</code>.</p> </li> <li> <p>Phase 2: class shape cleanup ✅ (incremental)</p> </li> <li>Class names were normalized toward behavior intent and sync/async mirroring in run behavior suites.</li> <li> <p>Follow-up splits can still be done for very large classes if desired.</p> </li> <li> <p>Phase 3: markers ✅</p> </li> <li>Added <code>behavior</code>, <code>integration</code>, <code>contracts</code>, and <code>typing_contract</code> markers.</li> <li>Marker assignment is automatic during collection based on test path, enabling CI partitioning without per-test decorator churn.</li> </ol>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#suggested-acceptance-criteria-for-the-refactor","level":2,"title":"Suggested acceptance criteria for the refactor","text":"<ul> <li>A contributor can find all <code>run()/run_async()</code> behavior tests under a single non-integration location.</li> <li><code>tests/integration/</code> only contains cross-subsystem tests.</li> <li>No stale references to non-existent test directories.</li> <li>Class names are unique within their file and descriptive of behavior intent.</li> </ul>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"design/test-suite-organization-review/#optional-next-step","level":2,"title":"Optional next step","text":"<p>If helpful, the next PR can be a pure test-layout refactor with no assertion changes, so risk stays low and git history is easy to review.</p>","path":["Design","Test Suite Organization Review"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"<p>Real-world examples demonstrating how to use Daglite for common computational workflows.</p>","path":["Examples"],"tags":[]},{"location":"examples/#available-examples","level":2,"title":"Available Examples","text":"","path":["Examples"],"tags":[]},{"location":"examples/#etl-pipeline","level":3,"title":"ETL Pipeline","text":"<p>Build data extraction, transformation, and loading pipelines with Pandas.</p> <p>Learn about: - Inline task chaining - Working with DataFrames - Error handling in pipelines - Async execution for I/O-bound tasks</p>","path":["Examples"],"tags":[]},{"location":"examples/#ml-workflow","level":3,"title":"ML Workflow","text":"<p>Create machine learning workflows with hyperparameter tuning.</p> <p>Learn about: - Parameter sweeps with <code>.product()</code> - Model training and evaluation - Finding optimal hyperparameters - Parallel model training</p>","path":["Examples"],"tags":[]},{"location":"examples/#data-processing","level":3,"title":"Data Processing","text":"<p>Process data in parallel with fan-out/fan-in patterns.</p> <p>Learn about: - Parallel task execution - Map-reduce patterns - Batch processing - Aggregating results</p>","path":["Examples"],"tags":[]},{"location":"examples/#more-resources","level":2,"title":"More Resources","text":"<ul> <li>User Guide - In-depth documentation</li> <li>API Reference - Complete API docs</li> <li>Test Suite - Additional usage patterns</li> <li>Plugins - Extend Daglite with plugins</li> </ul>","path":["Examples"],"tags":[]},{"location":"examples/#contributing-examples","level":2,"title":"Contributing Examples","text":"<p>Have a great use case for Daglite? We'd love to feature it! Consider:</p> <ul> <li>Writing a blog post - Share your experience</li> <li>Contributing to docs - Add your example to this section</li> <li>Sharing in discussions - Help others learn from your use case</li> </ul> <p>Share your example</p>","path":["Examples"],"tags":[]},{"location":"examples/data-processing/","level":1,"title":"Data Processing","text":"<p>Parallel data processing patterns using Daglite's fan-out/fan-in operations.</p>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#overview","level":2,"title":"Overview","text":"<p>Data processing often involves:</p> <ol> <li>Fan-out - Split work into parallel tasks</li> <li>Process - Transform each piece independently</li> <li>Fan-in - Aggregate results back together</li> </ol> <p>Daglite provides <code>.product()</code>, <code>.map()</code>, and <code>.join()</code> for these patterns, with optional async execution for I/O-bound workloads.</p>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#basic-parallel-processing","level":2,"title":"Basic Parallel Processing","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#process-multiple-files","level":3,"title":"Process Multiple Files","text":"<pre><code>from daglite import task, evaluate\nimport pandas as pd\nfrom pathlib import Path\n\n@task\ndef process_file(path: str) -&gt; pd.DataFrame:\n    \"\"\"Process a single CSV file.\"\"\"\n    df = pd.read_csv(path)\n\n    # Clean data\n    df = df.dropna()\n\n    # Add source file column\n    df[\"source_file\"] = Path(path).name\n\n    return df\n\n@task\ndef combine_files(dfs: list[pd.DataFrame]) -&gt; pd.DataFrame:\n    \"\"\"Combine processed files.\"\"\"\n    return pd.concat(dfs, ignore_index=True)\n\n# Process multiple files in parallel\nfiles = [\"data_2023.csv\", \"data_2024.csv\", \"data_2025.csv\"]\n\nresult = evaluate(\n    process_file.product(path=files)\n    .join(combine_files)\n)\n\nprint(f\"Total rows: {len(result)}\")\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#process-with-transformation","level":3,"title":"Process with Transformation","text":"<p>Use <code>.map()</code> to transform each result:</p> <pre><code>@task\ndef fetch_data(id: int) -&gt; dict:\n    \"\"\"Fetch data from API.\"\"\"\n    import requests\n    response = requests.get(f\"https://api.example.com/items/{id}\")\n    return response.json()\n\n@task\ndef enrich(data: dict, category: str) -&gt; dict:\n    \"\"\"Enrich with additional info.\"\"\"\n    data[\"category\"] = category\n    data[\"processed_at\"] = pd.Timestamp.now()\n    return data\n\n@task\ndef save_all(items: list[dict]) -&gt; None:\n    \"\"\"Save all items to database.\"\"\"\n    df = pd.DataFrame(items)\n    df.to_sql(\"items\", connection, if_exists=\"append\")\n\n# Fetch, enrich, and save\nevaluate(\n    fetch_data.product(id=range(1, 101))\n    .map(enrich, category=\"products\")\n    .join(save_all)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#batch-processing","level":2,"title":"Batch Processing","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#process-in-batches","level":3,"title":"Process in Batches","text":"<pre><code>from typing import TypeVar\nT = TypeVar('T')\n\n@task\ndef create_batches(items: list[T], batch_size: int) -&gt; list[list[T]]:\n    \"\"\"Split items into batches.\"\"\"\n    return [\n        items[i:i + batch_size]\n        for i in range(0, len(items), batch_size)\n    ]\n\n@task\ndef process_batch(batch: list[int]) -&gt; list[dict]:\n    \"\"\"Process a batch of IDs.\"\"\"\n    results = []\n    for id in batch:\n        # Expensive operation\n        result = expensive_computation(id)\n        results.append(result)\n    return results\n\n@task\ndef flatten(batches: list[list[dict]]) -&gt; list[dict]:\n    \"\"\"Flatten list of lists.\"\"\"\n    return [item for batch in batches for item in batch]\n\n# Process 1000 items in batches of 50\nitems = list(range(1, 1001))\n\nresult = evaluate(\n    create_batches(items=items, batch_size=50)\n    .then(process_batch.product)\n    .join(flatten)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#batch-api-calls","level":3,"title":"Batch API Calls","text":"<pre><code>@task\ndef batch_api_call(ids: list[int]) -&gt; list[dict]:\n    \"\"\"Call API with batch of IDs.\"\"\"\n    import requests\n    response = requests.post(\n        \"https://api.example.com/batch\",\n        json={\"ids\": ids}\n    )\n    return response.json()[\"results\"]\n\n@task\ndef create_batches(items: list[int], batch_size: int) -&gt; list[list[int]]:\n    \"\"\"Create batches.\"\"\"\n    return [items[i:i+batch_size] for i in range(0, len(items), batch_size)]\n\n@task\ndef flatten_results(batches: list[list[dict]]) -&gt; list[dict]:\n    \"\"\"Flatten results.\"\"\"\n    return [item for batch in batches for item in batch]\n\n# Process 10,000 IDs in batches of 100\nids = range(1, 10001)\n\nresults = evaluate(\n    create_batches(items=list(ids), batch_size=100)\n    .then(batch_api_call.product)\n    .join(flatten_results)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#map-reduce-patterns","level":2,"title":"Map-Reduce Patterns","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#word-count","level":3,"title":"Word Count","text":"<p>Classic map-reduce example:</p> <pre><code>@task\ndef read_file(path: str) -&gt; str:\n    \"\"\"Read file content.\"\"\"\n    with open(path) as f:\n        return f.read()\n\n@task\ndef count_words(text: str) -&gt; dict[str, int]:\n    \"\"\"Count words in text (map phase).\"\"\"\n    from collections import Counter\n    words = text.lower().split()\n    return Counter(words)\n\n@task\ndef merge_counts(counts: list[dict[str, int]]) -&gt; dict[str, int]:\n    \"\"\"Merge word counts (reduce phase).\"\"\"\n    from collections import Counter\n    total = Counter()\n    for count in counts:\n        total.update(count)\n    return dict(total)\n\n# Count words across multiple files\nfiles = [\"doc1.txt\", \"doc2.txt\", \"doc3.txt\"]\n\nword_counts = evaluate(\n    read_file.product(path=files)\n    .map(count_words)\n    .join(merge_counts)\n)\n\n# Top 10 words\ntop_words = sorted(\n    word_counts.items(),\n    key=lambda x: x[1],\n    reverse=True\n)[:10]\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#aggregate-statistics","level":3,"title":"Aggregate Statistics","text":"<pre><code>@task\ndef load_partition(partition: int) -&gt; pd.DataFrame:\n    \"\"\"Load data partition.\"\"\"\n    return pd.read_parquet(f\"data/partition_{partition}.parquet\")\n\n@task\ndef compute_stats(df: pd.DataFrame) -&gt; dict:\n    \"\"\"Compute statistics for partition.\"\"\"\n    return {\n        \"count\": len(df),\n        \"sum\": df[\"value\"].sum(),\n        \"min\": df[\"value\"].min(),\n        \"max\": df[\"value\"].max(),\n        \"sum_squares\": (df[\"value\"] ** 2).sum()\n    }\n\n@task\ndef merge_stats(stats: list[dict]) -&gt; dict:\n    \"\"\"Merge statistics from all partitions.\"\"\"\n    total_count = sum(s[\"count\"] for s in stats)\n    total_sum = sum(s[\"sum\"] for s in stats)\n\n    return {\n        \"count\": total_count,\n        \"mean\": total_sum / total_count,\n        \"min\": min(s[\"min\"] for s in stats),\n        \"max\": max(s[\"max\"] for s in stats),\n        \"std\": (\n            sum(s[\"sum_squares\"] for s in stats) / total_count -\n            (total_sum / total_count) ** 2\n        ) ** 0.5\n    }\n\n# Compute statistics across 100 partitions\nstats = evaluate(\n    load_partition.product(partition=range(100))\n    .map(compute_stats)\n    .join(merge_stats)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#async-processing","level":2,"title":"Async Processing","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#io-bound-tasks","level":3,"title":"I/O-Bound Tasks","text":"<p>Use async execution for I/O-heavy workloads:</p> <pre><code>@task(backend_name=\"threading\")\ndef fetch_url(url: str) -&gt; dict:\n    \"\"\"Fetch data from URL (I/O-bound).\"\"\"\n    import requests\n    response = requests.get(url, timeout=10)\n    return {\n        \"url\": url,\n        \"status\": response.status_code,\n        \"size\": len(response.content)\n    }\n\n@task\ndef summarize(results: list[dict]) -&gt; dict:\n    \"\"\"Summarize fetch results.\"\"\"\n    return {\n        \"total\": len(results),\n        \"successful\": sum(1 for r in results if r[\"status\"] == 200),\n        \"total_size\": sum(r[\"size\"] for r in results)\n    }\n\n# Fetch 100 URLs in parallel\nurls = [f\"https://example.com/page{i}\" for i in range(100)]\n\nsummary = evaluate(\n    fetch_url.product(url=urls)\n    .join(summarize),\n    use_async=True  # Enable async execution\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#cpu-bound-tasks","level":3,"title":"CPU-Bound Tasks","text":"<p>Use multiprocessing backend for CPU-intensive tasks:</p> <pre><code>@task(backend_name=\"multiprocessing\")\ndef compute_intensive(n: int) -&gt; float:\n    \"\"\"CPU-intensive computation.\"\"\"\n    import math\n    result = 0.0\n    for i in range(n):\n        result += math.sin(i) * math.cos(i)\n    return result\n\n@task\ndef sum_results(results: list[float]) -&gt; float:\n    \"\"\"Sum all results.\"\"\"\n    return sum(results)\n\n# Run CPU-intensive tasks in parallel\nresult = evaluate(\n    compute_intensive.product(n=[1000000, 2000000, 3000000])\n    .join(sum_results),\n    use_async=True\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#data-validation-pipeline","level":2,"title":"Data Validation Pipeline","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#validate-multiple-files","level":3,"title":"Validate Multiple Files","text":"<pre><code>from typing import Literal\n\n@task\ndef validate_schema(path: str, expected_columns: list[str]) -&gt; dict:\n    \"\"\"Validate file schema.\"\"\"\n    df = pd.read_csv(path)\n\n    missing = set(expected_columns) - set(df.columns)\n    extra = set(df.columns) - set(expected_columns)\n\n    return {\n        \"path\": path,\n        \"valid\": len(missing) == 0,\n        \"missing_columns\": list(missing),\n        \"extra_columns\": list(extra),\n        \"row_count\": len(df)\n    }\n\n@task\ndef check_data_quality(path: str) -&gt; dict:\n    \"\"\"Check data quality metrics.\"\"\"\n    df = pd.read_csv(path)\n\n    return {\n        \"path\": path,\n        \"null_percentage\": (df.isnull().sum().sum() / df.size) * 100,\n        \"duplicate_rows\": df.duplicated().sum(),\n        \"has_negatives\": (df.select_dtypes(include=[np.number]) &lt; 0).any().any()\n    }\n\n@task\ndef generate_report(\n    schema_results: list[dict],\n    quality_results: list[dict]\n) -&gt; dict:\n    \"\"\"Generate validation report.\"\"\"\n    return {\n        \"total_files\": len(schema_results),\n        \"valid_schemas\": sum(1 for r in schema_results if r[\"valid\"]),\n        \"total_rows\": sum(r[\"row_count\"] for r in schema_results),\n        \"quality_issues\": [\n            r for r in quality_results\n            if r[\"null_percentage\"] &gt; 5 or r[\"duplicate_rows\"] &gt; 0\n        ]\n    }\n\n# Validate multiple files\nfiles = [\"file1.csv\", \"file2.csv\", \"file3.csv\"]\nexpected_cols = [\"id\", \"name\", \"value\", \"date\"]\n\nschema_checks = validate_schema.product(\n    path=files,\n    expected_columns=[expected_cols]\n)\n\nquality_checks = check_data_quality.product(path=files)\n\nreport = evaluate(\n    generate_report(\n        schema_results=schema_checks,\n        quality_results=quality_checks\n    )\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#pairwise-operations-with-zip","level":2,"title":"Pairwise Operations with <code>.zip()</code>","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#process-pairs","level":3,"title":"Process Pairs","text":"<pre><code>@task\ndef compare_files(file1: str, file2: str) -&gt; dict:\n    \"\"\"Compare two files.\"\"\"\n    df1 = pd.read_csv(file1)\n    df2 = pd.read_csv(file2)\n\n    return {\n        \"files\": (file1, file2),\n        \"row_diff\": len(df1) - len(df2),\n        \"col_diff\": set(df1.columns) - set(df2.columns),\n        \"identical\": df1.equals(df2)\n    }\n\n# Compare file pairs\ncurrent_files = [\"current_1.csv\", \"current_2.csv\", \"current_3.csv\"]\nprevious_files = [\"previous_1.csv\", \"previous_2.csv\", \"previous_3.csv\"]\n\ncomparisons = evaluate(\n    compare_files.zip(file1=current_files, file2=previous_files)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#merge-corresponding-items","level":3,"title":"Merge Corresponding Items","text":"<pre><code>@task\ndef merge_data(main: pd.DataFrame, supplementary: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Merge main and supplementary data.\"\"\"\n    return main.merge(supplementary, on=\"id\", how=\"left\")\n\n# Load main files\nmain_dfs = load_file.product(path=[\"main_1.csv\", \"main_2.csv\"])\n\n# Load supplementary files\nsupp_dfs = load_file.product(path=[\"supp_1.csv\", \"supp_2.csv\"])\n\n# Merge pairwise\nmerged = evaluate(\n    merge_data.zip(main=main_dfs, supplementary=supp_dfs)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#best-practices","level":2,"title":"Best Practices","text":"","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#1-choose-appropriate-backend","level":3,"title":"1. Choose Appropriate Backend","text":"<pre><code># I/O-bound: Use threading\n@task(backend_name=\"threading\")\ndef fetch_api(url: str):\n    ...\n\n# CPU-bound: Use multiprocessing\n@task(backend_name=\"multiprocessing\")\ndef compute_fft(data: np.ndarray):\n    ...\n\n# Mixed: Let Daglite choose based on task\n@task\ndef process(data):\n    ...\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#2-handle-errors-gracefully","level":3,"title":"2. Handle Errors Gracefully","text":"<pre><code>@task\ndef safe_process(path: str) -&gt; dict:\n    \"\"\"Process file with error handling.\"\"\"\n    try:\n        df = pd.read_csv(path)\n        result = process_dataframe(df)\n        return {\"path\": path, \"success\": True, \"result\": result}\n    except Exception as e:\n        return {\"path\": path, \"success\": False, \"error\": str(e)}\n\n@task\ndef filter_successes(results: list[dict]) -&gt; list[dict]:\n    \"\"\"Extract successful results.\"\"\"\n    successes = [r[\"result\"] for r in results if r[\"success\"]]\n    failures = [r for r in results if not r[\"success\"]]\n\n    if failures:\n        print(f\"Failed files: {[r['path'] for r in failures]}\")\n\n    return successes\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#3-monitor-progress","level":3,"title":"3. Monitor Progress","text":"<pre><code>from daglite.plugins.hooks import hook_impl\n\nclass ProgressTracker:\n    \"\"\"Track task completion.\"\"\"\n\n    def __init__(self):\n        self.completed = 0\n        self.total = 0\n\n    @hook_impl\n    def before_graph_execute(self, root_id, node_count):\n        self.total = node_count\n        self.completed = 0\n        print(f\"Starting: 0/{self.total}\")\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        self.completed += 1\n        print(f\"Progress: {self.completed}/{self.total}\")\n\n# Use with plugin\nfrom daglite.plugins.manager import register_plugins\n\ntracker = ProgressTracker()\nregister_plugins(tracker)\n\nresult = evaluate(\n    process_file.product(path=files)\n    .join(combine_files)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#4-batch-for-efficiency","level":3,"title":"4. Batch for Efficiency","text":"<pre><code># Bad: One API call per item (100 calls)\nresults = evaluate(\n    fetch_single_item.product(id=range(100))\n)\n\n# Good: Batch API calls (10 calls)\nbatches = create_batches(list(range(100)), batch_size=10)\nresults = evaluate(\n    fetch_batch.product(batch=batches)\n    .join(flatten_results)\n)\n</code></pre>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/data-processing/#see-also","level":2,"title":"See Also","text":"<ul> <li>ETL Pipeline - Data transformation patterns</li> <li>ML Workflow - Machine learning examples</li> <li>Evaluation Guide - Async execution details</li> </ul>","path":["Examples","Data Processing"],"tags":[]},{"location":"examples/etl-pipeline/","level":1,"title":"ETL Pipeline","text":"<p>Build a complete Extract-Transform-Load (ETL) pipeline using Daglite and Pandas.</p>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#overview","level":2,"title":"Overview","text":"<p>ETL pipelines are a common pattern in data engineering:</p> <ol> <li>Extract - Load data from various sources (CSV, databases, APIs)</li> <li>Transform - Clean, filter, and reshape the data</li> <li>Load - Save the processed data to a destination</li> </ol> <p>Daglite's fluent API makes it easy to chain these operations while maintaining type safety and explicit dependencies.</p>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#basic-etl-pipeline","level":2,"title":"Basic ETL Pipeline","text":"","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#simple-inline-pipeline","level":3,"title":"Simple Inline Pipeline","text":"<pre><code>from daglite import task, evaluate\nimport pandas as pd\n\n@task\ndef extract(source: str) -&gt; pd.DataFrame:\n    \"\"\"Extract data from CSV source.\"\"\"\n    return pd.read_csv(source)\n\n@task\ndef transform(df: pd.DataFrame, columns: list[str]) -&gt; pd.DataFrame:\n    \"\"\"Transform by selecting and cleaning columns.\"\"\"\n    return df[columns].dropna()\n\n@task\ndef load(df: pd.DataFrame, destination: str) -&gt; None:\n    \"\"\"Load data to parquet destination.\"\"\"\n    df.to_parquet(destination)\n\n# Build and execute pipeline\nevaluate(\n    extract(source=\"data.csv\")\n    .then(transform, columns=[\"id\", \"name\", \"value\"])\n    .then(load, destination=\"output.parquet\")\n)\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#with-error-handling","level":3,"title":"With Error Handling","text":"<pre><code>@task\ndef extract_with_validation(source: str) -&gt; pd.DataFrame:\n    \"\"\"Extract and validate data.\"\"\"\n    try:\n        df = pd.read_csv(source)\n    except FileNotFoundError:\n        raise ValueError(f\"Source file not found: {source}\")\n\n    # Validate required columns\n    required_columns = [\"id\", \"name\", \"value\"]\n    missing = set(required_columns) - set(df.columns)\n    if missing:\n        raise ValueError(f\"Missing required columns: {missing}\")\n\n    return df\n\n@task\ndef transform_with_logging(\n    df: pd.DataFrame,\n    columns: list[str]\n) -&gt; pd.DataFrame:\n    \"\"\"Transform data with logging.\"\"\"\n    print(f\"Input rows: {len(df)}\")\n\n    # Select columns\n    df = df[columns]\n\n    # Remove nulls\n    df = df.dropna()\n\n    print(f\"Output rows: {len(df)} ({len(df)/len(df)*100:.1f}% of input)\")\n\n    return df\n\n# Execute with error handling\ntry:\n    result = evaluate(\n        extract_with_validation(source=\"data.csv\")\n        .then(transform_with_logging, columns=[\"id\", \"name\", \"value\"])\n        .then(load, destination=\"output.parquet\")\n    )\n    print(\"Pipeline completed successfully!\")\nexcept Exception as e:\n    print(f\"Pipeline failed: {e}\")\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#multi-source-etl","level":2,"title":"Multi-Source ETL","text":"<p>Combine data from multiple sources:</p> <pre><code>@task\ndef extract_csv(path: str) -&gt; pd.DataFrame:\n    \"\"\"Extract from CSV.\"\"\"\n    return pd.read_csv(path)\n\n@task\ndef extract_json(path: str) -&gt; pd.DataFrame:\n    \"\"\"Extract from JSON.\"\"\"\n    return pd.read_json(path)\n\n@task\ndef extract_db(table: str, connection_string: str) -&gt; pd.DataFrame:\n    \"\"\"Extract from database.\"\"\"\n    import sqlalchemy\n    engine = sqlalchemy.create_engine(connection_string)\n    return pd.read_sql_table(table, engine)\n\n@task\ndef merge_sources(\n    customers: pd.DataFrame,\n    orders: pd.DataFrame,\n    products: pd.DataFrame\n) -&gt; pd.DataFrame:\n    \"\"\"Merge multiple data sources.\"\"\"\n    # Join customers with orders\n    customer_orders = customers.merge(\n        orders,\n        on=\"customer_id\",\n        how=\"inner\"\n    )\n\n    # Join with products\n    result = customer_orders.merge(\n        products,\n        on=\"product_id\",\n        how=\"inner\"\n    )\n\n    return result\n\n@task\ndef aggregate(df: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Calculate aggregates.\"\"\"\n    return df.groupby(\"customer_id\").agg({\n        \"order_id\": \"count\",\n        \"amount\": \"sum\",\n        \"product_id\": \"nunique\"\n    }).reset_index()\n\n# Extract from multiple sources\ncustomers_df = extract_csv(path=\"customers.csv\")\norders_df = extract_json(path=\"orders.json\")\nproducts_df = extract_db(\n    table=\"products\",\n    connection_string=\"postgresql://localhost/db\"\n)\n\n# Combine and process\nresult = evaluate(\n    merge_sources(\n        customers=customers_df,\n        orders=orders_df,\n        products=products_df\n    )\n    .then(aggregate)\n    .then(load, destination=\"customer_summary.parquet\")\n)\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#incremental-etl","level":2,"title":"Incremental ETL","text":"<p>Process data incrementally with date partitions:</p> <pre><code>from datetime import datetime, timedelta\n\n@task\ndef extract_partition(\n    source: str,\n    start_date: str,\n    end_date: str\n) -&gt; pd.DataFrame:\n    \"\"\"Extract data for a date range.\"\"\"\n    df = pd.read_csv(source)\n    df[\"date\"] = pd.to_datetime(df[\"date\"])\n\n    mask = (df[\"date\"] &gt;= start_date) &amp; (df[\"date\"] &lt;= end_date)\n    return df[mask]\n\n@task\ndef deduplicate(df: pd.DataFrame, key: str) -&gt; pd.DataFrame:\n    \"\"\"Remove duplicates keeping latest record.\"\"\"\n    return df.sort_values(\"updated_at\").drop_duplicates(\n        subset=[key],\n        keep=\"last\"\n    )\n\n@task\ndef load_partition(\n    df: pd.DataFrame,\n    destination: str,\n    partition_date: str\n) -&gt; None:\n    \"\"\"Load data to partitioned destination.\"\"\"\n    partition_path = f\"{destination}/date={partition_date}\"\n    df.to_parquet(partition_path)\n\n# Process last 7 days\ntoday = datetime.now()\ndates = [(today - timedelta(days=i)).strftime(\"%Y-%m-%d\") for i in range(7)]\n\n@task\ndef process_date(date: str) -&gt; None:\n    \"\"\"Process single date partition.\"\"\"\n    return evaluate(\n        extract_partition(\n            source=\"raw_data.csv\",\n            start_date=date,\n            end_date=date\n        )\n        .then(transform, columns=[\"id\", \"value\", \"date\"])\n        .then(deduplicate, key=\"id\")\n        .then(load_partition, destination=\"output\", partition_date=date)\n    )\n\n# Process all dates in parallel\nevaluate(\n    process_date.product(date=dates)\n)\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#async-etl-for-io-bound-tasks","level":2,"title":"Async ETL for I/O-Bound Tasks","text":"<p>Use async execution for better performance with I/O operations:</p> <pre><code>@task(backend_name=\"threading\")\ndef fetch_api_data(endpoint: str, api_key: str) -&gt; pd.DataFrame:\n    \"\"\"Fetch data from API (I/O-bound).\"\"\"\n    import requests\n    response = requests.get(\n        f\"https://api.example.com/{endpoint}\",\n        headers={\"Authorization\": f\"Bearer {api_key}\"}\n    )\n    return pd.DataFrame(response.json())\n\n@task\ndef combine_api_results(dfs: list[pd.DataFrame]) -&gt; pd.DataFrame:\n    \"\"\"Combine multiple API responses.\"\"\"\n    return pd.concat(dfs, ignore_index=True)\n\n# Fetch from multiple endpoints in parallel\nendpoints = [\"users\", \"orders\", \"products\", \"reviews\"]\n\nresult = evaluate(\n    fetch_api_data.product(\n        endpoint=endpoints,\n        api_key=\"your_api_key\"\n    )\n    .join(combine_api_results)\n    .then(transform, columns=[\"id\", \"name\", \"value\"])\n    .then(load, destination=\"api_data.parquet\"),\n    use_async=True  # Enable async execution\n)\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#cli-pipeline","level":2,"title":"CLI Pipeline","text":"<p>Make your ETL pipeline runnable from the command line:</p> <pre><code># etl_pipeline.py\nfrom daglite import task, pipeline\nimport pandas as pd\n\n@task\ndef extract(source: str) -&gt; pd.DataFrame:\n    return pd.read_csv(source)\n\n@task\ndef transform(df: pd.DataFrame, columns: list[str]) -&gt; pd.DataFrame:\n    return df[columns].dropna()\n\n@task\ndef load(df: pd.DataFrame, destination: str) -&gt; None:\n    df.to_parquet(destination)\n\n@pipeline\ndef daily_etl(\n    source: str,\n    destination: str,\n    columns: list[str] = None\n):\n    \"\"\"Daily ETL pipeline.\"\"\"\n    if columns is None:\n        columns = [\"id\", \"name\", \"value\"]\n\n    return (\n        extract(source=source)\n        .then(transform, columns=columns)\n        .then(load, destination=destination)\n    )\n</code></pre> <p>Run from command line:</p> <pre><code>daglite run etl_pipeline.daily_etl \\\n    --param source=data.csv \\\n    --param destination=output.parquet \\\n    --param columns='[\"id\",\"name\",\"value\"]'\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#best-practices","level":2,"title":"Best Practices","text":"","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#1-validate-early","level":3,"title":"1. Validate Early","text":"<p>Validate data as soon as it's extracted:</p> <pre><code>@task\ndef extract_and_validate(source: str) -&gt; pd.DataFrame:\n    df = pd.read_csv(source)\n\n    # Check for empty data\n    if df.empty:\n        raise ValueError(\"Source data is empty\")\n\n    # Validate schema\n    expected_columns = [\"id\", \"name\", \"value\"]\n    if not all(col in df.columns for col in expected_columns):\n        raise ValueError(f\"Missing columns. Expected: {expected_columns}\")\n\n    return df\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#2-make-transforms-idempotent","level":3,"title":"2. Make Transforms Idempotent","text":"<p>Ensure transforms can be re-run safely:</p> <pre><code>@task\ndef idempotent_transform(df: pd.DataFrame) -&gt; pd.DataFrame:\n    # Normalize column names (safe to run multiple times)\n    df.columns = df.columns.str.lower().str.strip()\n\n    # Remove duplicates (safe to run multiple times)\n    df = df.drop_duplicates()\n\n    # Fill nulls with default (safe to run multiple times)\n    df = df.fillna({\"value\": 0})\n\n    return df\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#3-use-type-hints","level":3,"title":"3. Use Type Hints","text":"<p>Type hints help catch errors and improve IDE support:</p> <pre><code>from typing import Literal\n\n@task\ndef transform(\n    df: pd.DataFrame,\n    operation: Literal[\"filter\", \"aggregate\", \"pivot\"]\n) -&gt; pd.DataFrame:\n    if operation == \"filter\":\n        return df[df[\"value\"] &gt; 0]\n    elif operation == \"aggregate\":\n        return df.groupby(\"category\").sum()\n    elif operation == \"pivot\":\n        return df.pivot_table(index=\"date\", columns=\"category\", values=\"value\")\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#4-log-progress","level":3,"title":"4. Log Progress","text":"<p>Add logging for long-running pipelines:</p> <pre><code>import logging\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\n@task\ndef transform_with_logging(df: pd.DataFrame) -&gt; pd.DataFrame:\n    logger.info(f\"Starting transform on {len(df)} rows\")\n\n    df = df.dropna()\n    logger.info(f\"After dropna: {len(df)} rows\")\n\n    df = df[df[\"value\"] &gt; 0]\n    logger.info(f\"After filtering: {len(df)} rows\")\n\n    return df\n</code></pre>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/etl-pipeline/#see-also","level":2,"title":"See Also","text":"<ul> <li>Pandas Documentation - DataFrame operations</li> <li>Data Processing Example - Parallel processing patterns</li> <li>CLI Reference - Command-line interface for pipelines</li> </ul>","path":["Examples","ETL Pipeline"],"tags":[]},{"location":"examples/ml-workflow/","level":1,"title":"ML Workflow","text":"<p>Build machine learning workflows with hyperparameter tuning, cross-validation, and model evaluation using Daglite.</p>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#overview","level":2,"title":"Overview","text":"<p>Machine learning workflows typically involve:</p> <ol> <li>Data preparation - Load and preprocess training data</li> <li>Model training - Train models with different hyperparameters</li> <li>Evaluation - Assess model performance</li> <li>Selection - Choose the best performing model</li> </ol> <p>Daglite's <code>.product()</code> method makes hyperparameter sweeps easy, while <code>.map()</code> and <code>.join()</code> handle parallel training and result aggregation.</p>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#basic-ml-pipeline","level":2,"title":"Basic ML Pipeline","text":"","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#simple-training-pipeline","level":3,"title":"Simple Training Pipeline","text":"<pre><code>from daglite import task, evaluate\nimport pandas as pd\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.metrics import accuracy_score\n\n@task\ndef load_data(path: str) -&gt; pd.DataFrame:\n    \"\"\"Load training data.\"\"\"\n    return pd.read_csv(path)\n\n@task\ndef split_data(\n    df: pd.DataFrame,\n    target_col: str,\n    test_size: float = 0.2\n) -&gt; dict:\n    \"\"\"Split into train/test sets.\"\"\"\n    X = df.drop(columns=[target_col])\n    y = df[target_col]\n\n    X_train, X_test, y_train, y_test = train_test_split(\n        X, y, test_size=test_size, random_state=42\n    )\n\n    return {\n        \"X_train\": X_train,\n        \"X_test\": X_test,\n        \"y_train\": y_train,\n        \"y_test\": y_test\n    }\n\n@task\ndef train_model(\n    data: dict,\n    n_estimators: int,\n    max_depth: int\n) -&gt; dict:\n    \"\"\"Train random forest model.\"\"\"\n    model = RandomForestClassifier(\n        n_estimators=n_estimators,\n        max_depth=max_depth,\n        random_state=42\n    )\n\n    model.fit(data[\"X_train\"], data[\"y_train\"])\n\n    # Evaluate\n    y_pred = model.predict(data[\"X_test\"])\n    accuracy = accuracy_score(data[\"y_test\"], y_pred)\n\n    return {\n        \"model\": model,\n        \"accuracy\": accuracy,\n        \"params\": {\n            \"n_estimators\": n_estimators,\n            \"max_depth\": max_depth\n        }\n    }\n\n# Execute training pipeline\nresult = evaluate(\n    load_data(path=\"train.csv\")\n    .then(split_data, target_col=\"target\")\n    .then(train_model, n_estimators=100, max_depth=10)\n)\n\nprint(f\"Model accuracy: {result['accuracy']:.3f}\")\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#hyperparameter-tuning","level":2,"title":"Hyperparameter Tuning","text":"","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#grid-search-with-product","level":3,"title":"Grid Search with <code>.product()</code>","text":"<pre><code>@task\ndef train_model(\n    data: dict,\n    n_estimators: int,\n    max_depth: int,\n    min_samples_split: int\n) -&gt; dict:\n    \"\"\"Train model with specified hyperparameters.\"\"\"\n    model = RandomForestClassifier(\n        n_estimators=n_estimators,\n        max_depth=max_depth,\n        min_samples_split=min_samples_split,\n        random_state=42\n    )\n\n    model.fit(data[\"X_train\"], data[\"y_train\"])\n\n    y_pred = model.predict(data[\"X_test\"])\n    accuracy = accuracy_score(data[\"y_test\"], y_pred)\n\n    return {\n        \"model\": model,\n        \"accuracy\": accuracy,\n        \"params\": {\n            \"n_estimators\": n_estimators,\n            \"max_depth\": max_depth,\n            \"min_samples_split\": min_samples_split\n        }\n    }\n\n@task\ndef find_best_model(results: list[dict]) -&gt; dict:\n    \"\"\"Select model with highest accuracy.\"\"\"\n    best = max(results, key=lambda r: r[\"accuracy\"])\n    print(f\"Best accuracy: {best['accuracy']:.3f}\")\n    print(f\"Best params: {best['params']}\")\n    return best\n\n# Load and split data\ndata = evaluate(\n    load_data(path=\"train.csv\")\n    .then(split_data, target_col=\"target\")\n)\n\n# Hyperparameter grid search\nbest_model = evaluate(\n    train_model.product(\n        data=[data],  # Fixed parameter\n        n_estimators=[50, 100, 200],\n        max_depth=[5, 10, 15, None],\n        min_samples_split=[2, 5, 10]\n    )\n    .join(find_best_model)\n)\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#random-search","level":3,"title":"Random Search","text":"<pre><code>import numpy as np\n\n@task\ndef generate_random_params(seed: int) -&gt; dict:\n    \"\"\"Generate random hyperparameters.\"\"\"\n    np.random.seed(seed)\n    return {\n        \"n_estimators\": int(np.random.choice([50, 100, 150, 200])),\n        \"max_depth\": int(np.random.choice([5, 10, 15, 20, None])),\n        \"min_samples_split\": int(np.random.choice([2, 5, 10, 20])),\n        \"min_samples_leaf\": int(np.random.choice([1, 2, 4, 8]))\n    }\n\n@task\ndef train_with_params(data: dict, params: dict) -&gt; dict:\n    \"\"\"Train model with parameter dict.\"\"\"\n    model = RandomForestClassifier(**params, random_state=42)\n    model.fit(data[\"X_train\"], data[\"y_train\"])\n\n    y_pred = model.predict(data[\"X_test\"])\n    accuracy = accuracy_score(data[\"y_test\"], y_pred)\n\n    return {\"model\": model, \"accuracy\": accuracy, \"params\": params}\n\n# Random search with 20 trials\ndata = evaluate(\n    load_data(path=\"train.csv\")\n    .then(split_data, target_col=\"target\")\n)\n\nbest_model = evaluate(\n    generate_random_params.product(seed=range(20))\n    .map(train_with_params, data=data)\n    .join(find_best_model)\n)\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#cross-validation","level":2,"title":"Cross-Validation","text":"","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#k-fold-cross-validation","level":3,"title":"K-Fold Cross-Validation","text":"<pre><code>from sklearn.model_selection import KFold\n\n@task\ndef create_folds(\n    df: pd.DataFrame,\n    target_col: str,\n    n_folds: int = 5\n) -&gt; list[dict]:\n    \"\"\"Create k-fold splits.\"\"\"\n    X = df.drop(columns=[target_col])\n    y = df[target_col]\n\n    kf = KFold(n_splits=n_folds, shuffle=True, random_state=42)\n\n    folds = []\n    for train_idx, val_idx in kf.split(X):\n        folds.append({\n            \"X_train\": X.iloc[train_idx],\n            \"X_val\": X.iloc[val_idx],\n            \"y_train\": y.iloc[train_idx],\n            \"y_val\": y.iloc[val_idx]\n        })\n\n    return folds\n\n@task\ndef train_on_fold(\n    fold: dict,\n    n_estimators: int,\n    max_depth: int\n) -&gt; float:\n    \"\"\"Train and evaluate on a single fold.\"\"\"\n    model = RandomForestClassifier(\n        n_estimators=n_estimators,\n        max_depth=max_depth,\n        random_state=42\n    )\n\n    model.fit(fold[\"X_train\"], fold[\"y_train\"])\n    y_pred = model.predict(fold[\"X_val\"])\n\n    return accuracy_score(fold[\"y_val\"], y_pred)\n\n@task\ndef average_scores(scores: list[float]) -&gt; float:\n    \"\"\"Calculate mean cross-validation score.\"\"\"\n    mean_score = np.mean(scores)\n    std_score = np.std(scores)\n    print(f\"CV Score: {mean_score:.3f} (+/- {std_score:.3f})\")\n    return mean_score\n\n# Create folds\nfolds = evaluate(\n    load_data(path=\"train.csv\")\n    .then(create_folds, target_col=\"target\", n_folds=5)\n)\n\n# Train on each fold\ncv_score = evaluate(\n    train_on_fold.product(\n        fold=folds,\n        n_estimators=[100],\n        max_depth=[10]\n    )\n    .join(average_scores)\n)\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#cross-validation-with-hyperparameter-tuning","level":3,"title":"Cross-Validation with Hyperparameter Tuning","text":"<pre><code>@task\ndef cv_with_params(\n    folds: list[dict],\n    n_estimators: int,\n    max_depth: int\n) -&gt; dict:\n    \"\"\"Run cross-validation for hyperparameter combination.\"\"\"\n    scores = []\n\n    for fold in folds:\n        model = RandomForestClassifier(\n            n_estimators=n_estimators,\n            max_depth=max_depth,\n            random_state=42\n        )\n        model.fit(fold[\"X_train\"], fold[\"y_train\"])\n        y_pred = model.predict(fold[\"X_val\"])\n        score = accuracy_score(fold[\"y_val\"], y_pred)\n        scores.append(score)\n\n    return {\n        \"mean_score\": np.mean(scores),\n        \"std_score\": np.std(scores),\n        \"params\": {\n            \"n_estimators\": n_estimators,\n            \"max_depth\": max_depth\n        }\n    }\n\n@task\ndef find_best_params(results: list[dict]) -&gt; dict:\n    \"\"\"Find best hyperparameters by CV score.\"\"\"\n    best = max(results, key=lambda r: r[\"mean_score\"])\n    print(f\"Best CV score: {best['mean_score']:.3f} (+/- {best['std_score']:.3f})\")\n    print(f\"Best params: {best['params']}\")\n    return best\n\n# Create folds\nfolds = evaluate(\n    load_data(path=\"train.csv\")\n    .then(create_folds, target_col=\"target\", n_folds=5)\n)\n\n# Grid search with cross-validation\nbest_params = evaluate(\n    cv_with_params.product(\n        folds=[folds],\n        n_estimators=[50, 100, 200],\n        max_depth=[5, 10, 15]\n    )\n    .join(find_best_params)\n)\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#feature-engineering-pipeline","level":2,"title":"Feature Engineering Pipeline","text":"<pre><code>@task\ndef engineer_features(df: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Create derived features.\"\"\"\n    df = df.copy()\n\n    # Polynomial features\n    df[\"feature1_squared\"] = df[\"feature1\"] ** 2\n    df[\"feature1_feature2\"] = df[\"feature1\"] * df[\"feature2\"]\n\n    # Binning\n    df[\"age_group\"] = pd.cut(\n        df[\"age\"],\n        bins=[0, 18, 35, 50, 100],\n        labels=[\"young\", \"adult\", \"middle\", \"senior\"]\n    )\n\n    # One-hot encoding\n    df = pd.get_dummies(df, columns=[\"age_group\"])\n\n    return df\n\n@task\ndef select_features(\n    df: pd.DataFrame,\n    target_col: str,\n    n_features: int = 10\n) -&gt; pd.DataFrame:\n    \"\"\"Select top features using mutual information.\"\"\"\n    from sklearn.feature_selection import mutual_info_classif\n\n    X = df.drop(columns=[target_col])\n    y = df[target_col]\n\n    # Calculate mutual information\n    mi_scores = mutual_info_classif(X, y)\n\n    # Select top features\n    top_features = X.columns[np.argsort(mi_scores)[-n_features:]]\n\n    return df[[target_col] + list(top_features)]\n\n# Feature engineering + training pipeline\nresult = evaluate(\n    load_data(path=\"train.csv\")\n    .then(engineer_features)\n    .then(select_features, target_col=\"target\", n_features=10)\n    .then(split_data, target_col=\"target\")\n    .then(train_model, n_estimators=100, max_depth=10)\n)\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#model-comparison","level":2,"title":"Model Comparison","text":"<p>Compare multiple model types:</p> <pre><code>from sklearn.linear_model import LogisticRegression\nfrom sklearn.svm import SVC\nfrom sklearn.ensemble import GradientBoostingClassifier\n\n@task\ndef train_logistic_regression(data: dict, C: float) -&gt; dict:\n    \"\"\"Train logistic regression.\"\"\"\n    model = LogisticRegression(C=C, max_iter=1000, random_state=42)\n    model.fit(data[\"X_train\"], data[\"y_train\"])\n\n    y_pred = model.predict(data[\"X_test\"])\n    accuracy = accuracy_score(data[\"y_test\"], y_pred)\n\n    return {\n        \"model_type\": \"LogisticRegression\",\n        \"model\": model,\n        \"accuracy\": accuracy,\n        \"params\": {\"C\": C}\n    }\n\n@task\ndef train_svm(data: dict, C: float, kernel: str) -&gt; dict:\n    \"\"\"Train SVM.\"\"\"\n    model = SVC(C=C, kernel=kernel, random_state=42)\n    model.fit(data[\"X_train\"], data[\"y_train\"])\n\n    y_pred = model.predict(data[\"X_test\"])\n    accuracy = accuracy_score(data[\"y_test\"], y_pred)\n\n    return {\n        \"model_type\": \"SVM\",\n        \"model\": model,\n        \"accuracy\": accuracy,\n        \"params\": {\"C\": C, \"kernel\": kernel}\n    }\n\n@task\ndef train_gradient_boosting(\n    data: dict,\n    n_estimators: int,\n    learning_rate: float\n) -&gt; dict:\n    \"\"\"Train gradient boosting.\"\"\"\n    model = GradientBoostingClassifier(\n        n_estimators=n_estimators,\n        learning_rate=learning_rate,\n        random_state=42\n    )\n    model.fit(data[\"X_train\"], data[\"y_train\"])\n\n    y_pred = model.predict(data[\"X_test\"])\n    accuracy = accuracy_score(data[\"y_test\"], y_pred)\n\n    return {\n        \"model_type\": \"GradientBoosting\",\n        \"model\": model,\n        \"accuracy\": accuracy,\n        \"params\": {\n            \"n_estimators\": n_estimators,\n            \"learning_rate\": learning_rate\n        }\n    }\n\n@task\ndef compare_models(results: list[dict]) -&gt; dict:\n    \"\"\"Compare all model results.\"\"\"\n    for result in sorted(results, key=lambda r: r[\"accuracy\"], reverse=True):\n        print(f\"{result['model_type']}: {result['accuracy']:.3f} - {result['params']}\")\n\n    return max(results, key=lambda r: r[\"accuracy\"])\n\n# Prepare data\ndata = evaluate(\n    load_data(path=\"train.csv\")\n    .then(split_data, target_col=\"target\")\n)\n\n# Train all models with different hyperparameters\nlr_models = train_logistic_regression.product(\n    data=[data],\n    C=[0.01, 0.1, 1.0, 10.0]\n)\n\nsvm_models = train_svm.product(\n    data=[data],\n    C=[0.1, 1.0, 10.0],\n    kernel=[\"rbf\", \"linear\"]\n)\n\ngb_models = train_gradient_boosting.product(\n    data=[data],\n    n_estimators=[50, 100],\n    learning_rate=[0.01, 0.1]\n)\n\n# Combine and find best\n@task\ndef combine_results(lr, svm, gb):\n    return lr + svm + gb\n\nbest_model = evaluate(\n    combine_results(lr=lr_models, svm=svm_models, gb=gb_models)\n    .then(compare_models)\n)\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#best-practices","level":2,"title":"Best Practices","text":"","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#1-set-random-seeds","level":3,"title":"1. Set Random Seeds","text":"<p>Ensure reproducibility:</p> <pre><code>@task\ndef train_model(data: dict, seed: int = 42):\n    np.random.seed(seed)\n    model = RandomForestClassifier(random_state=seed)\n    # ...\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#2-track-all-metrics","level":3,"title":"2. Track All Metrics","text":"<p>Don't just track accuracy:</p> <pre><code>from sklearn.metrics import precision_score, recall_score, f1_score\n\n@task\ndef evaluate_model(model, X_test, y_test) -&gt; dict:\n    y_pred = model.predict(X_test)\n\n    return {\n        \"accuracy\": accuracy_score(y_test, y_pred),\n        \"precision\": precision_score(y_test, y_pred, average=\"weighted\"),\n        \"recall\": recall_score(y_test, y_pred, average=\"weighted\"),\n        \"f1\": f1_score(y_test, y_pred, average=\"weighted\")\n    }\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#3-save-models","level":3,"title":"3. Save Models","text":"<p>Persist trained models:</p> <pre><code>import joblib\n\n@task\ndef save_model(result: dict, path: str) -&gt; str:\n    \"\"\"Save trained model to disk.\"\"\"\n    joblib.dump(result[\"model\"], path)\n    return path\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#4-use-type-hints","level":3,"title":"4. Use Type Hints","text":"<p>Clear type hints improve code quality:</p> <pre><code>from typing import Literal\n\n@task\ndef train_model(\n    data: dict,\n    model_type: Literal[\"rf\", \"gb\", \"lr\"],\n    **kwargs\n) -&gt; dict:\n    if model_type == \"rf\":\n        model = RandomForestClassifier(**kwargs)\n    elif model_type == \"gb\":\n        model = GradientBoostingClassifier(**kwargs)\n    elif model_type == \"lr\":\n        model = LogisticRegression(**kwargs)\n\n    # ...\n</code></pre>","path":["Examples","ML Workflow"],"tags":[]},{"location":"examples/ml-workflow/#see-also","level":2,"title":"See Also","text":"<ul> <li>Scikit-learn Documentation - ML algorithms</li> <li>Data Processing Example - Parallel processing patterns</li> <li>ETL Pipeline - Data preparation patterns</li> </ul>","path":["Examples","ML Workflow"],"tags":[]},{"location":"migration-focus/","level":1,"title":"Zensical Migration Focus Areas","text":"<p>This section tracks the high-level documentation workstreams to complete after moving from MkDocs to Zensical.</p>","path":["Migration focus","Zensical Migration Focus Areas"],"tags":[]},{"location":"migration-focus/#planned-workstreams","level":2,"title":"Planned workstreams","text":"<ul> <li>Information architecture</li> <li>Authoring standards</li> <li>API reference strategy</li> <li>Publishing workflow</li> </ul>","path":["Migration focus","Zensical Migration Focus Areas"],"tags":[]},{"location":"migration-focus/api-reference-strategy/","level":1,"title":"API Reference Strategy (Stub)","text":"","path":["Migration focus","API Reference Strategy (Stub)"],"tags":[]},{"location":"migration-focus/api-reference-strategy/#goal","level":2,"title":"Goal","text":"<p>Design a Zensical-compatible approach for maintaining API docs.</p>","path":["Migration focus","API Reference Strategy (Stub)"],"tags":[]},{"location":"migration-focus/api-reference-strategy/#todo","level":2,"title":"TODO","text":"<ul> <li>Choose between generated API docs and curated reference pages.</li> <li>Define ownership and update cadence.</li> <li>Add CI checks to keep API docs aligned with source changes.</li> </ul>","path":["Migration focus","API Reference Strategy (Stub)"],"tags":[]},{"location":"migration-focus/authoring-standards/","level":1,"title":"Authoring Standards (Stub)","text":"","path":["Migration focus","Authoring Standards (Stub)"],"tags":[]},{"location":"migration-focus/authoring-standards/#goal","level":2,"title":"Goal","text":"<p>Establish writing, formatting, and content conventions for consistent docs.</p>","path":["Migration focus","Authoring Standards (Stub)"],"tags":[]},{"location":"migration-focus/authoring-standards/#todo","level":2,"title":"TODO","text":"<ul> <li>Define voice/tone and page templates.</li> <li>Define code example standards (length, realism, testability).</li> <li>Define cross-linking and glossary rules.</li> </ul>","path":["Migration focus","Authoring Standards (Stub)"],"tags":[]},{"location":"migration-focus/information-architecture/","level":1,"title":"Information Architecture (Stub)","text":"","path":["Migration focus","Information Architecture (Stub)"],"tags":[]},{"location":"migration-focus/information-architecture/#goal","level":2,"title":"Goal","text":"<p>Define the top-level and secondary navigation model for Daglite docs.</p>","path":["Migration focus","Information Architecture (Stub)"],"tags":[]},{"location":"migration-focus/information-architecture/#todo","level":2,"title":"TODO","text":"<ul> <li>Confirm audience segments (new users, maintainers, plugin authors).</li> <li>Reconcile current navigation depth and naming.</li> <li>Decide what belongs in guides vs reference vs examples.</li> </ul>","path":["Migration focus","Information Architecture (Stub)"],"tags":[]},{"location":"migration-focus/publishing-workflow/","level":1,"title":"Publishing Workflow (Stub)","text":"","path":["Migration focus","Publishing Workflow (Stub)"],"tags":[]},{"location":"migration-focus/publishing-workflow/#goal","level":2,"title":"Goal","text":"<p>Standardize local preview, CI build, and deployment for Zensical.</p>","path":["Migration focus","Publishing Workflow (Stub)"],"tags":[]},{"location":"migration-focus/publishing-workflow/#todo","level":2,"title":"TODO","text":"<ul> <li>Document local preview workflow.</li> <li>Verify GitHub Pages build/deploy behavior.</li> <li>Add release checklist for docs changes.</li> </ul>","path":["Migration focus","Publishing Workflow (Stub)"],"tags":[]},{"location":"plugins/","level":1,"title":"Plugins","text":"<p>Daglite's plugin system allows you to extend functionality without adding mandatory dependencies to the core library.</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#plugin-types","level":2,"title":"Plugin Types","text":"","path":["Plugins"],"tags":[]},{"location":"plugins/#built-in-plugins","level":3,"title":"Built-in Plugins","text":"<p>Plugins shipped with the core <code>daglite</code> package that require no additional installation.</p> <p>Centralized Logging - Cross-process/thread logging that works seamlessly with threading, multiprocessing, and distributed execution.</p> <p>→ Learn more about built-in plugins</p> <p>CLI - Built-in command-line interface for running pipelines from the terminal.</p> <p>→ Learn more about the CLI</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#extension-plugins","level":3,"title":"Extension Plugins","text":"<p>Optional packages that extend Daglite with additional capabilities.</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#daglite-serialization","level":4,"title":"daglite-serialization","text":"<p>Serialization and hashing support for scientific Python libraries.</p> <ul> <li>Fast hashing for NumPy arrays (sample-based for large arrays)</li> <li>Efficient hashing for Pandas DataFrames (schema + sample)</li> <li>Image hashing with Pillow (thumbnail-based)</li> <li>Custom hash strategies</li> </ul> <p>→ Learn more about daglite-serialization</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#installation","level":2,"title":"Installation","text":"","path":["Plugins"],"tags":[]},{"location":"plugins/#built-in-plugins_1","level":3,"title":"Built-in Plugins","text":"<p>Built-in plugins are available immediately after installing <code>daglite</code>:</p> <pre><code>uv pip install daglite\n</code></pre> <p>No additional installation needed!</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#extension-plugins_1","level":3,"title":"Extension Plugins","text":"<p>Install extension plugins as needed:</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#install-all-extension-plugins","level":4,"title":"Install All Extension Plugins","text":"<pre><code>uv pip install daglite-serialization[all]\n</code></pre>","path":["Plugins"],"tags":[]},{"location":"plugins/#install-specific-extension-plugins","level":4,"title":"Install Specific Extension Plugins","text":"<pre><code># Serialization with specific libraries\nuv pip install daglite-serialization[numpy]\nuv pip install daglite-serialization[pandas]\nuv pip install daglite-serialization[pillow]\n</code></pre>","path":["Plugins"],"tags":[]},{"location":"plugins/#plugin-philosophy","level":2,"title":"Plugin Philosophy","text":"<p>Daglite's core library has minimal dependencies to ensure it runs anywhere Python runs. Plugins extend this core with optional functionality:</p> <ul> <li>No mandatory overhead - Only install what you need</li> <li>Seamless integration - Plugins work automatically once installed</li> <li>Type-safe - Full type checking support across plugins</li> <li>Modular - Mix and match plugins based on your requirements</li> </ul>","path":["Plugins"],"tags":[]},{"location":"plugins/#creating-custom-plugins","level":2,"title":"Creating Custom Plugins","text":"<p>Want to create your own Daglite plugin? See the Creating Plugins guide to learn how to extend Daglite using the pluggy-based hook system.</p>","path":["Plugins"],"tags":[]},{"location":"plugins/#future-plugins","level":2,"title":"Future Plugins","text":"<p>We're planning additional plugins for:</p> <ul> <li>Distributed execution (Dask, Ray)</li> <li>Cloud storage backends (S3, GCS, Azure)</li> <li>Workflow visualization</li> <li>Advanced caching strategies</li> </ul> <p>Have an idea for a plugin? Open a discussion or contribute!</p>","path":["Plugins"],"tags":[]},{"location":"plugins/built-in/","level":1,"title":"Built-in Plugins","text":"<p>Daglite ships with default plugins that are part of the core library. These plugins provide essential functionality without requiring additional dependencies.</p>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#centralized-logging","level":2,"title":"Centralized Logging","text":"<p>The <code>CentralizedLoggingPlugin</code> enables logging that works seamlessly across different execution backends—threading, multiprocessing, and eventually distributed execution.</p>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#why-centralized-logging","level":3,"title":"Why Centralized Logging?","text":"<p>When running tasks across multiple processes or threads, standard Python logging can be problematic:</p> <ul> <li>Multiprocessing: Logs from worker processes don't reach the main process</li> <li>Threading: Race conditions can garble log output</li> <li>Distributed: Logs scattered across machines are hard to correlate</li> </ul> <p>Daglite's centralized logging solves this by routing all logs through the event reporter system to a single coordinator process.</p>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#quick-start","level":2,"title":"Quick Start","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#basic-usage","level":3,"title":"Basic Usage","text":"<pre><code>from daglite import task, evaluate\nfrom daglite.plugins import CentralizedLoggingPlugin, get_logger\nimport logging\n\n# Configure Python logging (optional)\nlogging.basicConfig(\n    format=\"%(daglite_task_name)s [%(levelname)s] %(message)s\",\n    level=logging.INFO\n)\n\n@task\ndef process_data(x: int) -&gt; int:\n    logger = get_logger(__name__)\n    logger.info(f\"Processing {x}\")\n    result = x * 2\n    logger.info(f\"Result: {result}\")\n    return result\n\n# Add plugin to enable centralized logging\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\n\nresult = evaluate(\n    process_data.product(x=[1, 2, 3]),\n    plugins=[plugin]\n)\n# Output:\n# process_data [INFO] Processing 1\n# process_data [INFO] Result: 2\n# process_data [INFO] Processing 2\n# process_data [INFO] Result: 4\n# process_data [INFO] Processing 3\n# process_data [INFO] Result: 6\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#api-reference","level":2,"title":"API Reference","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#get_loggernamenone","level":3,"title":"<code>get_logger(name=None)</code>","text":"<p>Get a logger instance that works across process/thread/machine boundaries.</p> <p>Parameters: - <code>name</code> (str, optional) - Logger name for code organization. If None, uses <code>\"daglite.tasks\"</code>. Typically use <code>__name__</code> for module-based naming.</p> <p>Returns: - <code>logging.LoggerAdapter</code> - Logger with automatic task context injection</p> <p>Features: - Automatically injects task context into all log records:   - <code>daglite_task_name</code> - Name of the task being executed   - <code>daglite_task_id</code> - Unique ID for the task instance   - <code>daglite_task_key</code> - Node key in the DAG - Routes logs through the reporter system when available - Falls back to standard Python logging for Inline execution</p> <p>Example:</p> <pre><code>@task\ndef my_task(x: int):\n    logger = get_logger(__name__)  # Use module name\n    logger.info(f\"Starting with {x}\")\n\n    try:\n        result = expensive_computation(x)\n        logger.info(f\"Completed successfully: {result}\")\n        return result\n    except Exception as e:\n        logger.error(f\"Failed: {e}\", exc_info=True)\n        raise\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#centralizedloggingpluginlevelloggingwarning","level":3,"title":"<code>CentralizedLoggingPlugin(level=logging.WARNING)</code>","text":"<p>Plugin that centralizes logs from all workers to the coordinator.</p> <p>Parameters: - <code>level</code> (int) - Minimum log level to handle on coordinator side. Default is <code>logging.WARNING</code>.</p> <p>How It Works:</p> <ol> <li>Worker Side: The <code>get_logger()</code> function automatically adds a handler that sends log records via the event reporter</li> <li>Coordinator Side: The plugin receives log events and emits them through Python's logging system</li> <li>Task Context: Task name, ID, and node key are automatically included in every log record</li> </ol> <p>Example:</p> <pre><code>import logging\nfrom daglite import task, evaluate\nfrom daglite.plugins import CentralizedLoggingPlugin, get_logger\n\n# Configure logging format with task context\nlogging.basicConfig(\n    format=\"[%(levelname)s] %(daglite_task_name)s: %(message)s\",\n    level=logging.DEBUG\n)\n\n@task(backend_name=\"multiprocessing\")\ndef cpu_intensive_task(n: int) -&gt; int:\n    logger = get_logger(__name__)\n    logger.debug(f\"Starting computation for {n}\")\n\n    result = sum(i ** 2 for i in range(n))\n\n    logger.info(f\"Completed: {result}\")\n    return result\n\n# Use plugin with INFO level\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\n\nresult = evaluate(\n    cpu_intensive_task.product(n=[1000, 2000, 3000]),\n    plugins=[plugin],\n    use_async=True  # Runs in multiprocessing\n)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#advanced-usage","level":2,"title":"Advanced Usage","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#custom-log-formatting","level":3,"title":"Custom Log Formatting","text":"<p>Use standard Python logging configuration with task context fields:</p> <pre><code>import logging\n\n# Configure with task context in format string\nlogging.basicConfig(\n    format=\"%(asctime)s | %(daglite_task_name)s | %(levelname)s | %(message)s\",\n    datefmt=\"%Y-%m-%d %H:%M:%S\",\n    level=logging.INFO\n)\n\n@task\ndef process(item: dict):\n    logger = get_logger(__name__)\n    logger.info(f\"Processing item {item['id']}\")\n    # Output: 2025-01-15 10:30:45 | process | INFO | Processing item 123\n    return item\n</code></pre> <p>Available task context fields: - <code>%(daglite_task_name)s</code> - Task function name - <code>%(daglite_task_id)s</code> - Unique task instance ID (UUID) - <code>%(daglite_task_key)s</code> - Node key in the execution graph</p>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#file-logging","level":3,"title":"File Logging","text":"<p>Write logs to a file:</p> <pre><code>import logging\n\n# Configure file handler\nlogging.basicConfig(\n    filename=\"pipeline.log\",\n    format=\"%(asctime)s [%(levelname)s] %(daglite_task_name)s - %(message)s\",\n    level=logging.INFO\n)\n\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\n\nresult = evaluate(my_dag, plugins=[plugin])\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#module-based-logger-names","level":3,"title":"Module-Based Logger Names","text":"<p>Organize logs by module:</p> <pre><code># myproject/tasks/etl.py\nfrom daglite import task\nfrom daglite.plugins import get_logger\n\n@task\ndef extract(source: str):\n    logger = get_logger(__name__)  # Uses \"myproject.tasks.etl\"\n    logger.info(f\"Extracting from {source}\")\n    return data\n\n# myproject/tasks/ml.py\nfrom daglite import task\nfrom daglite.plugins import get_logger\n\n@task\ndef train_model(data):\n    logger = get_logger(__name__)  # Uses \"myproject.tasks.ml\"\n    logger.info(\"Training model\")\n    return model\n</code></pre> <p>Then filter logs by module:</p> <pre><code># Only log from ML tasks\nlogging.getLogger(\"myproject.tasks.ml\").setLevel(logging.DEBUG)\nlogging.getLogger(\"myproject.tasks.etl\").setLevel(logging.WARNING)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#structured-logging","level":3,"title":"Structured Logging","text":"<p>Add custom fields to log records:</p> <pre><code>@task\ndef process_batch(batch_id: int, items: list):\n    logger = get_logger(__name__)\n\n    # Add custom fields via 'extra'\n    logger.info(\n        f\"Processing batch {batch_id}\",\n        extra={\n            \"batch_id\": batch_id,\n            \"item_count\": len(items),\n            \"batch_size\": len(items)\n        }\n    )\n\n    return results\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#different-log-levels","level":3,"title":"Different Log Levels","text":"<p>Control verbosity per execution:</p> <pre><code># Development - verbose logging\ndev_plugin = CentralizedLoggingPlugin(level=logging.DEBUG)\nevaluate(my_dag, plugins=[dev_plugin])\n\n# Production - only warnings and errors\nprod_plugin = CentralizedLoggingPlugin(level=logging.WARNING)\nevaluate(my_dag, plugins=[prod_plugin])\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#logging-exceptions","level":3,"title":"Logging Exceptions","text":"<p>Log exceptions with full tracebacks:</p> <pre><code>@task\ndef risky_operation(data):\n    logger = get_logger(__name__)\n\n    try:\n        result = process(data)\n        logger.info(\"Success\")\n        return result\n    except ValueError as e:\n        logger.error(f\"Validation failed: {e}\", exc_info=True)\n        raise\n    except Exception as e:\n        logger.critical(f\"Unexpected error: {e}\", exc_info=True)\n        raise\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#performance-considerations","level":2,"title":"Performance Considerations","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#log-level-filtering","level":3,"title":"Log Level Filtering","text":"<p>Filtering happens on the coordinator side to minimize data transfer:</p> <pre><code># Workers send all log records\n# Coordinator filters based on plugin level\nplugin = CentralizedLoggingPlugin(level=logging.WARNING)\n\n@task\ndef task_with_debug_logs():\n    logger = get_logger()\n    logger.debug(\"This is sent to coordinator\")  # But filtered out\n    logger.warning(\"This appears in output\")     # This is shown\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#overhead","level":3,"title":"Overhead","text":"<ul> <li>Inline execution: Negligible overhead (standard Python logging)</li> <li>Threading: Minimal overhead from thread-safe queue</li> <li>Multiprocessing: Small serialization overhead per log record</li> <li>Distributed: Network latency for log events</li> </ul> <p>For high-throughput scenarios, consider: - Using higher log levels (WARNING instead of INFO) - Logging less frequently - Batching log messages</p>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#working-with-async-execution","level":2,"title":"Working with Async Execution","text":"<p>The plugin works seamlessly with async execution:</p> <pre><code>@task(backend_name=\"threading\")\ndef io_task(url: str):\n    logger = get_logger(__name__)\n    logger.info(f\"Fetching {url}\")\n    data = requests.get(url).json()\n    logger.info(f\"Received {len(data)} items\")\n    return data\n\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\n\n# Logs from all threads appear in order\nresult = evaluate(\n    io_task.product(url=urls),\n    plugins=[plugin],\n    use_async=True\n)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#integration-with-external-loggers","level":2,"title":"Integration with External Loggers","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#using-with-third-party-logging","level":3,"title":"Using with Third-Party Logging","text":"<p>The plugin integrates with standard Python logging, so it works with:</p> <ul> <li>Loguru: Configure Loguru to intercept standard logging</li> <li>structlog: Use structlog processors with the standard logging</li> <li>Cloud Logging: Send to Google Cloud Logging, AWS CloudWatch, etc.</li> </ul> <p>Example with Cloud Logging:</p> <pre><code>import logging\nimport google.cloud.logging\n\n# Set up Google Cloud Logging\nclient = google.cloud.logging.Client()\nclient.setup_logging()\n\n# Daglite logs will be sent to Cloud Logging\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\nevaluate(my_dag, plugins=[plugin])\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#json-logging","level":3,"title":"JSON Logging","text":"<p>Use <code>python-json-logger</code> for structured JSON logs:</p> <pre><code>from pythonjsonlogger import jsonlogger\nimport logging\n\n# Configure JSON formatter\nhandler = logging.StreamHandler()\nformatter = jsonlogger.JsonFormatter(\n    \"%(asctime)s %(name)s %(levelname)s %(daglite_task_name)s %(message)s\"\n)\nhandler.setFormatter(formatter)\n\nlogger = logging.getLogger()\nlogger.addHandler(handler)\nlogger.setLevel(logging.INFO)\n\n# Logs will be JSON formatted\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\nevaluate(my_dag, plugins=[plugin])\n# Output: {\"asctime\": \"2025-01-15 10:30:45\", \"name\": \"daglite.tasks\",\n#          \"levelname\": \"INFO\", \"daglite_task_name\": \"process\", \"message\": \"Processing\"}\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#best-practices","level":2,"title":"Best Practices","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#1-use-module-names","level":3,"title":"1. Use Module Names","text":"<pre><code># Good - organized by module\nlogger = get_logger(__name__)\n\n# Less useful - all logs from same logger\nlogger = get_logger()\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#2-log-at-appropriate-levels","level":3,"title":"2. Log at Appropriate Levels","text":"<pre><code>@task\ndef process(item):\n    logger = get_logger(__name__)\n\n    logger.debug(f\"Raw item: {item}\")           # Detailed debugging\n    logger.info(f\"Processing {item['id']}\")     # Progress information\n    logger.warning(\"Using fallback method\")     # Potential issues\n    logger.error(\"Failed to process\")           # Errors\n    logger.critical(\"System unstable\")          # Critical failures\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#3-include-context-in-messages","level":3,"title":"3. Include Context in Messages","text":"<pre><code># Good - provides context\nlogger.info(f\"Processed {len(items)} items in {duration:.2f}s\")\n\n# Less helpful\nlogger.info(\"Done\")\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#4-dont-log-sensitive-data","level":3,"title":"4. Don't Log Sensitive Data","text":"<pre><code># Bad - logs credentials\nlogger.info(f\"Connecting to {username}:{password}@host\")\n\n# Good - masks sensitive info\nlogger.info(f\"Connecting to {username}:***@host\")\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#5-configure-logging-once","level":3,"title":"5. Configure Logging Once","text":"<pre><code># At application startup\nimport logging\n\nlogging.basicConfig(\n    format=\"%(asctime)s [%(levelname)s] %(daglite_task_name)s - %(message)s\",\n    level=logging.INFO\n)\n\n# Then use throughout application\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#troubleshooting","level":2,"title":"Troubleshooting","text":"","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#logs-not-appearing","level":3,"title":"Logs Not Appearing","text":"<p>Problem: No logs appear when using async execution.</p> <p>Solution: Make sure you're using the plugin:</p> <pre><code># Without plugin - logs may not appear in async mode\nevaluate(my_dag, use_async=True)\n\n# With plugin - logs work correctly\nplugin = CentralizedLoggingPlugin(level=logging.INFO)\nevaluate(my_dag, plugins=[plugin], use_async=True)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#duplicate-logs","level":3,"title":"Duplicate Logs","text":"<p>Problem: Each log message appears multiple times.</p> <p>Solution: Check for multiple logging configurations or handlers:</p> <pre><code># Bad - adds handler every time\nlogging.basicConfig(...)  # Called multiple times\n\n# Good - configure once at startup\nif not logging.getLogger().handlers:\n    logging.basicConfig(...)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#missing-task-context","level":3,"title":"Missing Task Context","text":"<p>Problem: Task name doesn't appear in logs.</p> <p>Solution: Use the format fields correctly:</p> <pre><code># Include task context fields in format\nlogging.basicConfig(\n    format=\"%(daglite_task_name)s [%(levelname)s] %(message)s\",\n    level=logging.INFO\n)\n</code></pre>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/built-in/#see-also","level":2,"title":"See Also","text":"<ul> <li>Creating Plugins - Build custom plugins</li> <li>Python Logging Documentation - Standard library logging</li> <li>Settings - Configure plugin tracing with <code>enable_plugin_tracing</code></li> </ul>","path":["Plugins","Built-in Plugins"],"tags":[]},{"location":"plugins/creating/","level":1,"title":"Creating Custom Plugins","text":"<p>Daglite uses pluggy for its plugin system, allowing you to extend functionality through hooks. This guide shows you how to create custom plugins that integrate with Daglite's execution lifecycle.</p>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#plugin-system-overview","level":2,"title":"Plugin System Overview","text":"<p>Daglite's plugin system is based on hook specifications that define extension points in the execution lifecycle:</p> <ul> <li>Node-level hooks - Called before/after individual task execution</li> <li>Graph-level hooks - Called before/after entire DAG execution</li> <li>Event system - Bidirectional communication between plugins and Daglite</li> </ul>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#quick-start","level":2,"title":"Quick Start","text":"","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#simple-plugin","level":3,"title":"Simple Plugin","text":"<p>Create a plugin that logs task execution:</p> <pre><code>from daglite.plugins.hooks import hook_impl\nfrom daglite.graph.base import GraphMetadata\nfrom typing import Any\n\nclass LoggingPlugin:\n    \"\"\"Plugin that logs task execution events.\"\"\"\n\n    @hook_impl\n    def before_node_execute(\n        self,\n        key: str,\n        metadata: GraphMetadata,\n        inputs: dict[str, Any],\n        reporter=None,\n    ) -&gt; None:\n        print(f\"Starting task: {metadata.name}\")\n\n    @hook_impl\n    def after_node_execute(\n        self,\n        key: str,\n        metadata: GraphMetadata,\n        inputs: dict[str, Any],\n        result: Any,\n        duration: float,\n        reporter=None,\n    ) -&gt; None:\n        print(f\"Completed task: {metadata.name} in {duration:.2f}s\")\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#register-and-use","level":3,"title":"Register and Use","text":"<pre><code>from daglite import task, evaluate\nfrom daglite.plugins.manager import register_plugins\n\n# Register plugin globally\nregister_plugins(LoggingPlugin())\n\n@task\ndef add(x: int, y: int) -&gt; int:\n    return x + y\n\n# Plugin hooks are called during evaluation\nresult = evaluate(add(x=1, y=2))\n# Output:\n# Starting task: add\n# Completed task: add in 0.00s\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#available-hooks","level":2,"title":"Available Hooks","text":"","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#node-level-hooks","level":3,"title":"Node-Level Hooks","text":"<p>Called for each task execution:</p>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#before_node_execute","level":4,"title":"<code>before_node_execute</code>","text":"<p>Called before a task begins execution.</p> <pre><code>@hook_impl\ndef before_node_execute(\n    self,\n    key: str,              # Unique key for this node\n    metadata: GraphMetadata,  # Task metadata (name, description)\n    inputs: dict[str, Any],   # Resolved input parameters\n    reporter=None,            # Event reporter (optional)\n) -&gt; None:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#after_node_execute","level":4,"title":"<code>after_node_execute</code>","text":"<p>Called after a task completes successfully.</p> <pre><code>@hook_impl\ndef after_node_execute(\n    self,\n    key: str,\n    metadata: GraphMetadata,\n    inputs: dict[str, Any],\n    result: Any,           # Task result\n    duration: float,       # Execution time in seconds\n    reporter=None,\n) -&gt; None:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#on_node_error","level":4,"title":"<code>on_node_error</code>","text":"<p>Called when a task execution fails.</p> <pre><code>@hook_impl\ndef on_node_error(\n    self,\n    key: str,\n    metadata: GraphMetadata,\n    inputs: dict[str, Any],\n    error: Exception,      # The exception that was raised\n    duration: float,\n    reporter=None,\n) -&gt; None:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#graph-level-hooks","level":3,"title":"Graph-Level Hooks","text":"<p>Called for entire DAG execution:</p>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#before_graph_execute","level":4,"title":"<code>before_graph_execute</code>","text":"<pre><code>from uuid import UUID\n\n@hook_impl\ndef before_graph_execute(\n    self,\n    root_id: UUID,         # UUID of root node\n    node_count: int,       # Total nodes in graph\n) -&gt; None:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#after_graph_execute","level":4,"title":"<code>after_graph_execute</code>","text":"<pre><code>@hook_impl\ndef after_graph_execute(\n    self,\n    root_id: UUID,\n    result: Any,           # Final result\n    duration: float,       # Total execution time\n) -&gt; None:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#on_graph_error","level":4,"title":"<code>on_graph_error</code>","text":"<pre><code>@hook_impl\ndef on_graph_error(\n    self,\n    root_id: UUID,\n    error: Exception,\n    duration: float,\n) -&gt; None:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#example-plugins","level":2,"title":"Example Plugins","text":"","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#timing-plugin","level":3,"title":"Timing Plugin","text":"<p>Track execution time for performance analysis:</p> <pre><code>from daglite.plugins.hooks import hook_impl\nimport time\n\nclass TimingPlugin:\n    \"\"\"Track execution times for all tasks.\"\"\"\n\n    def __init__(self):\n        self.timings = {}\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        self.timings[metadata.name] = duration\n\n    @hook_impl\n    def after_graph_execute(self, root_id, result, duration):\n        print(\"\\nExecution Times:\")\n        for task_name, task_duration in sorted(\n            self.timings.items(),\n            key=lambda x: x[1],\n            reverse=True\n        ):\n            print(f\"  {task_name}: {task_duration:.4f}s\")\n        print(f\"\\nTotal: {duration:.4f}s\")\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#progress-plugin","level":3,"title":"Progress Plugin","text":"<p>Show progress for long-running workflows:</p> <pre><code>from daglite.plugins.hooks import hook_impl\n\nclass ProgressPlugin:\n    \"\"\"Display execution progress.\"\"\"\n\n    def __init__(self):\n        self.total = 0\n        self.completed = 0\n\n    @hook_impl\n    def before_graph_execute(self, root_id, node_count):\n        self.total = node_count\n        self.completed = 0\n        print(f\"Starting execution: 0/{self.total} tasks\")\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        self.completed += 1\n        pct = (self.completed / self.total) * 100\n        print(f\"Progress: {self.completed}/{self.total} ({pct:.0f}%)\")\n\n    @hook_impl\n    def after_graph_execute(self, root_id, result, duration):\n        print(f\"Completed: {self.total}/{self.total} tasks in {duration:.2f}s\")\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#error-tracking-plugin","level":3,"title":"Error Tracking Plugin","text":"<p>Collect errors for debugging:</p> <pre><code>from daglite.plugins.hooks import hook_impl\n\nclass ErrorTrackingPlugin:\n    \"\"\"Track all errors during execution.\"\"\"\n\n    def __init__(self):\n        self.errors = []\n\n    @hook_impl\n    def on_node_error(self, key, metadata, inputs, error, duration, reporter=None):\n        self.errors.append({\n            'task': metadata.name,\n            'error': str(error),\n            'error_type': type(error).__name__,\n            'inputs': inputs,\n            'duration': duration\n        })\n\n    @hook_impl\n    def on_graph_error(self, root_id, error, duration, is_async):\n        print(f\"\\nExecution failed after {duration:.2f}s\")\n        print(f\"Total errors: {len(self.errors)}\")\n        for err in self.errors:\n            print(f\"  - {err['task']}: {err['error_type']}: {err['error']}\")\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#caching-plugin","level":3,"title":"Caching Plugin","text":"<p>Simple result caching:</p> <pre><code>from daglite.plugins.hooks import hook_impl\nimport hashlib\nimport json\n\nclass CachingPlugin:\n    \"\"\"Cache task results based on inputs.\"\"\"\n\n    def __init__(self):\n        self.cache = {}\n\n    def _hash_inputs(self, inputs):\n        \"\"\"Create hash from inputs.\"\"\"\n        return hashlib.sha256(\n            json.dumps(inputs, sort_keys=True).encode()\n        ).hexdigest()\n\n    @hook_impl\n    def before_node_execute(self, key, metadata, inputs, reporter=None):\n        cache_key = (metadata.name, self._hash_inputs(inputs))\n        if cache_key in self.cache:\n            print(f\"Cache hit for {metadata.name}\")\n            # Note: Currently can't skip execution, just track hits\n            self.cache_hits = self.cache.get('_hits', 0) + 1\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        cache_key = (metadata.name, self._hash_inputs(inputs))\n        self.cache[cache_key] = result\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#plugin-registration","level":2,"title":"Plugin Registration","text":"","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#global-registration","level":3,"title":"Global Registration","text":"<p>Register plugins that apply to all evaluations:</p> <pre><code>from daglite.plugins.manager import register_plugins\n\n# Register multiple plugins at once\nregister_plugins(\n    TimingPlugin(),\n    ProgressPlugin(),\n    ErrorTrackingPlugin()\n)\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#per-execution-registration","level":3,"title":"Per-Execution Registration","text":"<p>Register plugins for specific evaluations only:</p> <pre><code>from daglite import evaluate\n\ntiming = TimingPlugin()\nresult = evaluate(\n    my_dag,\n    plugins=[timing]  # Only active for this evaluation\n)\n\nprint(timing.timings)\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#entry-point-registration","level":3,"title":"Entry Point Registration","text":"<p>For distributable plugins, use setuptools entry points:</p> <pre><code># setup.py or pyproject.toml\nentry_points={\n    'daglite.hooks': [\n        'myplugin = mypackage.plugin:MyPlugin'\n    ]\n}\n</code></pre> <p>Plugins registered via entry points are automatically loaded.</p>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#best-practices","level":2,"title":"Best Practices","text":"","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#1-use-descriptive-names","level":3,"title":"1. Use Descriptive Names","text":"<pre><code># Good\nclass PostgresResultWriterPlugin:\n    \"\"\"Write task results to PostgreSQL database.\"\"\"\n    ...\n\n# Less clear\nclass DBPlugin:\n    ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#2-handle-errors-gracefully","level":3,"title":"2. Handle Errors Gracefully","text":"<p>Plugins shouldn't crash the DAG execution:</p> <pre><code>@hook_impl\ndef after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n    try:\n        self.save_to_database(metadata.name, result)\n    except Exception as e:\n        # Log but don't crash\n        logger.warning(f\"Failed to save result: {e}\")\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#3-make-plugins-configurable","level":3,"title":"3. Make Plugins Configurable","text":"<pre><code>class LoggingPlugin:\n    def __init__(self, verbose: bool = False, log_file: str | None = None):\n        self.verbose = verbose\n        self.log_file = log_file\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        message = f\"{metadata.name} completed in {duration:.2f}s\"\n        if self.verbose:\n            message += f\" with inputs: {inputs}\"\n\n        if self.log_file:\n            with open(self.log_file, 'a') as f:\n                f.write(message + '\\n')\n        else:\n            print(message)\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#4-document-your-hooks","level":3,"title":"4. Document Your Hooks","text":"<pre><code>class MetricsPlugin:\n    \"\"\"\n    Collect execution metrics and export to monitoring system.\n\n    This plugin tracks:\n    - Task execution times\n    - Task success/failure rates\n    - Total graph execution time\n\n    Metrics are exported via Prometheus client.\n    \"\"\"\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        \"\"\"Record task execution time metric.\"\"\"\n        ...\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#5-test-your-plugins","level":3,"title":"5. Test Your Plugins","text":"<pre><code>from daglite import task, evaluate\n\ndef test_timing_plugin():\n    plugin = TimingPlugin()\n\n    @task\n    def slow_task(x: int) -&gt; int:\n        import time\n        time.sleep(0.1)\n        return x * 2\n\n    result = evaluate(slow_task(x=5), plugins=[plugin])\n\n    assert 'slow_task' in plugin.timings\n    assert plugin.timings['slow_task'] &gt;= 0.1\n    assert result == 10\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#advanced-topics","level":2,"title":"Advanced Topics","text":"","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#stateful-plugins","level":3,"title":"Stateful Plugins","text":"<p>Maintain state across executions:</p> <pre><code>class StatefulPlugin:\n    def __init__(self):\n        self.execution_count = 0\n        self.total_duration = 0\n\n    @hook_impl\n    def after_graph_execute(self, root_id, result, duration, is_async):\n        self.execution_count += 1\n        self.total_duration += duration\n\n    def get_average_duration(self):\n        if self.execution_count == 0:\n            return 0\n        return self.total_duration / self.execution_count\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#plugin-dependencies","level":3,"title":"Plugin Dependencies","text":"<p>One plugin can depend on another:</p> <pre><code>class MetricsCollector:\n    def __init__(self):\n        self.metrics = []\n\n    @hook_impl\n    def after_node_execute(self, key, metadata, inputs, result, duration, reporter=None):\n        self.metrics.append({\n            'task': metadata.name,\n            'duration': duration\n        })\n\nclass MetricsExporter:\n    def __init__(self, collector: MetricsCollector):\n        self.collector = collector\n\n    @hook_impl\n    def after_graph_execute(self, root_id, result, duration, is_async):\n        # Export metrics collected by MetricsCollector\n        self.export_to_prometheus(self.collector.metrics)\n</code></pre>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/creating/#see-also","level":2,"title":"See Also","text":"<ul> <li>pluggy documentation - Plugin framework used by Daglite</li> <li>Hook Specifications Source - Complete hook specifications</li> <li>Serialization Plugin Source - Example plugin implementation</li> </ul>","path":["Plugins","Creating Custom Plugins"],"tags":[]},{"location":"plugins/serialization/","level":1,"title":"Serialization Plugin","text":"<p>The <code>daglite-serialization</code> plugin provides fast, efficient hashing and serialization support for popular scientific Python libraries. This is essential for caching workflows that use NumPy arrays, Pandas DataFrames, or Pillow images.</p>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#installation","level":2,"title":"Installation","text":"<pre><code># Install with all supported libraries\nuv pip install daglite-serialization[all]\n\n# Or install specific libraries\nuv pip install daglite-serialization[numpy]\nuv pip install daglite-serialization[pandas]\nuv pip install daglite-serialization[pillow]\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#quick-start","level":2,"title":"Quick Start","text":"","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#automatic-registration","level":3,"title":"Automatic Registration","text":"<p>Register all available handlers at once:</p> <pre><code>from daglite import task\nfrom daglite_serialization import register_all\nimport numpy as np\n\n# Register all plugins (numpy, pandas, pillow)\nregister_all()\n\n@task(cache=True)\ndef process_array(arr: np.ndarray) -&gt; np.ndarray:\n    \"\"\"Cache key automatically includes fast hash of array.\"\"\"\n    return arr * 2\n\n# Subsequent calls with same array use cached result\nresult = process_array(np.random.rand(10000, 10000)).run()\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#selective-registration","level":3,"title":"Selective Registration","text":"<p>Register only the handlers you need:</p> <pre><code>from daglite_serialization.numpy import register_handlers as register_numpy\nfrom daglite_serialization.pandas import register_handlers as register_pandas\n\nregister_numpy()\nregister_pandas()\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#supported-libraries","level":2,"title":"Supported Libraries","text":"","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#numpy","level":3,"title":"NumPy","text":"<p>Fast hashing for NumPy arrays using sampling strategies.</p> <p>Hash Strategies:</p> <ul> <li>Small arrays (&lt;10k elements): Full hash for complete accuracy</li> <li>Large arrays (≥10k elements): Sample-based hash (~0.2% of data)</li> <li>Samples from beginning, middle, and end</li> <li>Includes shape and dtype metadata</li> <li>Performance: &lt;100ms for 800MB arrays (vs ~2000ms for full hash)</li> </ul> <p>Example:</p> <pre><code>import numpy as np\nfrom daglite import task\nfrom daglite_serialization import register_all\n\nregister_all()\n\n@task(cache=True)\ndef expensive_computation(matrix: np.ndarray) -&gt; float:\n    \"\"\"Cached computation on large arrays.\"\"\"\n    return np.linalg.det(matrix)\n\n# First call computes and caches\nresult1 = expensive_computation(np.random.rand(1000, 1000)).run()\n\n# Second call with same array uses cache\nresult2 = expensive_computation(np.random.rand(1000, 1000)).run()\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#pandas","level":3,"title":"Pandas","text":"<p>Efficient hashing for DataFrames and Series using schema and sampling.</p> <p>Hash Strategies:</p> <ul> <li>Small DataFrames (&lt;1k rows): Full hash using <code>pd.util.hash_pandas_object</code></li> <li>Large DataFrames (≥1k rows): Hash schema + first/last 500 rows</li> <li>Includes column names, dtypes, and index</li> <li>Performance: ~10ms for 1M rows (vs ~5000ms for full hash)</li> </ul> <p>Example:</p> <pre><code>import pandas as pd\nfrom daglite import task\nfrom daglite_serialization import register_all\n\nregister_all()\n\n@task(cache=True)\ndef aggregate_data(df: pd.DataFrame, group_by: str) -&gt; pd.DataFrame:\n    \"\"\"Cached aggregation operation.\"\"\"\n    return df.groupby(group_by).agg({\n        'value': ['mean', 'std', 'count']\n    })\n\n# Works with large DataFrames\ndf = pd.DataFrame({\n    'category': np.random.choice(['A', 'B', 'C'], 1000000),\n    'value': np.random.randn(1000000)\n})\n\nresult = aggregate_data(df, group_by='category').run()\n</code></pre> <p>Supported Types:</p> <ul> <li><code>pd.DataFrame</code></li> <li><code>pd.Series</code></li> <li><code>pd.Index</code></li> </ul>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#pillow-pil","level":3,"title":"Pillow (PIL)","text":"<p>Image hashing using thumbnail downsampling.</p> <p>Hash Strategy:</p> <ul> <li>Downsample to 32×32 thumbnail</li> <li>Include image metadata (mode, size)</li> <li>Fast for any image size</li> <li>Catches all visually significant changes</li> </ul> <p>Example:</p> <pre><code>from PIL import Image\nfrom daglite import task\nfrom daglite_serialization import register_all\n\nregister_all()\n\n@task(cache=True)\ndef apply_filter(img: Image.Image, blur_radius: int) -&gt; Image.Image:\n    \"\"\"Cached image filtering.\"\"\"\n    from PIL import ImageFilter\n    return img.filter(ImageFilter.GaussianBlur(radius=blur_radius))\n\nimg = Image.open('photo.jpg')\nfiltered = apply_filter(img, blur_radius=5).run()\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#custom-hash-strategies","level":2,"title":"Custom Hash Strategies","text":"<p>Override default strategies for specific types:</p> <pre><code>from daglite.serialization import default_registry\nimport hashlib\nimport numpy as np\n\ndef hash_numpy_array_full(arr: np.ndarray) -&gt; str:\n    \"\"\"Full hash instead of sampling (slower but exact).\"\"\"\n    return hashlib.sha256(arr.tobytes()).hexdigest()\n\n# Register custom strategy\ndefault_registry.register_hash_strategy(\n    np.ndarray,\n    hash_numpy_array_full,\n    \"Full hash of numpy array\"\n)\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#custom-type-registration","level":3,"title":"Custom Type Registration","text":"<p>Register hash strategies for your own types:</p> <pre><code>from dataclasses import dataclass\nimport hashlib\n\n@dataclass\nclass CustomData:\n    matrix: np.ndarray\n    metadata: dict\n\ndef hash_custom_data(data: CustomData) -&gt; str:\n    \"\"\"Hash custom data type.\"\"\"\n    # Combine hashes from components\n    matrix_hash = default_registry.hash(data.matrix)\n    metadata_hash = hashlib.sha256(\n        str(sorted(data.metadata.items())).encode()\n    ).hexdigest()\n    return hashlib.sha256(\n        f\"{matrix_hash}:{metadata_hash}\".encode()\n    ).hexdigest()\n\ndefault_registry.register_hash_strategy(\n    CustomData,\n    hash_custom_data,\n    \"Hash CustomData by components\"\n)\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#performance-characteristics","level":2,"title":"Performance Characteristics","text":"","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#numpy-arrays","level":3,"title":"NumPy Arrays","text":"Array Size Elements Full Hash Time Sample Hash Time Speedup 10 MB 1M ~20ms ~5ms 4× 100 MB 10M ~200ms ~8ms 25× 800 MB 100M ~2000ms ~100ms 20×","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#pandas-dataframes","level":3,"title":"Pandas DataFrames","text":"Rows Columns Full Hash Time Sample Hash Time Speedup 1K 10 ~5ms ~5ms 1× 100K 10 ~500ms ~8ms 62× 1M 10 ~5000ms ~10ms 500×","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#pillow-images","level":3,"title":"Pillow Images","text":"Image Size Pixels Hash Time 640×480 307K ~5ms 1920×1080 2M ~10ms 4096×4096 16M ~15ms <p>All benchmarks on modern CPU. Actual performance varies by hardware.</p>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#when-to-use-sampling","level":2,"title":"When to Use Sampling","text":"","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#safe-to-use-sampling","level":3,"title":"Safe to Use Sampling:","text":"<ul> <li>Large datasets where small changes are unlikely</li> <li>Intermediate results in data pipelines</li> <li>Arrays/DataFrames that rarely change</li> <li>Development and testing workflows</li> </ul>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#use-full-hashing-when","level":3,"title":"Use Full Hashing When:","text":"<ul> <li>Exact cache invalidation is critical</li> <li>Working with adversarial data</li> <li>Financial or scientific precision required</li> <li>Small datasets where performance doesn't matter</li> </ul> <p>Trade-off: Sampling provides 10-500× speedup but has a small risk of hash collisions if only sampled regions differ.</p>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#best-practices","level":2,"title":"Best Practices","text":"","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#1-register-early","level":3,"title":"1. Register Early","text":"<p>Register handlers at the start of your script or in a setup module:</p> <pre><code># myproject/setup.py\nfrom daglite_serialization import register_all\n\nregister_all()\n</code></pre> <pre><code># myproject/pipeline.py\nfrom myproject.setup import *  # Handlers registered\nfrom daglite import task\n\n@task(cache=True)\ndef process(data):\n    ...\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#2-use-type-annotations","level":3,"title":"2. Use Type Annotations","text":"<p>Help type checkers understand your code:</p> <pre><code>import numpy as np\nfrom numpy.typing import NDArray\n\n@task(cache=True)\ndef process(arr: NDArray[np.float64]) -&gt; NDArray[np.float64]:\n    return arr * 2\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#3-document-hash-strategies","level":3,"title":"3. Document Hash Strategies","text":"<p>When using custom strategies, document your choice:</p> <pre><code># Using full hash for financial data - exact cache invalidation required\ndefault_registry.register_hash_strategy(\n    FinancialData,\n    hash_financial_data_full,\n    \"Full hash for financial accuracy\"\n)\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#4-test-cache-behavior","level":3,"title":"4. Test Cache Behavior","text":"<p>Verify caching works as expected:</p> <pre><code>import numpy as np\nfrom daglite import task\nfrom daglite_serialization import register_all\n\nregister_all()\n\n@task(cache=True)\ndef slow_operation(arr: np.ndarray) -&gt; float:\n    import time\n    time.sleep(1)  # Simulate slow operation\n    return arr.sum()\n\n# First call takes ~1 second\narr = np.ones(1000)\nresult1 = slow_operation(arr).run()\n\n# Second call with same array returns immediately\nresult2 = slow_operation(arr).run()\nassert result1 == result2\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#troubleshooting","level":2,"title":"Troubleshooting","text":"","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#no-hash-strategy-registered-error","level":3,"title":"\"No hash strategy registered\" Error","text":"<p>Register the appropriate handler:</p> <pre><code>from daglite_serialization.numpy import register_handlers\n\nregister_handlers()  # Now np.ndarray hashing works\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#hash-collisions","level":3,"title":"Hash Collisions","text":"<p>If you suspect hash collisions with sampling:</p> <pre><code># Switch to full hashing for specific type\nfrom daglite.serialization import default_registry\nimport hashlib\nimport numpy as np\n\ndef full_hash(arr: np.ndarray) -&gt; str:\n    return hashlib.sha256(arr.tobytes()).hexdigest()\n\ndefault_registry.register_hash_strategy(np.ndarray, full_hash, \"Full hash\")\n</code></pre>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#performance-issues","level":3,"title":"Performance Issues","text":"<p>If hashing is slow:</p> <ol> <li>Check array/DataFrame sizes</li> <li>Consider if caching is beneficial</li> <li>Profile to confirm hashing is the bottleneck</li> <li>Adjust sampling strategy if needed</li> </ol>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"plugins/serialization/#see-also","level":2,"title":"See Also","text":"<ul> <li>Tasks User Guide - Learn about task caching</li> <li>Creating Plugins - Create custom serialization plugins</li> <li>Serialization Source Code - Implementation details</li> </ul>","path":["Plugins","Serialization Plugin"],"tags":[]},{"location":"user-guide/cli/","level":1,"title":"Command-Line Interface","text":"<p>The <code>daglite</code> CLI is built in — no extra install needed. It lets you run pipelines defined with the <code>@pipeline</code> decorator from the terminal, CI/CD systems, or scheduled jobs.</p>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#basic-usage","level":2,"title":"Basic Usage","text":"","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#defining-a-pipeline","level":3,"title":"Defining a Pipeline","text":"<p>Use the <code>@pipeline</code> decorator to create a runnable pipeline:</p> <pre><code># myproject/pipelines.py\nfrom daglite import task, pipeline\n\n@task\ndef load_data(path: str) -&gt; list:\n    with open(path) as f:\n        return [line.strip() for line in f]\n\n@task\ndef process(data: list, multiplier: int = 1) -&gt; list:\n    return [item * multiplier for item in data]\n\n@task\ndef save(data: list, output_path: str) -&gt; None:\n    with open(output_path, \"w\") as f:\n        f.write(\"\\n\".join(data))\n\n@pipeline\ndef data_pipeline(input_file: str, output_file: str, multiplier: int = 2):\n    \"\"\"Process data with a multiplier.\"\"\"\n    data = load_data(path=input_file)\n    processed = process(data=data, multiplier=multiplier)\n    return save(data=processed, output_path=output_file)\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#running-from-command-line","level":3,"title":"Running from Command Line","text":"<pre><code># Run with required parameters\ndaglite run myproject.pipelines.data_pipeline \\\n    --param input_file=data.txt \\\n    --param output_file=output.txt\n\n# Override default parameters\ndaglite run myproject.pipelines.data_pipeline \\\n    --param input_file=data.txt \\\n    --param output_file=output.txt \\\n    --param multiplier=5\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#running-from-python","level":3,"title":"Running from Python","text":"<pre><code># Build and evaluate in one step\ndata_pipeline.run(\n    input_file=\"data.txt\",\n    output_file=\"output.txt\",\n    multiplier=5,\n)\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#command-reference","level":2,"title":"Command Reference","text":"","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#daglite-run","level":3,"title":"<code>daglite run</code>","text":"<p>Execute a pipeline from the command line.</p> <pre><code>daglite run [OPTIONS] PIPELINE\n</code></pre> <p>Arguments:</p> <ul> <li><code>PIPELINE</code> - Dotted path to pipeline function (e.g., <code>myproject.pipelines.my_pipeline</code>)</li> </ul> <p>Options:</p> <ul> <li><code>--param NAME=VALUE</code>, <code>-p NAME=VALUE</code> - Pipeline parameter (can be specified multiple times)</li> <li><code>--backend NAME</code>, <code>-b NAME</code> - Execution backend (<code>inline</code>, <code>threading</code>, <code>processes</code>)</li> <li><code>--parallel</code> - Enable sibling parallelism via async evaluation</li> <li><code>--settings NAME=VALUE</code>, <code>-s NAME=VALUE</code> - Override global settings (can be specified multiple times)</li> </ul> <p>Parallel Execution</p> <p>Use <code>--parallel</code> together with <code>--backend threading</code> or <code>--backend processes</code> to run independent sibling tasks concurrently.</p>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#examples","level":2,"title":"Examples","text":"","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#simple-pipeline","level":3,"title":"Simple Pipeline","text":"<pre><code>daglite run myproject.pipelines.simple_pipeline\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#pipeline-with-parameters","level":3,"title":"Pipeline with Parameters","text":"<pre><code>daglite run myproject.pipelines.etl_pipeline \\\n    --param source=raw_data.csv \\\n    --param destination=processed_data.csv \\\n    --param batch_size=1000\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#parallel-execution","level":3,"title":"Parallel Execution","text":"<pre><code>daglite run myproject.pipelines.parallel_pipeline \\\n    --param input_dir=./data \\\n    --parallel\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#custom-backend","level":3,"title":"Custom Backend","text":"<pre><code>daglite run myproject.pipelines.cpu_intensive_pipeline \\\n    --backend multiprocessing \\\n    --settings max_parallel_processes=8\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#multiple-parameters","level":3,"title":"Multiple Parameters","text":"<pre><code>daglite run myproject.pipelines.ml_pipeline \\\n    --param model_path=model.pkl \\\n    --param data_path=train.csv \\\n    --param epochs=50 \\\n    --param learning_rate=0.001 \\\n    --parallel\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#type-conversion","level":2,"title":"Type Conversion","text":"<p>The CLI automatically converts parameter values based on type annotations:</p> <pre><code>@pipeline\ndef typed_pipeline(\n    count: int,           # Converts to int\n    rate: float,          # Converts to float\n    enabled: bool,        # Converts to bool\n    name: str,            # Keeps as string\n    items: list[str],     # Parses JSON list\n):\n    ...\n</code></pre> <pre><code>daglite run myproject.pipelines.typed_pipeline \\\n    --param count=42 \\\n    --param rate=0.95 \\\n    --param enabled=true \\\n    --param name=\"My Pipeline\" \\\n    --param items='[\"a\",\"b\",\"c\"]'\n</code></pre> <p>Type Annotations Required</p> <p>Add type annotations to your pipeline parameters for automatic type conversion. Without annotations, all parameters are passed as strings.</p>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#settings","level":2,"title":"Settings","text":"<p>Override global settings for a single run:</p> <pre><code>daglite run myproject.pipelines.pipeline \\\n    --settings max_backend_threads=16 \\\n    --settings max_parallel_processes=4\n</code></pre> <p>Available Settings:</p> <ul> <li><code>max_backend_threads</code> - Maximum threads for threading backend</li> <li><code>max_parallel_processes</code> - Maximum processes for multiprocessing backend</li> </ul>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#cicd-integration","level":2,"title":"CI/CD Integration","text":"","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#github-actions","level":3,"title":"GitHub Actions","text":"<pre><code>name: Run Pipeline\n\non:\n  schedule:\n    - cron: '0 2 * * *'  # Daily at 2 AM\n\njobs:\n  pipeline:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n      - uses: actions/setup-python@v4\n        with:\n          python-version: '3.10'\n      - run: pip install daglite\n      - run: |\n          daglite run myproject.pipelines.daily_etl \\\n            --param date=$(date +%Y-%m-%d) \\\n            --parallel\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#gitlab-ci","level":3,"title":"GitLab CI","text":"<pre><code>pipeline:\n  script:\n    - pip install daglite\n    - daglite run myproject.pipelines.deployment_pipeline\n        --param environment=production\n        --param version=$CI_COMMIT_TAG\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#best-practices","level":2,"title":"Best Practices","text":"","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#1-use-type-annotations","level":3,"title":"1. Use Type Annotations","text":"<pre><code># Good - automatic type conversion\n@pipeline\ndef pipeline(count: int, threshold: float):\n    ...\n\n# Bad - everything is a string\n@pipeline\ndef pipeline(count, threshold):\n    ...\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#2-provide-docstrings","level":3,"title":"2. Provide Docstrings","text":"<pre><code>@pipeline\ndef data_pipeline(input_path: str, output_path: str):\n    \"\"\"\n    Process data from input_path and save to output_path.\n\n    Args:\n        input_path: Path to input CSV file\n        output_path: Path to save processed data\n    \"\"\"\n    ...\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#3-use-default-values","level":3,"title":"3. Use Default Values","text":"<pre><code>@pipeline\ndef pipeline(required_param: str, optional_param: int = 10):\n    \"\"\"Required params must be passed, optional params have defaults.\"\"\"\n    ...\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#troubleshooting","level":2,"title":"Troubleshooting","text":"","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#unknown-parameter-error","level":3,"title":"\"Unknown parameter\" Error","text":"<p>Ensure parameter names match exactly (case-sensitive):</p> <pre><code># Wrong\ndaglite run myproject.pipeline --param InputFile=data.txt\n\n# Correct\ndaglite run myproject.pipeline --param input_file=data.txt\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#missing-required-parameters-error","level":3,"title":"\"Missing required parameters\" Error","text":"<p>All parameters without default values must be provided:</p> <pre><code>@pipeline\ndef pipeline(required: str, optional: str = \"default\"):\n    ...\n</code></pre> <pre><code># Wrong - missing 'required'\ndaglite run myproject.pipeline --param optional=value\n\n# Correct\ndaglite run myproject.pipeline --param required=value\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#module-not-found-error","level":3,"title":"Module Not Found Error","text":"<p>Ensure the pipeline module is in your Python path:</p> <pre><code># Option 1: Run from project root\ncd /path/to/project\ndaglite run myproject.pipelines.pipeline\n\n# Option 2: Add to PYTHONPATH\nexport PYTHONPATH=/path/to/project:$PYTHONPATH\ndaglite run myproject.pipelines.pipeline\n</code></pre>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/cli/#see-also","level":2,"title":"See Also","text":"<ul> <li>Pipelines User Guide - Learn about the <code>@pipeline</code> decorator</li> <li>Creating Plugins - Extend Daglite with custom hooks</li> </ul>","path":["User guide","Command-Line Interface"],"tags":[]},{"location":"user-guide/composition/","level":1,"title":"Composition Patterns","text":"<p>This page is under construction. You can add detailed documentation about:</p> <ul> <li>Calling tasks directly with <code>task(**kwargs)</code> - Natural call syntax</li> <li><code>.product()</code> - Cartesian product for parameter sweeps</li> <li><code>.zip()</code> - Pairwise operations on sequences</li> <li><code>.partial()</code> - Partial parameter application</li> <li>Combining patterns</li> </ul>","path":["User guide","Composition Patterns"],"tags":[]},{"location":"user-guide/composition/#coming-soon","level":2,"title":"Coming Soon","text":"<p>Check back soon for comprehensive documentation on composition patterns!</p>","path":["User guide","Composition Patterns"],"tags":[]},{"location":"user-guide/evaluation/","level":1,"title":"Evaluation","text":"<p>This page is under construction. You can add detailed documentation about:</p> <ul> <li><code>evaluate()</code> function</li> <li>Async execution</li> <li>Backend configuration</li> <li>Error handling</li> <li>Performance optimization</li> </ul>","path":["User guide","Evaluation"],"tags":[]},{"location":"user-guide/evaluation/#coming-soon","level":2,"title":"Coming Soon","text":"<p>Check back soon for comprehensive documentation on DAG evaluation!</p>","path":["User guide","Evaluation"],"tags":[]},{"location":"user-guide/fluent-api/","level":1,"title":"Fluent API","text":"<p>This page is under construction. You can add detailed documentation about:</p> <ul> <li><code>.then()</code> - Inline chaining</li> <li><code>.map()</code> - Transform each element in a sequence</li> <li><code>.join()</code> - Reduce a sequence to a single value</li> <li>Inline kwargs</li> <li>Chaining patterns</li> </ul>","path":["User guide","Fluent API"],"tags":[]},{"location":"user-guide/fluent-api/#coming-soon","level":2,"title":"Coming Soon","text":"<p>Check back soon for comprehensive documentation on the fluent API!</p>","path":["User guide","Fluent API"],"tags":[]},{"location":"user-guide/pipelines/","level":1,"title":"Pipelines &amp; CLI","text":"<p>This page is under construction. You can add detailed documentation about:</p> <ul> <li><code>@pipeline</code> decorator</li> <li>CLI invocation with <code>daglite run</code></li> <li>Parameter passing</li> <li>Pipeline discovery</li> <li>CLI configuration</li> </ul>","path":["User guide","Pipelines &amp; CLI"],"tags":[]},{"location":"user-guide/pipelines/#coming-soon","level":2,"title":"Coming Soon","text":"<p>Check back soon for comprehensive documentation on pipelines and the CLI!</p>","path":["User guide","Pipelines &amp; CLI"],"tags":[]},{"location":"user-guide/streaming/","level":1,"title":"Streaming with <code>.iter()</code>","text":"<p>Daglite supports lazy streaming of generator-based tasks via <code>.iter()</code>. Instead of materialising an entire sequence into memory before dispatching work, <code>.iter()</code> lets the coordinator iterate the generator on-the-fly and dispatch each yielded item to a backend worker as it arrives.</p>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#why-iter","level":2,"title":"Why <code>.iter()</code>?","text":"<p>Consider a task that produces a large sequence of items:</p> <pre><code>from daglite import task\n\n@task\ndef generate_rows(path: str) -&gt; Iterator[dict]:\n    \"\"\"Stream rows from a large CSV — millions of rows.\"\"\"\n    with open(path) as f:\n        reader = csv.DictReader(f)\n        for row in reader:\n            yield row\n</code></pre>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#without-iter-eager-materialisation","level":3,"title":"Without <code>.iter()</code> — eager materialisation","text":"<pre><code># ❌ Materialises all rows into a list BEFORE any processing starts\nresult = process_row.map(x=generate_rows(path=\"data.csv\")).run()\n</code></pre> <p>Calling the task directly (<code>generate_rows(path=...)</code>) produces a <code>TaskFuture</code> that will materialise the generator into a <code>list</code> at execution time. Every row is held in memory simultaneously before <code>.map()</code> even begins dispatching work.</p>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#with-iter-lazy-streaming","level":3,"title":"With <code>.iter()</code> — lazy streaming","text":"<pre><code># ✅ Rows flow lazily — O(1) coordinator memory\nresult = generate_rows.iter(path=\"data.csv\").then(process_row).run()\n</code></pre> <p>With <code>.iter()</code>, the generator runs on the coordinator and each yielded item is dispatched to a worker immediately. The coordinator never holds more than one item at a time.</p>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#the-fluent-api-recommended","level":2,"title":"The Fluent API (recommended)","text":"<p>The preferred way to use <code>.iter()</code> is the fluent pipeline style:</p> <pre><code>generate_rows.iter(path=\"data.csv\").then(process_row).reduce(accumulate, initial=0).run()\n</code></pre> <p>This reads left-to-right as a data flow:</p> <ol> <li><code>.iter()</code> — lazily yield items from the generator</li> <li><code>.then()</code> — apply a task to each item (fan-out)</li> <li><code>.reduce()</code> — fold results into a single value (fan-in)</li> <li><code>.run()</code> — execute the pipeline</li> </ol>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#available-chaining-methods","level":3,"title":"Available chaining methods","text":"Method Returns Description <code>.then(task)</code> <code>MapTaskFuture</code> Apply task to each yielded item <code>.join(task)</code> <code>TaskFuture</code> Collect all results, pass the list to task <code>.reduce(task, initial=...)</code> <code>ReduceFuture</code> Streaming fold with O(1) memory <code>.save(key)</code> <code>Self</code> Save each yielded item (see Saving items) <code>.run()</code> <code>list[R]</code> Execute and return materialized results","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#chaining-then-calls","level":3,"title":"Chaining <code>.then()</code> calls","text":"<p>Multiple <code>.then()</code> calls chain naturally:</p> <pre><code>@task\ndef double(x: int) -&gt; int:\n    return x * 2\n\n@task\ndef add_ten(x: int) -&gt; int:\n    return x + 10\n\n# generate → double → add_ten → collect\nresult = generate_numbers.iter(n=5).then(double).then(add_ten).run()\n# [10, 12, 14, 16, 18]\n</code></pre>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#using-reduce-for-streaming-aggregation","level":3,"title":"Using <code>.reduce()</code> for streaming aggregation","text":"<p>For commutative/associative operations, use <code>ordered=False</code> for maximum throughput — items are reduced as soon as they complete rather than waiting for iteration order:</p> <pre><code>@task\ndef accumulate(acc: int, item: int) -&gt; int:\n    return acc + item\n\nresult = (\n    generate_numbers.iter(n=1_000_000)\n    .then(double)\n    .reduce(accumulate, initial=0, ordered=False)\n    .run()\n)\n</code></pre>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#using-join-for-collection","level":3,"title":"Using <code>.join()</code> for collection","text":"<p>When you need the full list (e.g. for sorting or aggregation that needs all values at once):</p> <pre><code>@task\ndef summarise(values: list[int]) -&gt; dict:\n    return {\"count\": len(values), \"total\": sum(values)}\n\nresult = generate_numbers.iter(n=100).then(double).join(summarise).run()\n</code></pre>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#alternative-map-with-iter","level":2,"title":"Alternative: <code>.map()</code> with <code>.iter()</code>","text":"<p>You can also pass an <code>.iter()</code> future as a mapped argument to <code>.map()</code>:</p> <pre><code>result = double.map(x=generate_numbers.iter(n=100)).run()\n</code></pre> <p>This produces the same optimised graph as <code>.iter().then()</code>. However, the fluent form is preferred because:</p> <ul> <li>It reads as a natural left-to-right pipeline</li> <li>It avoids the restriction that <code>.iter()</code> must be the only mapped argument   (mixing <code>.iter()</code> with other mapped kwargs like <code>y=[1, 2, 3]</code> raises a   <code>ParameterError</code>)</li> </ul> <p>The <code>.map()</code> form is useful for programmatic graph construction where the mapping target is determined dynamically.</p>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#saving-yielded-items","level":2,"title":"Saving yielded items","text":"<p>Use <code>.save()</code> on the iter future to persist each yielded item with an <code>{iteration_index}</code> key:</p> <pre><code>from daglite.datasets.store import DatasetStore\n\nstore = DatasetStore(\"/tmp/pipeline_output\")\n\nresult = (\n    generate_rows.iter(path=\"data.csv\")\n    .save(\"row_{iteration_index}\", save_store=store)\n    .then(process_row)\n    .reduce(accumulate, initial=0)\n    .run()\n)\n\n# Saved: row_0, row_1, row_2, ... (one per yielded item)\n</code></pre> <p>Each yielded item is saved before it is dispatched to a worker, so saves happen on the coordinator as the generator produces items. This works identically whether graph optimisation is enabled or disabled.</p>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#key-template-variables","level":3,"title":"Key template variables","text":"<p>The following variables are available in save key templates:</p> Variable Description <code>{iteration_index}</code> Zero-based index of the yielded item <code>{param_name}</code> Any parameter passed to <code>.iter()</code> (e.g. <code>{path}</code>)","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#partial-tasks-with-iter","level":2,"title":"Partial tasks with <code>.iter()</code>","text":"<p><code>.iter()</code> works with <code>PartialTask</code> — pre-fix some parameters and iterate the rest:</p> <pre><code>@task\ndef generate_range(start: int, n: int) -&gt; Iterator[int]:\n    for i in range(start, start + n):\n        yield i\n\npartial_gen = generate_range.partial(start=100)\nresult = partial_gen.iter(n=50).then(double).run()\n</code></pre>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/streaming/#how-it-works","level":2,"title":"How it works","text":"<p>Under the hood, <code>.iter()</code> creates an <code>IterNode</code> in the graph IR. When the graph optimizer runs (enabled by default), it detects <code>IterNode → MapTaskNode</code> chains and folds them into a single <code>CompositeMapTaskNode</code>. The composite executes the generator on the coordinator and dispatches each yielded item to a backend worker, avoiding the overhead of materialising the full sequence.</p> <p>When optimisation is disabled, the <code>IterNode</code> falls back to running the generator on the backend where it is materialised into a list — functionally identical to calling the task directly, but <code>.save()</code> still persists items individually.</p> <pre><code>┌─────────────────── Optimised (default) ───────────────────┐\n│                                                           │\n│  Coordinator          Workers                             │\n│  ┌──────────┐                                             │\n│  │ IterNode │─ yield 0 ──▶ [worker: double(0)] ──▶ 0     │\n│  │ (gen fn) │─ yield 1 ──▶ [worker: double(1)] ──▶ 2     │\n│  │          │─ yield 2 ──▶ [worker: double(2)] ──▶ 4     │\n│  └──────────┘                                    ▼        │\n│                                         reduce(acc, item) │\n│                                                           │\n└───────────────────────────────────────────────────────────┘\n</code></pre>","path":["User guide","Streaming with .iter()"],"tags":[]},{"location":"user-guide/tasks/","level":1,"title":"Tasks","text":"<p>Tasks are the fundamental building blocks of Daglite DAGs. This guide covers everything you need to know about defining and using tasks.</p>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#defining-tasks","level":2,"title":"Defining Tasks","text":"","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#basic-task","level":3,"title":"Basic Task","text":"<p>Use the <code>@task</code> decorator to convert a function into a task:</p> <pre><code>from daglite import task\n\n@task\ndef add(x: int, y: int) -&gt; int:\n    \"\"\"Add two numbers.\"\"\"\n    return x + y\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#task-with-options","level":3,"title":"Task with Options","text":"<p>Customize task behavior with decorator parameters:</p> <pre><code>@task(\n    name=\"custom_add\",\n    description=\"Adds two numbers with custom config\",\n    backend=\"threading\"\n)\ndef add(x: int, y: int) -&gt; int:\n    return x + y\n</code></pre> <p>Options:</p> <ul> <li><code>name</code> - Custom task name (default: function name)</li> <li><code>description</code> - Task description (default: docstring)</li> <li><code>backend</code> - Execution backend: <code>\"inline\"</code>, <code>\"threading\"</code>, or <code>\"multiprocessing\"</code></li> </ul>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#task-signatures","level":2,"title":"Task Signatures","text":"","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#type-annotations","level":3,"title":"Type Annotations","text":"<p>Tasks should have full type annotations for best IDE support:</p> <pre><code>@task\ndef process(data: pd.DataFrame, threshold: float = 0.5) -&gt; dict[str, float]:\n    \"\"\"Process a dataframe and return statistics.\"\"\"\n    return {\n        \"mean\": data[\"value\"].mean(),\n        \"count\": len(data[data[\"value\"] &gt; threshold])\n    }\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#_1","level":3,"title":"Tasks","text":"<p>Default Parameters</p> <p>Tasks can have default values:</p> <pre><code>@task\ndef scale(x: float, factor: float = 1.0) -&gt; float:\n    return x * factor\n\n# Use default\nresult = scale(x=5.0)  # factor=1.0\n\n# Override default\nresult = scale(x=5.0, factor=2.0)  # factor=2.0\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#task-futures","level":2,"title":"Task Futures","text":"<p>When you <code>()</code> a task, you get a <code>TaskFuture</code> - a lazy representation of the task's future result:</p> <pre><code>@task\ndef compute() -&gt; int:\n    return 42\n\nfuture = compute()  # Returns TaskFuture[int]\nprint(type(future))      # &lt;class 'daglite.futures.TaskFuture'&gt;\n\nresult = evaluate(future)  # Executes and returns 42\n</code></pre> <p>Futures are lazy</p> <p>Calling the task does not execute it. It only builds the DAG. Execution happens when you call <code>evaluate()</code>.</p>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#partial-application-with-partial","level":2,"title":"Partial Application with <code>.partial()</code>","text":"<p>Fix some parameters while leaving others unbound, creating a reusable task template:</p> <pre><code>@task\ndef power(base: int, exponent: int) -&gt; int:\n    return base ** exponent\n\n# Create a \"square\" function\nsquare = power.partial(exponent=2)\n\n# Use it in different contexts\nresult1 = square(base=5)                # 25\nresult2 = square.product(base=[1,2,3])  # [1, 4, 9]\nresult3 = square(base=7)                # 49\n</code></pre> <p>This pattern is especially useful for creating reusable task templates:</p> <pre><code># Create a scorer with partially-applied parameters\nscorer = score.partial(threshold=0.5, metric=\"accuracy\")\n\n# Reuse with different models\nfuture1 = scorer(model=model_a)\nfuture2 = scorer(model=model_b)\nfuture3 = scorer(model=model_c)\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#task-composition","level":2,"title":"Task Composition","text":"<p>Tasks can depend on other tasks:</p> <pre><code>@task\ndef fetch() -&gt; dict:\n    return {\"value\": 42}\n\n@task\ndef extract(data: dict) -&gt; int:\n    return data[\"value\"]\n\n@task\ndef double(x: int) -&gt; int:\n    return x * 2\n\n# Connect tasks\nfetched = fetch()\nextracted = extract(data=fetched)  # depends on fetched\ndoubled = double(x=extracted)      # depends on extracted\n\nresult = evaluate(doubled)  # 84\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#execution-backends","level":2,"title":"Execution Backends","text":"","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#inline-default","level":3,"title":"Inline (Default)","text":"<p>Tasks run one at a time in the main thread:</p> <pre><code>@task  # backend=\"inline\" by default\ndef task1() -&gt; int:\n    return 42\n</code></pre> <p>Use when: Tasks are fast or order matters</p>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#threading","level":3,"title":"Threading","text":"<p>Tasks run in separate threads (good for I/O-bound work):</p> <pre><code>@task(backend=\"threading\")\ndef fetch_url(url: str) -&gt; bytes:\n    return requests.get(url).content\n</code></pre> <p>Use when: I/O-bound operations (network, files)</p>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#multiprocessing","level":3,"title":"Multiprocessing","text":"<p>Tasks run in separate processes (good for CPU-bound work):</p> <pre><code>@task(backend=\"multiprocessing\")\ndef compute_fft(data: np.ndarray) -&gt; np.ndarray:\n    return np.fft.fft(data)\n</code></pre> <p>Use when: CPU-bound operations (computation, data processing)</p> <p>Mixing Backends</p> <p>You can mix backends in the same DAG. Daglite will handle the coordination.</p>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#task-options","level":2,"title":"Task Options","text":"","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#modifying-task-options","level":3,"title":"Modifying Task Options","text":"<p>Change task configuration after creation:</p> <pre><code>@task\ndef process(data: list) -&gt; list:\n    return [x * 2 for x in data]\n\n# Create variant with different backend\nprocess_parallel = process.with_options(\n    name=\"process_parallel\",\n    backend=\"threading\"\n)\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#best-practices","level":2,"title":"Best Practices","text":"","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#do","level":3,"title":"✅ Do","text":"<ul> <li>Use type annotations - Enables IDE support and type checking</li> <li>Keep tasks pure - Avoid side effects when possible</li> <li>Use descriptive names - Makes DAGs easier to understand</li> <li>Document with docstrings - Helps with generated documentation</li> </ul> <pre><code>@task\ndef calculate_discount(\n    price: float,\n    discount_rate: float,\n    minimum_price: float = 0.0\n) -&gt; float:\n    \"\"\"\n    Calculate discounted price.\n\n    Args:\n        price: Original price\n        discount_rate: Discount percentage (0.0 to 1.0)\n        minimum_price: Floor price\n\n    Returns:\n        Discounted price, clamped to minimum\n    \"\"\"\n    discounted = price * (1 - discount_rate)\n    return max(discounted, minimum_price)\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#dont","level":3,"title":"❌ Don't","text":"<ul> <li>Avoid global state - Makes tasks hard to test and reason about</li> <li>Don't use mutable defaults - Can cause unexpected behavior</li> <li>Avoid heavy computation in task definition - Keep it in the task body</li> </ul> <pre><code># ❌ Bad: Global state\ncounter = 0\n\n@task\ndef increment() -&gt; int:\n    global counter\n    counter += 1\n    return counter\n\n# ✅ Good: Explicit state\n@task\ndef increment(counter: int) -&gt; int:\n    return counter + 1\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#testing-tasks","level":2,"title":"Testing Tasks","text":"<p>Tasks are just functions, so they're easy to test:</p> <pre><code>@task\ndef add(x: int, y: int) -&gt; int:\n    return x + y\n\ndef test_add():\n    # Test the underlying function\n    assert add.func(2, 3) == 5\n\n    # Or evaluate the task\n    result = evaluate(add(x=2, y=3))\n    assert result == 5\n</code></pre>","path":["User guide","Tasks"],"tags":[]},{"location":"user-guide/tasks/#next-steps","level":2,"title":"Next Steps","text":"<ul> <li>Learn about composition patterns - <code>()</code>, <code>.product()</code>, <code>.zip()</code></li> <li>Explore the fluent API - <code>.then()</code>, <code>.map()</code>, <code>.join()</code></li> <li>See real examples - ETL pipelines and more</li> </ul>","path":["User guide","Tasks"],"tags":[]}]}