"""
Daglite execution backend implementations.

Note: This module is not intended to be imported directly (either in user or internal code).
See `BackendManager` in `daglite.backends.manager` for access to backends.
"""
